# Node
node_modules/
npm-debug.log
yarn-error.log

# Angular
.angular/
dist/
tmp/
out-tsc/
bazel-out/

# Entornos y credenciales
.env
.env.*
!.env.example

# IDEs y Editores
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Agentes de IA e Indexed (Koa Blueprint)
.mcp.json
.claude/logs/
.claude/temp/
.gemini/

# Playwright MCP
.playwright-mcp/
playwright-screenshots/
**/*.playwright.png
**/*.playwright.jpg

# QA artifacts — screenshots de auditoría visual (no van al repo)
docs/qa-ux/
/*.png
/*.jpg
/*.jpeg
/*.webp

# SDD — specs/fixes/hotfixes SÍ se commitean (así todos los agentes del equipo
# ven el trabajo de todos). Ver specs/AUTHORS.md para la convención de autoría.
# Lo único local por dev/máquina es qué track tengo activo YO ahora mismo:
specs/.active

# Código de autor (1 letra) de esta máquina/dev. Ver plantilla en
# .claude/author.local.json.example — cada dev crea el suyo, no se comparte.
.claude/author.local.json

# Supabase CLI — estado local de qué branch de Supabase tienes activo
# (supabase/.temp/ SÍ se commitea a propósito: project-ref/versiones del equipo)
supabase/.branches/

environments/
docs/2020libros-curso-profesional.pdf
docs/2020libros-curso-profesional.xlsx
docs/2TESTPSICOLOGICO.doc
.claude/settings.local.json
