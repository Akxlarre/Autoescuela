# Roadmap SDD — Autoescuela

> Índice vivo de todas las specs del proyecto.
> Mantener actualizado: cada spec nueva o cambio de estado se refleja acá.

---

## Activa

| ID | Título | Owner | Activada |
|----|--------|-------|----------|

---

## Backlog

| ID | Título | Prioridad | Owner | Notas |
|----|--------|-----------|-------|-------|
| [0005](./specs/0005-b-publicacion-automatica-cpanel-self-hosted/spec.md) | Publicación Estática Automática en cPanel Self-Hosted | P1 | Akxlarre | Desactivada por ahora; se implementará tras finalizar el desarrollo visual. |
| [0006](./specs/0006-b-borrado-mensajes-modulo-comunicacion/spec.md) | Borrado de mensajes en módulo de comunicación | P1 | Akxlarre | Criterios acordados. Botón eliminar + filtro 90 días para completadas. |
| [0023](./specs/0023-b-bash-guard-hardening-file-protector/spec.md) | Hardening Bash Guard: File Protector para canal shell | P2 | Akxlarre | Draft. Modelo sugerido: **Fable 5**. El humano aplica el diff (hooks protegidos). Origen: bypass demostrado 2026-07-01. Excluida del batch del 2026-07-01 por decisión del owner. |
| [0007-i](./specs/0007-i-ex-alumnos-content-unificado/spec.md) | Consolidar Ex-Alumnos Clase B en un `*-content` compartido | P2 | i | Draft. Originada de `ASG-b-096`. `admin-ex-alumnos.component.ts` (622 líneas) y `secretaria-ex-alumnos.component.ts` (603 líneas) son ~93% código idéntico. Precedente: `0032-b-pre-inscritos-content-fill-screen`. Absorbe el selector de período duplicado a propósito por `0038-b`. |

---

## En progreso

| ID | Título | % tareas | Última edición |
|----|--------|----------|----------------|

---

## Done

| ID | Título | Cerrada | Verificada por |
|----|--------|---------|----------------|
| [0006-i](./specs/0006-i-app-like-alumno-detalle/spec.md) | App-like: Ficha de Alumno (`/admin/alumnos/:id` + `/secretaria/alumnos/:id`) | 2026-08-30 | ✅ PASA. Originada de ASG-b-085 — la pieza más grande/riesgosa del rollout app-like (1660 líneas, página de mayor tráfico, shared admin+secretaria+Clase B+Profesional), cierra la familia "fichas de detalle de alumno" abierta por `fix-027-i` (piloto instructor). **Pivote final tras 2 rondas de QA visual rechazadas:** se probaron 2 diseños con tabs (4, luego 3) y el owner pidió volver al layout original de 3 columnas horizontales — "no es necesario que quede app-like [vertical], hazlo diferenciando en horizontal como antes". Diseño final: root `--fill-screen` (llena el viewport en desktop), contenido preservado 1:1 en 3 columnas (Info Personal / Progreso / Estado Financiero) dentro de `.bento-banner.bento-fill`, cada columna con scroll interno propio — resuelve el problema original (historial de pagos podía crecer sin límite) sin cambiar la jerarquía visual que el owner conocía. **2 bugs reales de flexbox/grid encontrados y corregidos en `/verify`:** grilla de 12 clases con `grid-cols-2` fijo (heredado de cuando esa card tenía ~600px de ancho) se solapaba al quedar en una columna mucho más angosta — resuelto con `grid-template-columns: repeat(auto-fit, minmax(160px,1fr))` (mide el ancho real del contenedor, no el viewport); trampa `min-height:auto` de flexbox + `overflow:hidden` de `.bento-card` recortaba cards en silencio sin mostrar scrollbar — resuelto con `shrink-0`. 30/30 tests verdes, `lint:arch` exit 0, `npx ng build` limpio. Ver `specs/specs/0006-i-app-like-alumno-detalle/tasks.md` §"Changelog (pivote final)" para el detalle completo — el `acceptance.md` generado para el diseño con tabs (descartado) queda como registro histórico, no como descripción del código actual. |
| [0012-m](./specs/0012-m-persistir-borrador-cierre-caja/spec.md) | Persistir borrador de Arqueo y Cierre de Caja | 2026-08-27 | ✅ PASA — 7/8 AC cumplidos con evidencia directa, 1 parcial no bloqueante (AC-E1, concurrencia real no simulada, ya blindada por constraint único de BD). Originada de un hallazgo de UX durante UAT (F5 perdía el arqueo a medias) + fix-225-m (botón "Cerrar Caja" dentro del drawer). Reutiliza la misma fila de `cash_closings` para el borrador (`status:'draft'`→`'closed'`, sin tabla nueva) con autoguardado debounced (800ms) y botón "Listo"→"Cerrar panel". **2 bugs de RLS reales encontrados y corregidos en la propia sesión, ninguno detectable sin probar contra Supabase real:** (1) `onConflict` de PostgREST no acepta un índice sobre expresión `COALESCE(branch_id,-1)` — resuelto con columna generada `branch_id_key`; (2) `FOR UPDATE USING (...)` sin `WITH CHECK` explícito bloqueaba la propia transición `draft→closed` (403 reproducido en vivo) — resuelto con `WITH CHECK` separado que no repite la condición de `status`. 3 migraciones aplicadas manualmente por el owner (mismo flujo que el resto del proyecto), validadas primero contra Supabase local (Docker) antes de cada aplicación. 42/42 tests del Facade verdes (8 nuevos), suite completa 2234/2235 (1 falla preexistente no relacionada). Ver acceptance.md. |
| [0005-i](./specs/0005-i-app-like-libro-de-clases/spec.md) | App-like: `/admin/libro-de-clases` + `/secretaria/libro-de-clases` | 2026-08-26 | ✅ PASA, visto bueno del owner otorgado tras 2 rondas de ajuste sobre el render real (con datos ficticios: 6 alumnos, 6 semanas). Originada de `ASG-b-086`, último paso (17/17 — **cierra todo el rollout app-like**). Contexto corregido al reclamar: el subnav ya era show/hide real (no scroll-to-anchor) y el bug de skeleton gap (fix-074) ya estaba resuelto — ambos declarados out of scope. Root a `bento-grid--fill-screen-4`; las 7 secciones (cabecera, profesores, alumnos, asistencia, calendario, evaluaciones, resumen) convertidas al mismo patrón `.bento-fill` uniforme — "Calendario de Clases" era la única sin ningún `overflow` acotado. "Control de Asistencia (Firma Diaria)" ganó paginación por semana (stepper prev/next, mismo patrón que `mesAnterior`/`mesSiguiente` de `historial-cuadraturas-content`) en vez de listar todas las semanas apiladas. **1 bug real post-implementación**: la fila del subnav (3ª fila de `--fill-screen-4`, pensada para contenido variable con `1fr`) se estiraba de más al competir por espacio sobrante con la card protagonista, dejando un hueco vacío — corregido con un override local (`grid-template-rows` a `auto` en esa fila, scope por `ViewEncapsulation`, sin tocar el SCSS global ni afectar a `0003-i` que sí necesita esa fila flexible). Nuevo `libro-de-clases.component.spec.ts` (no existía antes): 4 tests de la lógica de paginación. Ver `acceptance.md` para el detalle completo (2 iteraciones documentadas). |
| [0011-m](./specs/0011-m-print-flows-edge-functions/spec.md) | Migrar flujos de impresión client-side a Edge Function | 2026-08-23 | 11/11 AC ✅ PASA. Originada de ASG-i-002 (alcance corregido por el dueño: 3 flujos de impresión — ficha técnica, hoja de ruta, EPQ — no auditoría amplia de lógica de negocio). 3 Edge Functions nuevas (`generate-ficha-tecnica-pdf`, `generate-route-sheet-pdf`, `generate-epq-pdf`) reemplazan HTML client-side + `window.print()`; reutilizan `_shared/pdf-utils.ts`. `EPQ_QUESTIONS` espejado a Deno con test de paridad que detecta divergencia. **QA manual real del owner encontró y forzó a corregir 4 bugs**, ninguno detectable sin ver el PDF renderizado: (1) grilla horaria de Hoja de Ruta hardcodeada 08:00-18:00 nunca coincidía con el horario real de clases (13 bloques de 45min desde `courses.schedule_blocks`); (2) horas sin centrar en su celda; (3) separador entre preguntas del EPQ pegado al texto siguiente (el offset no descontaba la altura del ascendente del glifo — 2 rondas para corregirlo bien); (4) fuente 8pt/interlineado 11pt ilegible para responder a mano, subido a 9pt/13pt (81 preguntas pasan de ~2 a 5 páginas, aceptado a propósito). **Ajuste de UX no planeado, pedido por el owner tras probar el flujo real:** Ficha Técnica y EPQ dejan de abrir la pestaña en blanco antes de tener el PDF listo (evita flash de `about:blank`), con spinner en el botón en su lugar. 175 archivos/2197 tests verdes, `lint:arch` limpio. Ver acceptance.md. |
| [0003-i](./specs/0003-i-app-like-reportes-contables/spec.md) | App-like: reportes contables (`admin` + `secretaria`) | 2026-08-25 | 7/8 AC ✅ PASA (1 no aplicable, documentado). Originada de `ASG-b-082`, dividida en 2 specs junto con `0004-i`. 2 bugs reales encontrados y corregidos en `/verify` (panel de tabs colapsado a 0px, Categorías invisible a 768px de alto) + 3 rondas de ajuste por feedback visual directo del owner sobre el render real (compacidad de tabs, Gastos Fijos pasó de sección fija a 4º tab, balance de espacio entre Categorías y el panel). Ver acceptance.md. |
| [0004-i](./specs/0004-i-app-like-cuadratura/spec.md) | App-like: cuadratura (`admin` + `secretaria`) | 2026-08-25 | ✅ PASA, visto bueno del owner otorgado tras 7 rondas de ajuste sobre el render real. Originada de `ASG-b-082`, dividida en 2 specs junto con `0003-i`. Rediseño mayor: "Arqueo y Cierre Operativo" pasó de card fijo (rompía el layout al activar el contador de billetes/monedas) a Drawer, con trigger movido al Hero como botón compacto tras descartar 2 layouts intermedios (2 columnas fijas, luego 2 columnas con Arqueo como card lateral) por feedback directo del owner ("sin dividir la pantalla mitad egreso, mitad registros"). Layout final: 1 sola columna ancho completo, Ingresos protagonista (`flex:3`) sobre Egresos (`flex:2`), header de tabla fijo sin scrollear con las filas. Nueva capacidad de infraestructura retrocompatible: `LayoutDrawerService`/`Facade` y `GsapAnimationsService` ganan un `width?` opcional (sin impacto en los ~40 drawers existentes que no lo pasan). **1 bug real de anidación de DOM** (div sin cerrar dejaba Egresos renderizado como hijo de Ingresos, causando solapamiento visual) encontrado por inspección de `getBoundingClientRect`/`parentElement` en vivo, no por lectura de código — el build no lo detectaba porque el conteo de etiquetas seguía balanceado en otro punto del archivo. Pulido final: estados vacíos de Ingresos/Egresos unificados en `app-empty-state` (antes cada uno con markup distinto), botón "Agregar Egreso" con la misma opacidad disabled que "Agregar Ingreso", y confirmación (`ConfirmModalService`, severity danger) antes de "Cerrar Caja" por ser una acción irreversible. Ver acceptance.md. |
| [0038-b](./specs/0038-b-filtro-periodo-listas-sin-techo/spec.md) | Ventana de período en listas históricas (sin atrapar la búsqueda) | 2026-08-22 | 11/11 AC ✅ PASA · visto bueno del owner otorgado 2026-08-23 (sobre descripción escrita, no sobre capturas — el panel del navegador no renderizó del lado del agente; ver §"Procedencia del visto bueno" en acceptance.md). Originada en `ASG-b-087`; **convertida desde `fix-149-b`** por alcance (9 archivos, 4 superficies) — la conversión no cambió una línea de código. El AC central (la búsqueda atraviesa siempre la ventana) vive en una función pura, así que las 4 superficies lo heredan por construcción. 2 decisiones del owner durante la implementación: nota "Buscando en todo el historial" en vez de deshabilitar el selector, y **unificar el filtro de año** dentro del selector en Clase B — esto último no era cosmético: eran dos controles de tiempo compitiendo y elegir un año viejo devolvía 0 filas. 2 hallazgos no previstos: `fechaEgreso` con precisión de día (sin él la ventana erraba hasta 11 meses) y que **el riesgo de export truncado no existía** en ninguna superficie (2 sin export, 1 server-side). Deuda aceptada: selector cableado 2 veces en Clase B por duplicación preexistente → `ASG-b-096`. 2229/2229 tests, `lint:arch` 0 errores. Ver acceptance.md. |
| [0010-m](./specs/0010-m-consentimiento-test-psicometrico/spec.md) | Consentimiento del test psicométrico EPQ (Ley 21.719, Art. 16) | 2026-08-18 | 8/8 AC + edge cases ✅ PASA. Dependencia dura de la `0009-m`: reutiliza `ConsentsFacade`, `consent-builder.utils` y `persistConsents` de la EF sin duplicar nada — Talla S, sin facade ni componente nuevo. **Gate de persistencia en dos capas** (cliente en `PublicEnrollmentFacade.submitPreInscription()` + servidor en `handleSubmitPreInscription()`, la EF nunca confía en `skipPsychTest` del payload): **verificado empíricamente contra Supabase local** (`npx supabase db reset` + `functions serve` + invocación real) — sin consentimiento otorgado, `psych_test_answers` queda `NULL` aunque el body traiga las 81 respuestas; la negativa se registra igual en `consents` (no como fila ausente). AC1/AC2 verificados con Playwright MCP contra `ng serve` real (flujo público completo hasta `psych-test-intro`); AC5 (panel en el drawer de admin) verificado manualmente por el owner. 3 decisiones de negocio cerradas antes de planificar (spec §9): revocación conserva las respuestas (el veredicto ya emitido es parte del expediente), el aviso advierte que "no apto" puede impedir la matrícula, retención 12 meses igual que el resto de la preinscripción. Migración aplicada en Supabase local y en producción (verificado con `supabase db diff --linked`, sin drift en `consent_type`). 2190/2190 tests verdes, `lint:arch` 0 errores. Ver acceptance.md. |
| [0009-m](./specs/0009-m-consentimiento-ley-21719/spec.md) | Registro de consentimiento y deber de información (Ley 21.719) | 2026-08-18 | 7/7 AC + 3/3 edge cases vigentes ✅ PASA (AC-E2 eliminado: describía un flujo —matrícula en ficha de papel— que el negocio confirmó que nunca ocurre). Tabla `consents` **append-only por construcción**: sin policy de DELETE en ningún rol, trigger que rechaza todo UPDATE salvo `revoked_at`, y `anon` sin policy + `REVOKE ALL` (el flujo público escribe vía Edge Function con `service_role`). El consentimiento **nunca es fire-and-forget**: si no se registra, la matrícula no se completa —en los 4 caminos—. AC1, AC2, AC4 y AC6 verificados en navegador con Playwright. **Tres ACs se corrigieron durante la ejecución, todos por premisas falsas de la spec original:** AC4 (el certificado médico no se pide en matrícula, solo entra por el DMS para justificar inasistencias); AC7 (los menores no pueden matricularse online — `canAdvanceFn` los bloquea en el paso 1 — así que vive solo en secretaría, y el apoderado se acredita con la autorización notarial, no recolectando su RUT); **AC1 (el aviso del Art. 14 ter estaba en una pantalla que solo ve la secretaria — lo detectó el owner; se movió a la cláusula QUINTA del contrato que el alumno firma, que además es prueba acreditable, y de paso apareció que citaba la Ley 19.628, derogada).** **6 bugs reales, ninguno detectable por tests unitarios:** (1) trigger `SECURITY DEFINER` donde `current_user` es el dueño → toda matrícula online habría guardado la IP de un datacenter de Supabase; (2) el panel de consentimientos mostraba un fallo de consulta como "este titular nunca consintió" —la conclusión opuesta a la verdad, en la pantalla con que se le responde a la Agencia—; (3) el giro de una sociedad copiado en la política de la otra; (4) `*/` dentro de un JSDoc cerrando el comentario; (5) el aviso no se renderizaba para secretarias (slug derivado de un input que solo recibe el admin); (6) `CREATE TABLE IF NOT EXISTS` no es idempotente sobre una tabla con otra forma — la migración falló al aplicarse en la nube y necesitó una correctiva. **Hallazgo fuera de alcance → track `0010-m`:** la preinscripción profesional guarda 81 respuestas del test psicométrico EPQ (salud psíquica) sin consentimiento; la corrida 1 de la auditoría no lo detectó. Expediente `.compliance/` actualizado (corrida 2). **2 pendientes del negocio, rastreados en constantes con test:** región de Supabase (`SUPABASE_HOSTING_COUNTRY`) y dominio público (`POLICY_BASE_URL`, se imprime en el contrato). 2184 tests verdes, `lint:arch` 0 errores. Ver acceptance.md. |
| [0036-b](./specs/0036-b-instructor-alumnos-canon-lista-cards/spec.md) | "Mis Alumnos" (instructor) alineado al canon dual-viewport de Base Alumnos | 2026-08-12 | 5/5 AC + 2/2 edge cases ✅ PASA (2 rondas). `InstructorAlumnosComponent` migrado de cards-siempre a dual-viewport real de `alumnos-list-content` (Base Alumnos): `<p-table>` con paginador, header `micro-label`, avatar plano, filas `list-item-hover`, un solo card unificado (toolbar+tabla), `@container` a 900px. **1ª ronda usó por error el patrón de `admin-instructores` (tabla hand-rolled sin paginar, breakpoint 850px) — el owner lo detectó en QA ("la tabla no es la canon") y se corrigió en la misma sesión** (ver nota en acceptance.md). Switch por ancho de contenedor verificado con el drawer de detalle abierto, sin `force-compact`. Cero cambios de Facade/modelo. |
| [0037-b](./specs/0037-b-evaluacion-practica-reevaluacion-exception/spec.md) | Reevaluación de la excepción app-like en Evaluación Práctica | 2026-08-12 | 2/2 AC ✅ PASA. Investigación (no rediseño): el espacio vacío en `/instructor/alumnos/:id/evaluacion/:sessionId` es `max-w-4xl mx-auto` centrando un formulario corto a propósito, no un fill-screen roto — aplicar `--fill-screen` lo empeoraría. Excepción re-confirmada en `indices/APP-LIKE-ROLLOUT.md` con criterio #1+#2. Sin cambios de código. |
| [0008-m](./specs/0008-m-app-like-matriz-notas-evaluaciones/spec.md) | App-like: matriz de notas (Evaluaciones profesional) — convergencia total admin/secretaria + unificación de nombre y rutas | 2026-08-10 | 11/11 AC ✅ PASA, validado visualmente por el owner en vivo (7 rondas de QA). Originada de ASG-b-080. Extraído `app-evaluaciones-profesional-content` (Dumb compartido) — 2 casi-clones (828 + 758 líneas) que divergían en silencio pasan a garantizar paridad admin/secretaria **por construcción**, no por disciplina manual; corrigió de paso un bug real (badge sin ícono/texto separados en secretaria). Nombre unificado a "Evaluaciones" en menú/título/ruta (antes "Calificaciones" en secretaria, ruta `/profesional/notas` → `/profesional/evaluaciones`, sin redirect por decisión del owner). App-like fill-screen en ambos modos (aterrizaje/grilla) sin romper el scroll bidireccional sticky preexistente. **3 bugs reales encontrados y corregidos en QA visual, ninguno detectable por tests unitarios:** (1) wrapper `.bento-fill` sin `.bento-banner` colapsaba a 1 columna del grid; (2) `.bento-fill` sin `shrink-0` en las tarjetas de promoción comprimía su alto por debajo del natural y `overflow-hidden` recortaba contenido en silencio (curso, progreso, alumnos quedaban invisibles); (3) orden de cursos/promociones no determinista por falta de `.sort()` explícito en `buildLanding()` (Supabase no garantiza orden de `promotion_courses`/`professional_promotions`). **Diseño iterado en vivo con el owner tras el cierre inicial de Fase 4:** skeleton genérico reemplazado por 2 skeletons fieles a la forma real; paginación de 2 promociones a la vez en layout desktop (`p-paginator`, vía `LayoutService.tier()` por contenedor — revierte a scroll libre sin paginar con el drawer abierto); rediseño a una sola card contenedora (antes cada promoción + el paginador quedaban visualmente desconectados); promociones activas siempre primero; botón "volver" migrado al hero (`backClickable`) reemplazando un botón propio. 63/63 tests del área tocada verdes en la última corrida, suite completa 1936+/1936+ verificada en rondas previas, `lint:arch` sin regresiones (1 hallazgo real corregido: ARCH-15 pills ad-hoc → `<app-badge>`). Ver acceptance.md. |
| [0007-m](./specs/0007-m-dms-documentos-por-matricula/spec.md) | DMS: documentos por matrícula (no por alumno) + unificación de carpetas de Storage | 2026-08-09 | 9/9 AC ✅ PASA. Originada de fix-146-m (excedía alcance de fix). `studentsWithDocs()` pasa a agrupar por `(studentId, enrollmentId)` — un alumno con 2+ matrículas produce 2+ filas, cada una con `matriculaNumber`/`branchName` resueltos desde `enrollments.branch_id` (no `users.branch_id`, pueden diferir en re-matrículas a otra sede). Selector genérico de subida deduplicado por alumno + segundo selector de matrícula cuando hay 2+ (AC-E2, verificado visualmente por el owner). Uploads unificados a `students/{enrollmentId}/...` (antes `student-docs/{studentId}/...`). **Hallazgo durante implementación:** Supabase bloquea `DELETE` directo sobre `storage.objects` ("Direct deletion from storage tables is not allowed") — la migración SQL de limpieza planeada no era viable; se corrigió moviendo la limpieza (barrido + `.remove()`) al mismo `scripts/migrate-student-docs-storage.mjs` vía Storage API. `npm run test:ci` y `lint:arch` verdes, QA visual completo con Playwright MCP (admin, `ng serve` real). Ver acceptance.md. |
| [0006-m](./specs/0006-m-matricula-refuerzo-clase-b/spec.md) | Matrícula de refuerzo Clase B (6 clases) sin romper el modelo de 12 prácticas | 2026-08-09 | 9/9 AC ✅ PASA, verificado por el owner en producción (wizard, ficha de alumno, portal alumno, certificación, RLS pública, numeración de matrícula compartida). Originada de ASG-b-038. Curso "Refuerzo Clase B" (6 clases, `is_reinforcement`) comparte `license_class='B'` con Clase B estándar para heredar numeración de matrícula y `license_group` sin tocar triggers/RLS — distinguido vía `courses.is_reinforcement`, mismo patrón que SENCE (`findCourseByLicenseClass` con `isReinforcement`). `classCountFromPracticalHours()` reemplaza el hardcodeo de "12" en 6 sitios: gate del certificado (Edge Function + facade), ficha de alumno admin/secretaría, y 3 facades del portal alumno (dashboard/clases/horario) — hallazgos que el plan original no cubría, expandidos con aprobación del owner. **Bug real encontrado y corregido en el QA:** `saveAssignment()` tenía una whitelist de `courseType` que excluía `'class_b_reinforcement'`, saltando en silencio el guardado de `class_b_sessions` — corregido con test de regresión, re-verificado en vivo con matrícula nueva. RLS pública (`select_courses_anon`) excluye el curso del flujo de auto-inscripción externa. ASG-m-001 creada para 3 bugs de `audit_log` detectados en el camino, fuera de alcance. 1896/1896 test:ci, `lint:arch` limpio. Ver acceptance.md. |
| [0035-b](./specs/0035-b-app-like-alumno-dashboard/spec.md) | App-like: `/alumno/dashboard` | 2026-08-08 | ✅ PASA, 8/10 AC con evidencia sólida, confirmado visualmente por el owner ("muchísimo mejor, cerramos"). Originada de ASG-b-083. **3 diseños descartados antes del final** (documentados en spec.md §7 para no repetir el error): modificador CSS propio `--fill-screen-stack` (la fila fill quedaba en 47px), tabs sobre `--fill-screen-kpi` (owner: "no me gustó para nada"), centrado vertical sin restructura (owner: "esa no es la solución"). Diseño final tras auditar redundancia contra el resto del portal alumno (horario/pagos/clases ya mostraban el detalle completo): hero con 4 KPIs (2 clickeables) + 2 columnas que comparten una fila (Mi Progreso | Evaluación) + celda nueva "Camino al Certificado" (tracker de 3 pasos, mismos signals ya usados, sin regla de negocio nueva) — reutiliza `--fill-screen-2` existente, cero CSS nuevo. Bug real preexistente encontrado en `_bento-grid.scss` (`data-col-span`+`data-col-start` combinados perdían el span) y corregido, blast radius acotado a esta página (grep confirmado). Verificado Clase B con datos reales y Profesional con snapshot simulado inyectado client-side (sin cuenta de prueba disponible, sin tocar BD). AC5 (multi-matrícula real), AC6 (mobile real) y AC-E3 (certificado `enabled`) sin verificación visual — cubiertos por construcción, no bloquean el cierre. Ver acceptance.md. |
| [0002-m](./specs/0002-m-promociones-cadencia-automatica/spec.md) | Promociones automáticas: cadencia, matrícula tardía y convalidaciones | 2026-08-07 | 10/10 AC ✅ PASA, verificado visualmente por el owner (drawer de creación + modal de matrícula tardía). Originada de ASG-035. Edge Function `auto-create-next-promotions` (pg_cron diario) mantiene colchón de 1 `in_progress` + 2 `planned`, cadencia 14 días, `class_book` explícito por curso. `computePromotionEndDate()` (`core/utils/promotion-end-date.utils.ts`, portado a Deno en `_shared/holidays.ts`) reemplaza el offset fijo `start+33` por recuperación real de feriados (L-S, hasta 30 días hábiles no-feriado) — corrige también la creación manual (`crearPromocion()`/`previewEndDate()`). Gate de matrícula tardía en `EnrollmentFacade.saveAssignment()` (>3 días desde `start_date` → modal de confirmación, mismo para admin/secretaria). **Corrección de aritmética in-flight:** el plan tenía un error heredado (`start+34`/`+35`) que ignoraba que el día 34 siempre cae domingo (promociones siempre arrancan lunes) — corregido a `start+35`/`+36`, confirmado por el usuario como error propio de una sesión anterior. QA manual exhaustivo de la Edge Function contra Supabase local (idempotencia AC-E1, colchón, `class_book` sin huérfanos, sesiones L-S exactas). 1835/1835 test:ci (9 fallos preexistentes en `flota.facade.spec.ts`, no relacionados), lint:arch limpio. Ver acceptance.md. |
| [0002-i](./specs/0002-i-cuadratura-editable-ajustes/spec.md) | Cuadratura editable vía ajustes + egresos de combustible por vehículo | 2026-08-06 | ⚠️ PARCIAL no bloqueante — 10/10 AC cumplidos con evidencia automatizada (47 tests verdes: Facade `registrarAjuste()`/`totalVigente()`/`fetchAjustes()`, drawer `RegistrarAjusteCuadraturaDrawerComponent` con toggle de signo explícito), pero **sin QA visual en navegador** — cerrada así a pedido explícito del owner por tiempo. Originada de ASG-b-037. Tabla nueva `cuadratura_adjustments` (admin-only, inmutable — sin policy UPDATE/DELETE). Descubierto en el camino: bug real donde `computed()` sobre `FormControl.value` plano nunca se re-evaluaba (migrado a `toSignal(valueChanges)`); Architect Guard bloqueó `inject(LayoutDrawerFacadeService)` en el componente `shared/` (matchea substring "Facade"), resuelto moviendo la apertura del drawer al Facade. Sesión también corrigió 2 hallazgos de UX del owner fuera de esta spec (hotfix-001-i: chip de categoría; hotfix-002-i: fondo inicial hardcodeado en $50.000). QA visual pendiente movida a fix nuevo (mejora visual de "editar cuadratura"). Ver acceptance.md. |
| [0005-m](./specs/0005-m-facades-respuestas-stale/spec.md) | Ningún Facade descarta respuestas "stale" ante cambios rápidos de filtro/sede | 2026-08-05 | 7/7 AC ✅ PASA. Originada de ASG-b-064. `createRequestGuard()` (`core/utils/request-guard.utils.ts`) — guard de `requestId` puro, integrado en `AdminAlumnosFacade` (spike), `DashboardFacade`, `FlotaFacade`, `DmsFacade`; `EnrollmentFacade` excluido a propósito (no sigue el patrón `initialize()`/`fetchXxxData()`/`refreshSilently()`). Descubierto en el camino: `DmsFacade.initialize()` marca `_initialized=true` de forma síncrona antes del `await` (a diferencia de los otros 3), documentado como deuda menor no bloqueante. Patrón documentado en `.claude/rules/facades.md` §7. QA manual (Playwright MCP): cambio rápido de sede en Alumnos admin sin flash de datos viejos, 0 errores de consola en los 4 Facades. 1742/1742 test:ci, `lint:arch` limpio. Ver acceptance.md. |
| [0001-i](./specs/0001-i-ciclo-vida-clase-exclusion-cierre/spec.md) | Ciclo de vida de la clase: exclusión mutua y aviso de cierre atrasado | 2026-08-04 | ⚠️ PARCIAL no bloqueante — 5/7 AC cumplidos con evidencia directa, 2 con gap de verificación (no de implementación): AC1 cruzado (2 Facades protegidas por el mismo trigger de BD, sin cuenta de instructor para probar end-to-end) y AC-E1 (exclusión global por instructor, sin datos multi-sede para confirmar en vivo). Originada de ASG-b-036. Trigger `prevent_concurrent_in_progress_class_b_sessions()` (BEFORE UPDATE en `class_b_sessions`) protege `InstructorClasesFacade.startClass()` y `AsistenciaClaseBFacade.startClass()` sin duplicar validación. Aviso visual "atrasada" (15+ min de retraso en el cierre) nuevo en `app-live-classes-panel`, con tick de UI de 60s (sin fetch). 83/83 tests verdes en los 7 archivos relacionados, `lint:arch` limpio (2 warnings de complejidad preexistentes agravados levemente, documentados como deuda no bloqueante). Ver acceptance.md. |
| [0004-m](./specs/0004-m-instructores-vehiculos-multi-sede/spec.md) | Instructores y vehículos multi-sede ("Ambas") | 2026-07-31 | 12/12 AC ✅ PASA. Originada durante fix-090-m-drawers-scope-sede. Columna `both_branches` (instructors/vehicles), rewrite de `v_class_b_schedule_availability` (disponibilidad cruzada de sede para instructor/vehículo "Ambas"), RLS (`select_instructors`/`instructor_documents`/`insert_vehicles`/`update_vehicles`), `app-branch-scope-selector` ("Sede principal" + checkbox "Ambas") integrado en Crear/Editar Instructor y Crear/Editar Vehículo, columna "Sede" en ambos listados (reemplaza "Tipo" en Instructores). Bug de contraste real de `p-toggleswitch` encontrado y corregido en QA (3 capas apiladas: color de token, tamaño de token, input opaco encima — ver `_primeng-overrides.scss`), confirmado visualmente por el owner. AC4 (disponibilidad cruzada) validado por SQL directo (diff-cero + conteos exactos), aceptado por el owner sin booking end-to-end (BD de dev sin `enrollments` seedeados). **Corrección post-cierre inicial (2026-07-31, owner):** en los drawers de Crear/Editar Instructor, el selector de sede + switch "Ambas" pasaron de solo-deshabilitados a **completamente ocultos** para Secretaria (un control no-apretable seguía siendo confuso en pantalla); pendiente aplicar el mismo criterio al drawer de Vehículo. 1583/1583 test:ci, `lint:arch` 0 errores. Ver acceptance.md. |
| [0003-m](./specs/0003-m-repositorio-documentos-instructores/spec.md) | Repositorio de documentos: sección Instructores + poder abrir el archivo | 2026-07-30 | 7/7 AC ✅ PASA, verificado visualmente por el owner en su propio entorno (Docker + migración aplicados) a lo largo de múltiples rondas de QA en la misma sesión. Tab "Instructores" en el DMS calcado del de alumnos (tabla + N° Licencia + Sede condicional + doc count), migración `instructor_documents` con RLS `branch_visible()`, enum de 7 tipos de documento (Decreto 39/1985 MTT), visor de archivo real vía signed URL. **Pivote UX in-flight (owner):** detalle de documentos pasó de subruta de página completa (como planeaba plan.md) a drawer apilable con back button (`LayoutDrawerService.push()`/`canGoBack()`), aplicado también a alumnos por consistencia. **Scope creep aprobado por el owner:** se extendió a los drawers de Crear/Editar/Ver Instructor (botones para ver/subir documentos reutilizando la misma infraestructura, + corrección del selector de clase de licencia heredado A2-A5 → fijo 'B', ya que `instructors` es exclusivamente Clase B). 15+ bugs de UI/UX reales encontrados y corregidos en QA en vivo (colores, tipos de documento con claves inconsistentes DG-038, host de flex faltante en el preview). 131 archivos / 1556 tests, `lint:arch` 0 errores. Ver acceptance.md. |
| [0034-b](./specs/0034-b-portal-alumno-matriculas-multiples/spec.md) | Portal alumno no muestra matrículas múltiples (H-039 residual) | 2026-07-29 | 4/4 AC + 3/3 edge cases ✅ PASA. Investigación previa reveló que Dashboard/Clases/Horario ya habían resuelto el mismo problema (`StudentEnrollmentContextFacade` + `<app-tabs>`) después de creada la Asignación — alcance acotado a la página Pagos + su Edge Function `student-payment` (nuevo `enrollmentId` opcional en `load-enrollment-status`, mismo chequeo de ownership que `initiate-payment`, OWASP A01). QA en vivo con alumno real de 2 matrículas (`alumno@test.com` + 2ª matrícula temporal, limpiada sin residuos post-verificación) atrapó un gap de proceso real: el Edge Function editado no tenía efecto hasta `npx supabase functions deploy` — el código local nunca se despliega solo. Tras desplegar, los 4 AC pasaron con datos reales (saldo/historial por matrícula seleccionada, 403 en `enrollmentId` ajeno). 1527/1527 test:ci, lint:arch limpio. Ver acceptance.md. |
| [0033-b](./specs/0033-b-asistencia-profesional-fill-screen-tabs/spec.md) | Asistencia Profesional: fill-screen app-like + tabs (Firma semanal \| Resumen) + eliminar duplicado huérfano | 2026-07-21 | 10/10 AC + 4/4 edge cases ✅ PASA, con visto bueno del owner ("sí ciérralo por ahora"). Shell `--fill-screen-kpi` (hero/mapa semanal/panel tabs con scroll interno), duplicado huérfano `SecretariaAsistenciaProfesionalComponent` (585 líneas) + su ruta eliminados, tablas extraídas a Dumb colocated (`FirmaSemanalTableComponent`/`ResumenAlumnosTableComponent`) con pills → `<app-badge>`. **Refinamiento in-flight (owner):** día-cards → `WeekMatrixComponent` (matriz compacta transpuesta, banda de mapa 367→246px, panel de tablas +60%). **Ronda 2 de QA dirigida (owner):** 3 bugs encontrados y corregidos — (1) toolbar de selectores se desbordaba con el drawer de sesión abierto por usar breakpoints de viewport (`xl:`/`sm:`) en vez de contenedor, fix con `LayoutService.tier()` mismo patrón que `ciclos-teoricos-content`; (2) matriz semanal con texto solapado al mismo ancho angosto, fix apilando nombre/fecha; (3) paginación de tablas investigada y descartada como no-bug (cupo real de 25 alumnos/módulo, RF-059, verificado inyectando 25 filas sintéticas en vivo — scroll interno + header sticky sin paginador, igual que el roster de Ciclos). Cero SCSS nuevo, 1350/1350 test:ci, lint:arch idéntico a HEAD, `ng build` limpio. Ver acceptance.md. |
| [0032](./specs/0032-b-pre-inscritos-content-fill-screen/spec.md) | Pre-inscritos: content unificado + app-like fill-screen | 2026-07-17 | 8/8 AC de código + 2 AC visuales (AC3 fill-screen, AC-E1 switch con drawer) con visto bueno visual del owner → ✅ PASA. Nuevo Dumb `pre-inscritos-content` consolida admin+secretaría (~95% duplicados): buscador+filtros+tabla/cards en 1 card. **Paginación `<p-paginator>` de PrimeNG** (12 desktop/6 móvil, pagina tabla y cards, look de Base Alumnos B). **Cards responsive** en móvil / tabla en desktop (switch por CONTENEDOR `isDesktopLayout()`, no `md:`). Fill-screen (cero SCSS nuevo) + hover canon en el card. `<app-badge variant="brand">` para el pill. Ampliación de scope in-flight (paginación + cards + p-paginator, a pedido del owner). 1335/1335 test:ci (spec del Dumb 15/15), build + lint:arch limpios. Ver acceptance.md. |
| [0031](./specs/0031-b-ciclos-teoricos-fill-screen/spec.md) | Ciclos Teóricos: fill-screen app-like + fix del shift de tabs por scrollbar | 2026-07-13 | 5/5 AC + 3/3 edge cases ✅ PASA (AC4 con nota de entorno: viewport fijo del Chrome de automatización → verificado por paridad con 0030 + prueba aislada de estructura). Continuación de 0030 (Ciclos era out-of-scope). El modificador `--fill-screen-kpi` pasa a incondicional (ambos tabs) → **elimina el shift de la fila de tabs** (ya no aparece/desaparece scrollbar de página al alternar). `ciclos-teoricos-content`: host como celda `.bento-fill` (`:host` flex), selector fijo + 2 columnas flex con scroll interno (Clases protagonista `flex-1` / Alumnos rail `w-96`); input `isDesktop` (por contenedor, no `lg:`). Limpieza de `.bento-banner` muertas. Cero SCSS nuevo (AC5). Build + test:ci + lint:arch limpios. **Refinamiento (owner):** fusión del selector de ciclo en el header de la columna Clases (era fila-tarjeta de ~140px de un fill de 457px → Clases +72% visible, 237→407px); badge de estado migrado a `<app-badge>` (limpió ARCH-15). Ver acceptance.md §"Refinamiento post-cierre". |
| [0030](./specs/0030-b-asistencia-b-layout-dual/spec.md) | Asistencia B: layout dual (fill-screen desktop / scroll móvil) + densidad adaptativa | 2026-07-13 | 8/8 AC + 5/5 edge cases ✅ PASA, con visto bueno visual del owner tras **2 rondas de feedback**. Cero SCSS nuevo (reusa `--fill-screen-kpi`; AC4 = diff vacío en `_bento-grid.scss`). **Ronda 1:** alertas de card pesada → fila compacta de 1 línea (detalle en `title`); fix bug real `formatIsoDate`+`timestamptz`. **Ronda 2:** la distribución seguía invirtiendo la jerarquía (alertas apiladas arriba comían el espacio de la tabla, que es la protagonista) → **redistribución a 2 columnas** (tabla `order-1 flex-1` ancha + alertas `<aside order-2 w-80>` rail); switch col/row por CONTENEDOR (`isDesktopLayout()`= `maxVisible()===null`), no por viewport `lg:`, para apilar con el drawer abierto igual que la densidad. Tab Ciclos ya era 2col → intacto. 1293/1293 test:ci, build + lint:arch limpios. Ver acceptance.md §§ "Revisión post-cierre" (×2). |
| [0029](./specs/0029-b-comunicaciones-task-list-consolidacion/spec.md) | Comunicaciones: consolidar 3 implementaciones + patrón dual (spec 0028) | 2026-07-12 | 6/6 AC + 3 edge cases ✅ PASA (2 AC con evidencia unitaria, no end-to-end, declarado). Nuevo `<app-task-list-content>` compartido reemplaza 3 templates casi duplicados (Admin/Secretaria/Instructor). Nuevo modificador CSS `.bento-grid--fill-screen-kpi` (hero+KPI-row+lista). Bug real encontrado y corregido en vivo (selector `.bento-fill` no cubría el modificador nuevo). Login real con los 3 roles vía Playwright. Ver acceptance.md. |
| [0028](./specs/0028-b-layout-responsive-dual-densidad-adaptativa/spec.md) | Layout responsive dual: fill-screen desktop / scroll natural móvil + densidad adaptativa | 2026-07-11 | 13/13 ACs ✅ PASA (verificación en vivo Playwright). Canon `.bento-fill` en `_bento-grid.scss` (contain:size solo lg+), fix capa `.bento-card min-height` scoped, `LayoutService.tier()` por ResizeObserver de `<main>`, presupuestos 4/3/3 dashboard + 6/"Cargar más" alumnos. 1274/1274 tests, lint:arch 0 errores. Rollout a ~28 páginas = deuda declarada. Ver acceptance.md. |
| [0027](./specs/0027-b-notificaciones-ola-4/spec.md) | Notificaciones Ola 4: vencimiento de documentos de flota (D3) | 2026-07-10 | ✅ 5/5 AC + 3/3 edge cases, todos verificados con datos reales vía RPC/REST directo (sin harness — mismo criterio que Spec 0026). Función `notify_vehicle_document_expiry()` + `cron.schedule` diario. Alcance reducido a D3 (D2 diferido documentado, D4 bloqueado, WhatsApp fuera). `lint:arch` sin cambios (0 TS tocado). Datos de prueba limpiados sin residuos. Corrección adicional de documentación: `vehicles.vehicle_id` (inexistente) → `vehicles.id` en `indices/DATABASE.md`. Ver acceptance.md. |
| [0026](./specs/0026-b-notificaciones-ola-3/spec.md) | Notificaciones Ola 3: triggers SQL (clase completada, tareas, aviso 2ª cuota) | 2026-07-10 | ✅ 11/11 ACs, todos verificados con datos reales vía REST directo (no solo tests con mocks — no hay harness para triggers SQL). 2 bugs reales encontrados y corregidos en QA en vivo: guard `payment_mode='deposit'` (valor inexistente, real es `'partial'`) y `reference_id` INT vs UUID de tareas. También se reparó un drift pre-existente de tracking de migraciones remoto (99/139 sin registrar) + 5 archivos con timestamp duplicado, pre-requisito para poder aplicar la spec. `lint:arch` sin cambios (0 TS tocado). Datos de prueba limpiados sin residuos. Ver acceptance.md. |
| [0025](./specs/0025-b-notificaciones-ola-2/spec.md) | Notificaciones Ola 2: circuito financiero (A4-A7) + onboarding (B2, B3) | 2026-07-10 | ✅ 11/12 ACs con evidencia (AC-E3 documentado como no aplicable al dominio real — no bloqueante), 0 fallidos. 100 tests nuevos/actualizados verdes en 7 facades. `lint:arch` 0 errores nuevos (+3 warnings ARCH-10 aceptados, verificado contra HEAD vía `git stash` parcial). `ng build` limpio (encontró y corrigió un TS2345 real en `servicios-especiales.facade.ts`). QA en vivo real (AC4): anticipo de prueba registrado contra la BD de desarrollo, verificado con datos reales vía REST directo (RLS admin lee todas las notifications) que `recipient_id` coincidió exactamente con el instructor correcto, luego eliminado limpiamente sin dejar residuos. Ver acceptance.md. |
| [0024](./specs/0024-b-notificaciones-ola-1/spec.md) | Notificaciones Ola 1: infraestructura de tipos + primeros productores (RF-022) | 2026-07-07 | ✅ 12/12 ACs (10 con test dedicado, 2 por construcción documentada), 0 fallidos. 155 tests nuevos/actualizados verdes en 6 specs (uno creado desde cero: `certificacion-clase-b.facade.spec.ts`, no existía). `lint:arch` sin errores nuevos (verificado contra HEAD vía `git stash`). QA manual Playwright (T5.3) verificado OK por el owner el 2026-07-07. Ver acceptance.md. |
| [0018](./specs/0018-b-guardrails-quick-wins-linter-indexer/spec.md) | Guardrails quick-wins: ARCH-12 (DTO en componentes), ARCH-13 (effect en facades), ARCH-06 inline, PIPES.md | 2026-07-01 | 9/9 ACs. ARCH-12 atrapó las 2 violaciones predichas (corregidas vía re-export en ui/); ARCH-06-inline atrapó 3 [ngClass] reales en live-classes-panel (corregidos). PIPES.md auto-generado (3 pipes). `ng build` limpio. Ver acceptance.md. |
| [0019](./specs/0019-b-theme-whitelist-clases-token/spec.md) | Whitelist de clases token derivada del `@theme` (ARCH-11 v2) | 2026-07-01 | 8/8 ACs. `scripts/lib/theme-tokens.js` + suite 28 casos. Backlog real: 20 clases muertas en 12 archivos (el "~549" ya migrado por fix-030 + inflado por bug de \b). Warning default + `--strict`. Atrapó familias que la lista negra no conocía (`*-dark`, `bg-brand-primary`). Ver acceptance.md. |
| [0020](./specs/0020-b-lint-iconos-lucide-registrados/spec.md) | Cross-reference íconos Lucide usados vs `pick()` (ARCH-14) | 2026-07-01 | 9/9 ACs. `scripts/lib/icon-registry.js` + suite 11 casos. **19 íconos sin registrar detectados y corregidos** (18 registrados + `pen-to-square`→`square-pen`, que no existe en Lucide). 23 huérfanos listados. Ver acceptance.md. |
| [0021](./specs/0021-b-indexer-routes-y-huerfanos/spec.md) | Indexer: ROUTES.md + huérfanos en USAGE-MAP | 2026-07-01 | 9/9 ACs. ROUTES.md: 107 rutas (paths anidados, guards, redirects). Huérfanos: 11 componentes + 2 directivas (spot-checks confirman: `app-kpi-card` real sin uso — solo vive su skeleton). shared/ ahora cuenta como consumidor. Ver acceptance.md. |
| [0022](./specs/0022-b-database-md-desde-migraciones/spec.md) | DATABASE.md auto-generado desde migraciones SQL | 2026-07-01 | 10/10 ACs. `scripts/lib/sql-schema.js` (regex por sentencia, cero deps) + suite 21 casos. 133 migraciones → 76 tablas / 274 policies / 40 funciones / 4 vistas con **0 warnings**. Todas las tablas con RLS. Manual intacto. Ver acceptance.md. |
| [0017](./specs/0017-b-secretaria-multi-sede-grant-admin/spec.md) | Secretaria multi-sede (grant del admin) | 2026-07-01 | Akxlarre |
| [0007](./specs/0007-b-reorganizacion-modular-menu-candado-ajustes/spec.md) | Reorganización Modular del Menú, Candado de Sede y Drawer de Ajustes | 2026-07-01 | Akxlarre |
| [0009](./specs/0009-b-rediseno-ux-flujo-inscripcion-online-publico/spec.md) | Rediseño UX del Flujo de Inscripción Online Público | 2026-07-01 | Akxlarre |
| [0016](./specs/0016-b-base-alumnos-split-b-profesional/spec.md) | Separación de Base de Alumnos en páginas Clase B / Profesional | 2026-06-23 | 15/15 ACs cumplidos (1 con salvedad de scope). 3 fases: Base B acotada a `class_b`, Base Profesional nueva (facade+componente propio, semáforo `v_professional_attendance`, módulos 1–7), Ex-Alumnos split. Admin+Secretaría. 15/15 tests facades, `ng build` limpio, QA Playwright 0 errores consola. Pre-inscritos movido a sección profesional. `secretaria-ex-alumnos` (B) reconstruido de stub a funcional (paridad). Ver acceptance.md. |
| [0015](./specs/0015-b-header-slim-mode/spec.md) | Header Slim Mode (Section Hero Compacto) | 2026-06-18 | 12/12 ACs cumplidos Playwright. `density="slim"` + `kpis` en `app-section-hero`. Dashboard + liquidaciones migrados. row1=59px ≤60px, total=120px ≤120px. `sparkline.utils` 8/8 tests verdes. `ng build` limpio. Ver acceptance.md. |
| [0014](./specs/0014-b-reemplazar-inputs-fecha-nativos-por-p-datepicker/spec.md) | Reemplazar inputs de fecha nativos por p-datepicker del Design System | 2026-06-13 | 18 instancias `type="date"` → `app-date-input` (wrapper `p-datepicker`). 0 nativos restantes en build. Playwright: calendario abre en flujo público `/inscripcion`, junio 2026, locale español, 0 errores consola. `ng build` limpio. |
| [0013](./specs/0013-b-reemplazar-selects-nativos-por-design-system/spec.md) | Reemplazar selects nativos por componentes del Design System | 2026-06-12 | 5 componentes: `p-select` (anticipo ×2, vehículo, tema, curso) + segmented control gender (público + admin). `ng build` limpio. Playwright: segmented control interactivo, dark mode OK, 0 errores consola. |
| [0012](./specs/0012-b-validaciones-ux-flujo-inscripcion-publica/spec.md) | Validaciones UX — Flujo de inscripción pública | 2026-06-12 | 18/19 ACs verificados Playwright. AC19 cubierto por browser nativo + unit tests. `app-phone-input` E.164, dirty-state per-field, dirty-all-on-submit, ARIA WCAG 3.3.1. Ver acceptance.md. |
| [0011](./specs/0011-b-auditoria-ui-ux-global/spec.md) | Auditoría UI/UX Global — responsive, polish, Design System | 2026-06-04 | ⚠️ PARCIAL. Flujo público auditado a fondo (Playwright mobile/tablet/desktop): 0 overflow, touch targets ≥44px, 4 fixes (7cad8de, 721263d) + fix-007/fix-008. AC-R1 retorno success/cancelled = deuda manual (necesita pago real). ACs del panel admin diferidos (fuera de §7). Ver acceptance.md. |
| [0010](./specs/0010-b-hardening-seguridad-flujo-inscripcion-publico/spec.md) | Hardening de Seguridad del Flujo de Inscripción Online Público | 2026-06-04 | 7/10 ACs verificados en cloud. AC-F1/F2 (E2E Webpay completo) documentados como deuda manual. |
| [0008](./specs/0008-b-eliminar-estilos-inline-clases-semanticas/spec.md) | Eliminar estilos inline — migrar a clases semánticas del design system | 2026-05-28 | Akxlarre (AC5 dark mode pendiente QA visual manual) |
| [0001](./specs/0001-b-sistema-de-tareas-multi-rol/spec.md) | Sistema de tareas y observaciones multi-rol | 2026-05-18 | Akxlarre (T6.3/T6.4 pendientes QA manual con Supabase local) |
| [0002](./specs/0002-b-instructor-tareas-vista-por-tipo/spec.md) | Instructor: Vista de Tareas con Diferenciación por Tipo | 2026-05-22 | Akxlarre (T3.3 QA manual pendiente usuario) |
| [0003](./specs/0003-b-landing-pages-panel-control-autoescuelas-chillan/spec.md) | Landing Pages & Panel de Control — Autoescuelas Chillán | 2026-05-22 | Akxlarre (8/8 tests automatizados vitest verdes) |
| [0004](./specs/0004-b-refactor-website-config-courses-fk/spec.md) | Refactor website_config.courses → FK al catálogo operacional | 2026-05-23 | Akxlarre (build + Astro build limpios; 24/24 tests facade verdes; QA manual pendiente con Supabase local) |

---

## Archived (descartadas / superseded)

| ID | Título | Motivo | Reemplazada por |
|----|--------|--------|-----------------|

---

## Convenciones

- **IDs:** `NNNN-kebab-slug`, secuencial, nunca se reutilizan
- **Prioridad:** P0 (bloquea producción) / P1 (necesario sprint) / P2 (nice-to-have)
- **Estado:** draft → approved → in_progress → done | archived
- **Owner:** quién es responsable de redactar + cerrar la spec
