# Tasks 0021-b

- [x] T1 — `collectRoutes()` en indices-sync.js (AST: paths anidados, loadComponent lazy, guards, redirects)
- [x] T2 — `generateRoutesTable` + `indices/ROUTES.md` nuevo con marcadores
- [x] T3 — `collectUsageMap`: escanear `shared/` como consumidor + exclusión de self-match
- [x] T4 — Huérfanos en `main()` (usage=0 + no enrutados) + sección con disclaimer en USAGE-MAP
- [x] T5 — (descubierta) Registrar también rutas-grupo con guards propios (sin componente) para no perder `hasRoleGuard(...)` de /app/admin etc.
- [x] T6 — Verificación: fidelidad vs app.routes.ts, idempotencia, spot-checks de huérfanos
