# Índice de Facades (Facades Index)

Este índice mantiene el registro de todas las Fachadas (Facades) del sistema.
Los Facades son el **único punto de entrada** permitido para que la UI interactúe con el dominio (base de datos) y su estado global.

## 📁 Facades Activos (`core/facades/`)

| Facade | Archivo | Responsabilidad (Dominio) |
|---|---|---|
| `AuthFacade` | `core/facades/auth.facade.ts` | Maneja el estado de la sesión del usuario actual, login, logout y persistencia con Supabase Auth. **Realtime del grant (AC-E3, spec 0017):** `initializeRealtime()`/`disposeRealtime()` suscriben la fila propia de `users` (filtro `id=eq.{dbId}`) → al cambiar `can_access_both_branches`, `refreshProfile()` re-lee el perfil sin re-login; si se revoca, llama `branchFacade.reset()`. Idempotente; AppShell lo inicia al autenticar y lo dispone al logout. |
| `DashboardFacade` | `core/facades/dashboard.facade.ts` | Orquesta la carga de las métricas principales (KPIs) en tiempo real (**SWR + Realtime**). Conectado a `enrollments`, `payments` (vía join), `class_b_sessions` y `vehicles`. **Branch-scoped**: filtra por `BranchFacade.selectedBranchId()` para admin, por `user.branchId` para secretaria. `_lastBranchId` invalida caché SWR al cambiar sede. |
| `EnrollmentFacade` | `core/facades/enrollment.facade.ts` | Orquestador principal del wizard de matrícula (6 pasos). Draft progresivo con expiración 24h, **draft recovery**: `loadActiveDrafts(branchId)` lista borradores pendientes como `DraftSummary[]`, `resumeDraft(enrollmentId)` rehidrata los 3 facades desde BD, `discardDraft(enrollmentId)` elimina borrador y datos asociados. `goToStep()` persiste `current_step` en BD (fire-and-forget). Maneja: upsert user+student (Step 1), instructores/slots/promociones (Step 2), generación y upload de contrato (Step 5), confirmación con número secuencial (Step 6). 16+ signals privados, computed `canAdvance`, `studentSummary`, `sidebarSummary`. **Email duplicado (fix-018):** `findUserByEmail()` valida en Step 1 que el email no pertenezca a otra persona antes de continuar. **Re-matrícula (fix-020):** `evaluateCourseReenrollment(studentId, licenseClass)` delega en `evaluateReenrollment` (`reenrollment.utils`) → bloquea solo si hay matrícula viva (active/pending_payment) en el mismo curso; histórico (completed/cancelled) pide confirmación de re-matrícula; excluye drafts (los maneja `findExistingDraft`) y la matrícula en edición. `prefillFromStudent(rut)` precarga datos personales de un alumno existente para el Paso 1 (sin pisar RUT ni curso) — usado por el blur del RUT y el botón "Re-matricular" de Ex-alumnos (query param `rut`). **Wrappers UI:** `confirm` (ConfirmModal), `openDocument` (DmsViewer). |
| `EnrollmentDocumentsFacade` | `core/facades/enrollment-documents.facade.ts` | Step 3 del wizard de matrícula: documentos y storage. Upload foto carnet (cámara/archivo), documentos profesionales (HVC, cédula, licencia), autorización notarial para menores. Validación antigüedad HVC (RF-082.3, max 30 días). Persistencia en Supabase Storage bucket `documents` + tabla `student_documents`. Computed `uploadedCount`, `allRequiredUploaded()`. Carga docs existentes para re-edición de drafts. **Re-matrícula — foto anterior (fix-020):** `loadPreviousPhoto(studentId, enrollmentId)` busca el `id_photo` más reciente de OTRA matrícula del alumno y lo muestra como preview marcando `photoNeedsConfirmation` (readonly) = true (bloquea avance); `confirmPreviousPhoto(enrollmentId)` copia el objeto al destino `students/{id}/id_photo` + upsert en `student_documents` y baja el flag. Subir una foto nueva (reemplazo) también baja el flag. |
| `EnrollmentPaymentFacade` | `core/facades/enrollment-payment.facade.ts` | Step 4 del wizard de matrícula: pagos y descuentos. `computePricing()` calcula desglose (base price, deposit 50%, amount due). `loadAvailableDiscounts()` carga descuentos activos desde tabla `discounts`. `applyPredefinedDiscount()` calcula monto (porcentaje o fijo). `recordPayment()` **idempotente** (delete-before-insert) inserta en `payments`, registra en `discount_applications` si aplica, actualiza `enrollments` (payment_status, total_paid, pending_balance, discount). `rehydrateFromEnrollment(enrollmentId)` reconstruye estado de pago desde BD (draft recovery). Computed `totalToPay`, `canConfirmPayment`. |
| `AdminAlumnosFacade` | `core/facades/admin-alumnos.facade.ts` | Base de Alumnos con **SWR + Realtime**. `initialize()` — primera visita skeleton, re-visitas refetch silencioso. Realtime en `students` y `enrollments`. **Branch-scoped (fix-027)**: `getActiveBranchId()` (vía `resolveBranchScope` de `branch-scope.utils`) filtra por sede; `null` = admin "Todas las escuelas". Tracking de `branchId` para invalidación SWR. **Papelera:** `trashView` (signal readonly) + `setTrashView(bool)` cambia el filtro Supabase entre `.neq('status','archived')` y `.eq('status','archived')`, fuerza reload completo con skeleton. `archivarAlumno(id)` — soft-delete (`status='archived'`). `restaurarAlumno(id)` — restaura (`status='active'`) + toast + refreshSilently. `checkHistorial(id)` — pre-check pagos/clases antes de archivar. |
| `AdminAlumnosProfesionalFacade` | `core/facades/admin-alumnos-profesional.facade.ts` | **Base de Alumnos Profesional** (spec 0016) con **SWR + Realtime** (`enrollments`). Lista plana branch-scoped de matrículas `license_group='professional'` (estados active/completed/inactive). Join `enrollments + students + users + promotion_courses→courses` + semáforo `v_professional_attendance.attendance_flag` + módulos aprobados (`professional_module_grades.passed`). Computed KPIs: `totalAlumnos`, `activos`, `conDeuda`, `enRiesgo` (semáforo rojo). Papelera: `trashView`/`setTrashView`, `archivarAlumno`/`restaurarAlumno` (soft-delete `students.status`). `dispose()` limpia Realtime. |
| `AsistenciaProfesionalFacade` | `core/facades/asistencia-profesional.facade.ts` | Gestión de asistencia para cursos profesionales. Carga promociones, cursos y sesiones (teóricas/prácticas). Registro de asistencia masiva o individual. Edición de sesiones (fecha, estado, notas). Resumen de asistencia acumulada por alumno. **Wrappers UI:** `confirm` (ConfirmModal). |
| `AgendaFacade` | `core/facades/agenda.facade.ts` | Agenda Semanal con **SWR + Realtime**. `initialize()` con guard SWR: primera llamada → skeleton + fetch + subscribe Realtime; re-visitas → render cacheado + refetch silencioso. `dispose()` limpia canal Realtime (llamar desde `DestroyRef` del Smart component). `loadWeek()` con skeleton (navegación de semana). `refreshSilently()` sin skeleton (SWR re-entry, Realtime events, post-action). **Realtime:** canal `agenda-sessions` escucha `class_b_sessions` INSERT/UPDATE/DELETE → refresh silencioso automático. Navegación: `goToNextWeek()`, `goToPrevWeek()`, `goToToday()`. `setInstructorFilter(id)` limpia `_selectedSlot` (cierra drawer si está abierto) + computed `filteredDays`. **fix-017:** se eliminó el agendamiento de clases desde la Agenda (`loadAgendableStudents`/`agendableStudents`/`scheduleClass`/`cancelClass`/`AgendableStudent`) — al matricularse en Clase B siempre se agendan las 12 clases, así que ya no existe el flujo de "agendar la segunda mitad". El click en un slot (ocupado o disponible) solo abre el detalle de solo lectura. Signals públicos: `weekData`, `kpis`, `filteredDays`, `timeRows`, `weekLabel`, `isLoading`, `error`, `instructors`, `selectedInstructorId`, `selectedSlot`, `isCurrentWeek`. **KPIs (`AgendaWeekKpis`):** `clasesAgendadas`, `clasesCompletadas`, `alumnosDistintos` (COUNT DISTINCT enrollmentId de slots ocupados/completados — reemplazó `instructoresDisponibles`), `vehiculosDisponibles`. **Branch-scoped**: `fetchSessions()` filtra `enrollments.branch_id`; `loadInstructors()` filtra `users.branch_id`; `loadLookupMaps()` filtra vehículos por `branch_id`; slots disponibles filtrados client-side por instructores de la sede (la vista `v_class_b_schedule_availability` no expone `branch_id`). `_lastBranchId` invalida caché SWR al cambiar sede. |
| `NotificationsFacade` | `core/facades/notifications.facade.ts` | Capa 2 de notificaciones: persistentes en BD + Supabase Realtime. Estado: `notifications`, `unreadCount`, `filteredNotifications`, `panelEntries` (agrupado, AC8), `filter`. Métodos: `initialize()` (await auth + carga + Realtime), `loadNotifications()`, `markAsRead(id)`, `markAllAsRead()`, `markManyAsRead(ids)` (**Spec 0024**, marca un grupo entero), `createNotification(payload)`, `notifyUsers(recipientIds, payload)` (**Spec 0024**, 1 INSERT batch), `notifyRole(role, branchId, payload)` (**Spec 0024**, resuelve destinatarios por rol/sede excluyendo al actor), `setFilter()`, `dispose()`. Inicializado en `AppShellComponent`, consumido por `TopbarComponent` y por los productores (`EnrollmentFacade`, `AdminAlumnoDetalleFacade`, `Certificacion*Facade`). |
| `DashboardAlertsFacade` | `core/facades/dashboard-alerts.facade.ts` | Capa 3: alertas computadas en tiempo real desde BD. Queries paralelas: documentos de vehículos vencidos/por vencer (`vehicle_documents` + `alert_config`), pagos pendientes (`enrollments`). Estado: `activeAlerts`, `alertCount`, `isLoading`, `error`. Método: `loadAlerts()`. Consumido por `DashboardComponent`. |
| `AdminAlumnoDetalleFacade` | `core/facades/admin-alumno-detalle.facade.ts` | Ficha del Alumno (**SWR + Realtime**). **Dual-mode**: detecta `license_group` del enrollment y ramifica la lógica de datos. **Clase B:** asistencia práctica (`class_b_practice_attendance`) + teórica (`class_b_theory_attendance`) + sesiones (`class_b_sessions`). **Profesional:** asistencia teórica/práctica (`professional_theory/practice_attendance`), nota promedio (`professional_module_grades`), elegibilidad (teoría ≥75%, nota ≥75, pago completo). Signal `certPdfPath`: `certificate_b_pdf_url` o `certificate_professional_pdf_url` según tipo. Signals profesionales: `progresoTeoriaProf`, `progresoPracticaProf`, `notaPromedioProf`, `elegibilidadProf`, `elegibleProf`. Realtime suscrito a 6 tablas (Clase B + Profesional). **Reprogramar clases (solo B):** `setReprogramarTarget`, `loadInstructores`, `loadScheduleGrid`, `reprogramarClase`. Método público `refresh()` para actualizar tras mutaciones externas. **Carnet PDF (solo Clase B):** signals `licensePdfPath` (path relativo en bucket `documents`, hidratado desde `enrollments.license_pdf_url` al cargar) e `isGeneratingLicense` (spinner). `generarCarnet(enrollmentId)` → invoca EF `generate-student-license-pdf`, persiste path, abre DmsViewer. `verCarnet(storagePath)` → `createSignedUrl` TTL 1h + DmsViewer. **Contrato:** signals `contractGeneratedPath` (`digital_contracts.file_url`), `contractSignedPath` (`digital_contracts.signed_contract_url`), `registrationChannel` (`'presential'\|'online'\|null`), `isUploadingContract`, `isDownloadingContract`. Lógica: presencial → `file_url` es el firmado → solo `verContrato()`; online sin firmado → `descargarContrato()` (fetch blob → objectURL → anchor con `download` attr, spinner via `isDownloadingContract`) + `subirContratoFirmado(enrollmentId, file)` (upload a `contracts/{id}/signed_contract.pdf` + UPDATE `digital_contracts.signed_contract_url`); online con firmado → solo `verContrato()`. **Inasistencias Clase B (RF-053):** `inasistenciasClaseB` signal (`InasistenciaClaseBUI[]`) — `fetchClassBProgress()` hace una única query a `class_b_practice_attendance!inner(class_b_sessions)` filtrada por `enrollment_id` (reemplazó el `select('status').eq('student_id', ...)` que contaba across TODAS las matrículas del alumno — ahora scoped a la matrícula seleccionada); de ahí deriva tanto `progresoPractico.completadas` (status='present') como `inasistenciasClaseB` (status IN absent/no_show/excused). `ClasePracticaUI.ausente` = `session.status==='no_show'`. **`ClasePracticaUI.justificada`/`justificacion`:** `fetchClassBProgress()` arma un mapa `attendanceBySessionId` (session_id → `{status, justification}`) desde las mismas `attendanceRows` y lo cruza por `ses.id` al construir cada `ClasePracticaUI` — así la grilla sabe si una clase `ausente` ya fue perdonada (`status='excused'`) sin tocar el color (se mantiene rojo; solo cambia el subtítulo a "Inasistencia — Justificada"). `justificarInasistenciaClaseB(attendanceId, reason)` → `UPDATE class_b_practice_attendance SET status='excused', justification=reason` + `refreshSilently()`. Realtime ahora también escucha `class_b_practice_attendance` (antes solo `class_b_sessions`). Profesional sigue usando `inasistencias` (`absence_evidence`, sin cambios). **Reagendar Clases Penalizadas (RF-053, flujo de 2 pasos):** `clasesPendientesReagendar` signal (`ClasePendienteReagendarUI[]`, ordenada por `class_number` — sesiones `cancelled` + `no_show` de la matrícula, derivada sin query extra dentro de `fetchClassBProgress()`) alimenta el checklist del Paso 1; `clasesPendientesReagendarCount` y `puedeReagendarPenalizacion` son `computed()` sobre esa misma lista. **Paso 1 (selección):** `setReagendarSeleccion(clases: ClaseSeleccionadaReagendar[])` guarda el subconjunto marcado en `_reagendarSeleccion` (signal expuesto como `reagendarSeleccion`) y limpia `scheduleGrid`/`_slotVehicleMap` — llamado por `AdminReagendarClasesDrawerComponent` antes de navegar al Paso 2. **Paso 2 (agendamiento masivo):** `AdminReagendarHorariosDrawerComponent` lee `reagendarSeleccion().length` como `requiredCount` del mismo `app-assignment-step`/`app-schedule-grid` del wizard de Matrícula (un solo instructor + grilla). `reagendarClasesPenalizadas(payload: ReagendarPenalizacionPayload)` — `payload` vuelve a ser `{enrollmentId, instructorId, selectedSlotIds}` (como el wizard: un instructor, N slots) — lee `_reagendarSeleccion()` (no vuelve a consultar la BD) para saber cuántas son `no_show` vs `cancelled` y las reparte: los primeros N slots (ordenados cronológicamente) reciclan las N sesiones `no_show` de la selección (ordenadas por `class_number`) — `UPDATE` mismo `id`/`class_number` con la nueva fecha/instructor/vehículo, `status` vuelve a `'scheduled'`, y `DELETE` su fila en `class_b_practice_attendance` (queda "limpia", vuelve a verse azul); el resto `INSERT`a sesiones nuevas para las `cancelled` (nunca se tocan), continuando la numeración desde el `class_number` máximo existente. El vehículo se resuelve por **slot** (`_slotVehicleMap`, poblado por `loadScheduleGrid()` — igual que `reprogramarClase()`), no por instructor. Limpia `_reagendarSeleccion` al terminar. Interfaces exportadas: `ClaseSeleccionadaReagendar` (`sessionId`, `claseNumero`, `origen`), `ReagendarPenalizacionPayload` (`enrollmentId`, `instructorId`, `selectedSlotIds`). Ver COMPONENTS.md para el detalle de ambos drawers. |
| `FlotaFacade` | `core/facades/flota.facade.ts` | Gestión de Flota Vehicular (**SWR + Realtime**). Sincronización de disponibilidad, KM y documentos. Agenda diaria de vehículos combinando clases y mantenimientos. |
| `DashboardAlertsFacade` | `core/facades/dashboard-alerts.facade.ts` | Capa 3: alertas computadas en tiempo real desde BD. Queries paralelas: documentos de vehículos vencidos/por vencer (`vehicle_documents!inner(vehicles)` + `alert_config`), pagos pendientes (`enrollments`). **Branch-scoped**: filtra ambas queries por sede activa via `getActiveBranchId()`. `_lastBranchId` invalida caché SWR al cambiar sede. Estado: `activeAlerts`, `alertCount`, `isLoading`, `error`. Método: `loadAlerts()`. Consumido por `DashboardComponent`. |
| `FlotaFacade` | `core/facades/flota.facade.ts` | Gestión de Flota Vehicular (**SWR + Realtime**). Sincronización de disponibilidad, KM y documentos. Agenda diaria de vehículos combinando clases y mantenimientos. **Branch-scoped**: `.eq('branch_id', branchId)` en `fetchVehiclesData()`. `_lastBranchId` invalida caché SWR al cambiar sede. |
| `FlotaDetalleFacade` | `core/facades/flota-detalle.facade.ts` | Detalle de Vehículo. `loadVehicleDetail(vehicleId)` — queries paralelas `vehicles!inner(vehicle_assignments,vehicle_documents)` + `maintenance_records`. Signals: `vehicle` (`VehicleTableRow\|null`), `maintenances` (`MaintenanceRow[]`), `scheduledMaintenances` (`ScheduledMaintenance[]`), `isLoading`, `error`. Computed: `maintenanceKpis` (totalCount, totalSpent, avgMonthly, kmTraveled). CRUD: `createMaintenance(vehicleId, payload)`, `updateMaintenance(id, vehicleId, payload)`. |
| `DmsFacade` | `core/facades/dms.facade.ts` | Módulo DMS (Repositorio de Documentos) con **SWR**. Orquesta documentos de alumnos, institucionales y plantillas. `initialize()` carga KPIs, actividad reciente, docs escuela y plantillas. `loadStudentDocuments(id)` carga docs específicos por alumno. CRUD completo: `uploadStudentDocument()`, `uploadSchoolDocument()`, `uploadTemplate()`, `deleteStudentDocument()` (físico), `deleteSchoolDocument()`, `deleteTemplate()` (soft delete). **Wrappers UI:** `showSuccess/Error` (Toast), `confirm` (ConfirmModal), `openDocument` (DmsViewer), `closeDrawer` (LayoutDrawer). **Branch-scoped**: `school_documents` filtrados por `branch_id`; `students` filtrados por `users.branch_id`; `v_dms_student_documents` (sin `branch_id`) filtrado client-side contra el `studentMap` de la sede. `_lastBranchId` invalida caché SWR al cambiar sede. |
| `BranchFacade` | `core/facades/branch.facade.ts` | Fuente única de verdad para la sede activa en el panel admin. `loadBranches()` carga todas las sedes desde `branches` (llamar solo para rol admin desde `AppShellComponent`). `selectBranch(id\|null)` actualiza `selectedBranchId` (null = "Todas las escuelas"). `reset()` vuelve a null. Computed `selectedBranchLabel`. NO inyectar en Dumb components — solo en Smart (Topbar, AppShell) y en Facades multi-sede (ver regla 7 de facades.md). |
| `InstructorProfileFacade` | `core/facades/instructor-profile.facade.ts` | Perfil del instructor. Obtiene datos básicos y el vehículo asignado en base al `users.id` actual autenticado. Usado transversalmente en el portal de instructores. |
| `InstructorClasesFacade` | `core/facades/instructor-clases.facade.ts` | Carga clases programadas del instructor para el día (`todayClasses`). Computed: `kpis` (clasesHoy/completadas/pendientes/enCurso), `nextClass` (primera clase agendada). `fetchUpcomingDays()` → `upcomingDays` signal con los próximos 3 días (fecha + cantidad de clases). `startClass()` escribe `start_time` en formato TIME. `finishClass()` escribe `end_time` TIME + registra `class_b_practice_attendance` (upsert, status='present'). `saveEvaluation()` con checklist y firma en base64. **Wrappers UI:** `showSuccess/Error` (Toast). SWR + Realtime. |
| `InstructorAlumnosFacade` | `core/facades/instructor-alumnos.facade.ts` | Expone listado de estudiantes **filtrado por instructor_id** (2 pasos: `class_b_sessions` → distinct enrollment_ids, luego `enrollments` + `v_student_progress_b` para progreso real). Computed `kpis`: `totalAlumnos`, `activos`, `completados`, `promedioProgreso`, `porCertificar`. `loadStudentDetail()` incluye `theoryPercent` desde `v_student_progress_b`. `loadExamScores()` filtrado por enrollment_ids del instructor. `fetchTheoryAttendance()` query real a `class_b_theory_attendance` filtrado por `instructor_id`. **Wrappers UI:** `showSuccess/Error` (Toast), `openDrawer/closeDrawer` (LayoutDrawer). |
| `EvaluacionesProfesionalFacade` | `core/facades/evaluaciones-profesional.facade.ts` | Grilla de notas módulo × alumno para Clase Profesional (escala MTT 10–100). **Aterrizaje (fix-025):** `loadLanding()` — 4 queries secuenciales (`professional_promotions` + `promotion_courses` + `enrollments` + `professional_module_grades`) y llama a `buildLanding()` (Functional Core); signals `landing` (`PromocionConCursos[]`), `landingLoaded` (distingue "vacío real" de "cargando"). `cerrarGrilla()` — limpia `_selectedCursoId` y `_grilla` para volver al aterrizaje. **Selectores legacy:** `loadPromociones()` / `selectPromocion(id)` / `loadCursos()`. **Grilla:** `loadGrilla(promotionCourseId)` construye `GrillaEvaluacion` con 7 celdas por fila. `setNota(enrollmentId, moduleIndex, value)` actualiza localmente marcando `dirty=true`. `corregirNotaMinima(enrollmentId, moduleIndex)` eleva a 10 una nota < 10 al blur del input y avisa vía toast (fix-024). `guardarBorrador()` → upsert de celdas dirty en estado `draft`. `confirmarNotas()` → upsert de todas las notas en estado `confirmed` (irreversible). Computed: `hayDirty`, `grillaConfirmada`. Usa `getModuleNames(licenseClass)` de `core/utils/professional-modules.ts` para derivar nombres de módulo (módulo 5 varía entre A2/A3 = Pasajeros vs A4/A5 = Carga). |
| `ServiciosEspecialesFacade` | `core/facades/servicios-especiales.facade.ts` | RF-037 (Módulo 6): Punto de venta de servicios complementarios. Conectado a Supabase (`service_catalog` + `special_service_sales`). Signals: `catalogo`, `ventas`, `selectedServicio`, `isLoading`. Computed `kpis`. Métodos: `registrarVenta()`, `agregarServicio()`, `registrarCobro()`, `openRegistrarVentaDrawer(servicio?)`, `openAgregarServicioDrawer()`. **Branch-scoped**: `fetchData()` filtra `special_service_sales.branch_id`; `registrarVenta()` auto-popula `branch_id` desde `getActiveBranchId()`. `_lastBranchId` invalida caché SWR al cambiar sede. Requiere migración `20260424000001`. |
| `AsistenciaClaseBFacade` | `core/facades/asistencia-clase-b.facade.ts` | Control de Asistencia — clases prácticas individuales Clase B (`class_b_sessions` + `class_b_practice_attendance`), con **SWR**. `markAttendance()` marca presente/ausente; `justifyAbsence()` registra `status='excused'` + motivo (excluida del conteo de penalización). `startClass()`/`finishClass()` gestionan el ciclo de una clase en curso (KM, evaluación, firmas). **RF-053 (auto-penalización, `20260705000000`, corregida en `20260706000000` para exigir consecutividad):** al marcar una inasistencia, invoca `apply_class_b_absence_penalty()` vía RPC — si la matrícula tiene 2 clases de `class_number` consecutivo (N, N+1) ambas no justificadas, la BD cancela sus `class_b_sessions` futuras `'scheduled'` y el facade refresca alertas + muestra toast de aviso. Una clase `'present'` o `'excused'` entre dos inasistencias rompe la secuencia y no penaliza. El fin de jornada automático (`mark_end_of_day_class_b_absences()`, pg_cron `0 1 * * *` ≈ 21:00 CLT invierno) marca `no_show` las clases de hoy no resueltas y aplica la misma penalización server-side, sin depender de que la UI esté abierta. `removeSchedule()`/`reactivateSchedule()` quedan como override manual de la secretaria. |
| `LibroDeClasesFacade` | `core/facades/libro-de-clases.facade.ts` | Vista agregada del Libro de Clases Profesional. Selectores: `loadPromociones()` (filtrado por `branchId`), `selectPromocion(id)`, `selectCurso(promotionCourseId)`. Al seleccionar curso carga en paralelo 6 secciones: cabecera, profesores por módulo, lista de alumnos, asistencia semanal agrupada por semana L-S, evaluaciones, resumen asistencia. `saveClassBookFields(senceCode, horario)` → upsert en `class_book`. `exportPdf()` → invoca Edge Function `generate-class-book-pdf`. SWR con `initialize()`. Branch-scoped via `BranchFacade`. |
| `InstructorHorasFacade` | `core/facades/instructor-horas.facade.ts` | SWR con `initialize()` en 2 fases paralelas. **Fase 1:** `fetchMonthlyHours()` desde `instructor_monthly_hours`, `fetchAdvancesTotal()` suma `instructor_advances` pendientes del mes, `fetchMonthlyTarget()` (privado) — queries paralelas `class_b_sessions` + `instructor_monthly_payments.amount_per_hour` (default 5.000); computa `completedHours`, `targetHours`, `projectedHours`. **Fase 2:** `fetchSessionsLog()` agrupa sesiones completadas por fecha. `fetchWeeklySchedule(dateInWeek)` grilla semanal. `fetchSessionDetailsForPeriod(period)` carga detalle por período. `liquidacionKpis` computed desde `monthlyHours` + `anticiposMes`. |
| `AuditoriaFacade` | `core/facades/auditoria.facade.ts` | Gestión de logs de auditoría. Filtros: fecha, secretaria, acción, módulo. Paginación (PAGE_SIZE=25). **Branch-scoped**: `fetchLogs()` filtra `users.branch_id`; `fetchSecretarias()` filtra `branch_id`. `_lastBranchId` invalida caché SWR al cambiar sede (resetea página a 1). |
| `ExAlumnosFacade` | `core/facades/ex-alumnos.facade.ts` | Gestión de egresados, certificados y estados de cuenta históricos. **Branch-scoped** (**SWR**): `loadEgresados()` con guard `_initialized + _lastBranchId`. Filtra `enrollments.branch_id` en `loadEgresadosList()`. |
| `InstructoresFacade` | `core/facades/instructores.facade.ts` | Administración de instructores, licencias, vehículos asignados y estados de actividad. **Branch-scoped (fix-027)**: `getActiveBranchId()` (vía `resolveBranchScope` de `branch-scope.utils`) filtra `users.branch_id`; `null` = admin "Todas las escuelas". Cierra la fuga multi-sede que mostraba instructores de todas las sedes a la secretaria. |
| `CiclosTeoricosFacade` | `core/facades/ciclos-teoricos.facade.ts` | **Spec 0001.** Gestión de Ciclos Teóricos Clase B (cohorte 2 semanas, 6 clases L/X/V). **Branch-scoped** (`setBranchFilter`+`loadCycles`). Selector de ciclo (RF-11), roster (RF-12), enlace Zoom por clase + envío masivo con destinatarios seleccionables (RF-14/15/16, vía EF `send-zoom-email`), y reasignación de alumnos entre ciclos (override). Reemplaza a la ex `ClaseOnlineFacade` (eliminada). |
| `PagosFacade` | `core/facades/pagos.facade.ts` | Gestión financiera con **SWR + Realtime**. KPIs de ingresos (hoy/mes), deudores y pagos recientes sincronizados globalmente. Estado de cuenta detallado por matrícula. **Branch-scoped**: filtra `payments` vía `enrollments!inner(branch_id)` y `enrollments` directamente por `branch_id` en 7 métodos de fetch. `_lastBranchId` invalida caché SWR al cambiar sede. |
| `PromocionesFacade` | `core/facades/promociones.facade.ts` | Gestión de cursos profesionales (A2-A5), planificación de fechas (lunes cada 2 semanas) y asignación de relatores. |
| `RelatoresFacade` | `core/facades/relatores.facade.ts` | Administración de relatores profesionales, especialidades y cursos asignados. |
| `SecretariasFacade` | `core/facades/secretarias.facade.ts` | Gestión de cuentas de secretarias, sedes asignadas y permisos de acceso. Lee/escribe el grant multi-sede `canAccessBothBranches` (spec 0017): `fetchData`/`mapRow` lo exponen en `SecretariaTableRow`; `crearSecretaria`/`editarSecretaria` lo propagan a las edge functions `create-secretary`/`update-secretary`. |
| `LiquidacionesFacade` | `core/facades/liquidaciones.facade.ts` | Gestión de remuneraciones con **SWR + Realtime**. Maneja el estado de selección (`selectedRow`) para el registro de pagos vía `LayoutDrawer`. Realtime en pagos y anticipos de instructores. |
| `CuadraturaFacade` | `core/facades/cuadratura.facade.ts` | Cierre de caja diario con **SWR + Realtime**. Sincronización de arqueo, ingresos (payments) y egresos (expenses/advances) por sede. `fetchPayments` filtra **solo `status='paid'`** (pagos pendientes excluidos). Rango de fechas usa `getChileDateTimeRange()` con offset `America/Santiago` real (CLT/CLST) para evitar pérdida de pagos vespertinos por interpretación UTC en PostgreSQL. |
| `HistorialCuadraturasFacade` | `core/facades/historial-cuadraturas.facade.ts` | Historial de cierres con **SWR**. `initialize()` — primera visita skeleton, navegación de meses instantánea. `seleccionarCierre(cierre)` gestiona el estado para el detalle. Exportación CSV. |
| `LiquidacionesFacade` | `core/facades/liquidaciones.facade.ts` | Gestión de remuneraciones con **SWR + Realtime**. Maneja el estado de selección (`selectedRow`) para el registro de pagos vía `LayoutDrawer`. Realtime en pagos y anticipos de instructores. **Branch-scoped**: usa `getActiveBranchId()` en `initialize()` y `refreshSilently()` para filtrar instructores por sede activa. |
| `CuadraturaFacade` | `core/facades/cuadratura.facade.ts` | Cierre de caja diario con **SWR + Realtime**. Sincronización de arqueo, ingresos (payments **+ cobros de cursos singulares**, fix-016 AC3) y egresos (expenses/advances) por sede. **Cursos singulares:** `fetchSingularSales()` lee `standalone_course_enrollments` con `payment_status='paid'` y `paid_at` de hoy (branch via `standalone_courses.branch_id`), mapea con `mapSingularSaleToIngreso()` (exportado, testeado — buckets: efectivo→claseB, transferencia→claseA, tarjeta→otros, sence→sence). `IngresoRow.source` (`'payment'\|'singular'`) decide la reversa: `eliminarIngreso()` con source singular NO borra la inscripción, la vuelve a `pending` (`amount_paid=0`, `paid_at=null`). Realtime también escucha `standalone_course_enrollments`. **Branch-scoped**: `getActiveBranchId()` en lecturas y escrituras. `_lastBranchId` invalida caché SWR al cambiar sede. |
| `HistorialCuadraturasFacade` | `core/facades/historial-cuadraturas.facade.ts` | Historial de cierres de caja con **SWR**. `initialize()` — primera visita skeleton, navegación de meses instantánea. `seleccionarCierre(cierre)` gestiona el estado para el detalle. Exportación CSV. **Branch-scoped**: `getActiveBranchId()` (admin → `BranchFacade.selectedBranchId()`, secretaria → `user.branchId`). `_lastBranchId` invalida caché SWR al cambiar sede. |
| `CursosSingularesFacade` | `core/facades/cursos-singulares.facade.ts` | RF-035: Gestión de Cursos Singulares (SENCE, Grúa, Retroexcavadora…). SWR con `initialize()` + **`_lastBranchId`** (fix-016: cambio de sede invalida caché y recarga con skeleton). Branch-scoped via `BranchFacade`. Signals: `cursos` (`CursoSingularRow[]`), `isLoading`, `isSaving`, `error`. Computed `kpis`: cursosActivos, totalCursos, totalInscritos, **`ingresosCobrados`** (Σ `amount_paid` real) y **`porCobrar`** (Σ saldos `precio − descuento − pagado`) — cursos no cancelados. **`mapCursoDto` exportado** (Functional Core, testeado). **Cursos (fix-016):** `crearCurso()` exige `form.branchId` (NOT NULL en BD) y persiste `registered_by`. **Wizard inscripción (fix-015/016):** `searchByRut()` pre-carga ficha completa (users + students); `savePersonalData()` valida birth_date requerida y edad ≥ 17 con `calcAge()`; `inscribirAlumno()` verifica duplicado y **cupos (`hasAvailableSeats`)** ANTES de escribir (trigger `trg_standalone_capacity` como backstop), persiste `discount_amount`/`discount_reason` (incluso con pago pendiente) y `paid_at` si paga al tiro; `upsertUser`/`upsertStudent` solo actualizan campos con valor. `marcarEnrollmentPagado()` cobra `precio − discount_amount` y sella `paid_at`. **Errores sanitizados:** todo `catch` pasa por `setSafeError()` → `toFriendlyDbMessage()` (`db-error.utils`) + `console.error` del error real. `loadInscriptos()` expone error amigable (no lista vacía engañosa). Tests: `cursos-singulares.facade.spec.ts` (15). |
| `StudentHomeFacade` | `core/facades/student-home.facade.ts` | Home del alumno (**SWR**). Orquesta 7+ queries paralelas en `Promise.all` para armar `StudentHomeSnapshot`. Rama por `licenseGroup`: `buildClassBSnapshot()` (enrollments + `v_student_progress_b` + prácticas + examen + certificado + próxima clase + teoría) o `buildProfessionalSnapshot()` (módulos + asistencia + sesión + cert). `initialize()` con SWR guard (`_initialized`): 1ª carga con skeleton, re-visitas con `refreshSilently()`. Computed públicos: `hero`, `progress`, `attendance`, `grades`, `certificate`, `side`, `licenseGroup`. `downloadCertificate()` → `createSignedUrl` bucket `documents` TTL 3600. Manejo de null enrollment (sin matrícula activa) → `snapshot=null`, `error=null`. |
| `TasksFacade` | `core/facades/tasks.facade.ts` | Sistema de tareas multi-rol con **SWR + Realtime** (dos canales). `initialize()` — primera visita skeleton + fetch + suscripción Realtime; re-visitas `refreshSilently()`. `dispose()` cierra canales y resetea `_initialized`. **Fetch**: JOIN `users!from_user_id` + `users!to_user_id` + `task_replies(count)`, mapea con `mapTaskDtoToRow()`. **Branch-scoped**: `.eq('branch_id', branchId)` solo si `BranchFacade.selectedBranchId() !== null` (admin "Todas las escuelas" ve sin filtro). **Realtime**: canal `tasks-sent` (filter `from_user_id=eq.{dbId}`) + canal `tasks-received` (filter `to_user_id=eq.{dbId}`). Computed: `pendingCount`, `overdueCount`, `sentTasks`, `receivedTasks`, `observationTasks`, `selectedTask`. Mutaciones: `createTask(payload)` (valida `canSendTo`, strip `due_date` si type=observation, notificación fire-and-forget), `updateStatus(id, status)` (agrega `completed_at` si status=completed), `markSeen(id)`, `addReply(taskId, body)` (INSERT `task_replies`), `softDelete(id)` (UPDATE `deleted_at`), `selectTask(id\|null)`. Mapeo de roles UI→DB: 'secretaria'→'secretary', 'admin'→'admin'. 36/36 tests en `tasks.facade.spec.ts`. |
| `WebsiteConfigFacade` | `core/facades/website-config.facade.ts` | Fachada de dominio para gestionar la configuración web dinámica. Permite a los administradores y secretarias editar los metadatos SEO, banner promocional, cursos y precios, y preguntas frecuentes en caliente sin redeploys de la landing page. Sigue el patrón estricto de fachadas Angular con señales readonly (`config`, `isLoading`, `isSaving`, `error`). Carga estructurada por `branchId` (1=Azul, 2=Roja). **Spec 0004:** consume `CoursesFacade` y expone `resolvedCourses` computed que hace JOIN en memoria entre `config.courses` (CourseConfig[] editorial) y el catálogo operacional (CourseCatalogItem[]) para producir `ResolvedCourse[]` con `displayPrice`, `displayPriceLabel` ("$320.000" / "Gratis"), `isOverrideActive`, `isCourseActive`. `saveConfig` valida pre-UPSERT que `course_id` sea único en el array (AC-E1). |
| `CoursesFacade` | `core/facades/courses.facade.ts` | **Spec 0004.** Lectura del catálogo operacional `courses` para uso editorial (dropdown de Configuración Web; futuros pickers). Branch-scoped según facades.md sec 7 (`getActiveBranchId()` deriva scope según rol). SWR con `_initialized + _lastBranchId`. `loadAvailableCourses(branchId)` filtra `.eq('branch_id').eq('active', true)` y trae subset `Pick<Course, id\|name\|license_class\|base_price\|active>`. Computed `availableById` (Map id→item) para JOIN en memoria desde `WebsiteConfigFacade`. Signals: `availableCourses`, `isLoading`, `error`. 8/8 tests en `courses.facade.spec.ts`. |


> **Nota para los Agentes**:
> - NO inyectes repositorios o `SupabaseService` en la UI. Inyecta el Facade correspondiente.
> - Si creas un Facade nuevo, DEBES registrarlo en esta tabla.
> - Todo archivo aquí debe terminar en `.facade.ts`.

## Auto-Index — Facades detectadas por AST (generado automáticamente)

<!-- AUTO-GENERATED:BEGIN -->
| Clase | Dependencias | Signals expuestos | Archivo |
|-------|-------------|------------------|---------|
| `AdminAlumnoDetalleFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `DmsViewerService`, `NotificationsFacade` | — | `src/app/core/facades/admin-alumno-detalle.facade.ts` |
| `AdminAlumnosProfesionalFacade` | `ErrorSanitizerService`, `SupabaseService`, `BranchFacade`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/admin-alumnos-profesional.facade.ts` |
| `AdminAlumnosFacade` | `ErrorSanitizerService`, `SupabaseService`, `BranchFacade`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/admin-alumnos.facade.ts` |
| `AdminHorariosFacade` | `SupabaseService`, `ToastService` | — | `src/app/core/facades/admin-horarios.facade.ts` |
| `AdminPreInscritosFacade` | `ErrorSanitizerService`, `SupabaseService`, `BranchFacade`, `AuthFacade`, `EpqPrintService` | — | `src/app/core/facades/admin-pre-inscritos.facade.ts` |
| `AgendaFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/agenda.facade.ts` |
| `AnticiosFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/anticipos.facade.ts` |
| `ArchivoFacade` | `SupabaseService`, `BranchFacade`, `AuthFacade`, `ToastService` | `promociones`, `cursos`, `selectedPromocionId`, `selectedCursoId`, `alumnos`, `isLoading`, `isLoadingAlumnos`, `error`, `kpis`, `moduleNames` | `src/app/core/facades/archivo-profesional.facade.ts` |
| `AsistenciaClaseBFacade` | `SupabaseService`, `ToastService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/asistencia-clase-b.facade.ts` |
| `AsistenciaProfesionalFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `AuthFacade`, `ConfirmModalService` | — | `src/app/core/facades/asistencia-profesional.facade.ts` |
| `AuditoriaFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | — | `src/app/core/facades/auditoria.facade.ts` |
| `AuthFacade` | `SupabaseService`, `Router`, `BranchFacade` | — | `src/app/core/facades/auth.facade.ts` |
| `BranchFacade` | `ErrorSanitizerService`, `SupabaseService` | — | `src/app/core/facades/branch.facade.ts` |
| `CertificacionClaseBFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService`, `DmsViewerService`, `NotificationsFacade` | `alumnos`, `log`, `isLoading`, `error`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes`, `kpis` | `src/app/core/facades/certificacion-clase-b.facade.ts` |
| `CertificacionProfesionalFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService`, `DmsViewerService`, `NotificationsFacade` | `promociones`, `cursos`, `selectedPromocionId`, `selectedCursoId`, `alumnos`, `log`, `isLoading`, `isLoadingAlumnos`, `error`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes`, `kpis` | `src/app/core/facades/certificacion-profesional.facade.ts` |
| `CiclosTeoricosFacade` | `SupabaseService`, `ToastService`, `BranchFacade` | — | `src/app/core/facades/ciclos-teoricos.facade.ts` |
| `CoursesFacade` | `SupabaseService`, `BranchFacade`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/courses.facade.ts` |
| `CuadraturaFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | — | `src/app/core/facades/cuadratura.facade.ts` |
| `CursosSingularesFacade` | `SupabaseService`, `BranchFacade`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/cursos-singulares.facade.ts` |
| `DashboardAlertsFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/dashboard-alerts.facade.ts` |
| `DashboardFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/dashboard.facade.ts` |
| `DmsFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `LayoutDrawerService`, `ConfirmModalService`, `ToastService`, `DmsViewerService` | — | `src/app/core/facades/dms.facade.ts` |
| `EnrollmentDocumentsFacade` | `ErrorSanitizerService`, `SupabaseService` | — | `src/app/core/facades/enrollment-documents.facade.ts` |
| `EnrollmentPaymentFacade` | `ErrorSanitizerService`, `SupabaseService` | — | `src/app/core/facades/enrollment-payment.facade.ts` |
| `EnrollmentFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `EnrollmentDocumentsFacade`, `EnrollmentPaymentFacade`, `ConfirmModalService`, `DmsViewerService`, `NotificationsFacade`, `ToastService` | — | `src/app/core/facades/enrollment.facade.ts` |
| `EvaluacionesProfesionalFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `AuthFacade` | — | `src/app/core/facades/evaluaciones-profesional.facade.ts` |
| `ExAlumnosFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/ex-alumnos.facade.ts` |
| `FlotaDetalleFacade` | `ErrorSanitizerService`, `SupabaseService` | — | `src/app/core/facades/flota-detalle.facade.ts` |
| `FlotaFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade` | — | `src/app/core/facades/flota.facade.ts` |
| `GlobalSearchFacade` | `AuthFacade`, `AdminAlumnosFacade`, `Router` | — | `src/app/core/facades/global-search.facade.ts` |
| `HistorialCuadraturasFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | — | `src/app/core/facades/historial-cuadraturas.facade.ts` |
| `InstructorAlumnosFacade` | `ErrorSanitizerService`, `InstructorProfileFacade`, `SupabaseService`, `ToastService`, `LayoutDrawerService` | — | `src/app/core/facades/instructor-alumnos.facade.ts` |
| `InstructorClasesFacade` | `ErrorSanitizerService`, `InstructorProfileFacade`, `SupabaseService`, `ToastService` | — | `src/app/core/facades/instructor-clases.facade.ts` |
| `InstructorHorasFacade` | `ErrorSanitizerService`, `InstructorProfileFacade`, `SupabaseService` | — | `src/app/core/facades/instructor-horas.facade.ts` |
| `InstructorProfileFacade` | `AuthFacade`, `SupabaseService` | — | `src/app/core/facades/instructor-profile.facade.ts` |
| `InstructoresFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `BranchFacade`, `AuthFacade` | — | `src/app/core/facades/instructores.facade.ts` |
| `LibroDeClasesFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `BranchFacade`, `AuthFacade` | — | `src/app/core/facades/libro-de-clases.facade.ts` |
| `LiquidacionesFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | — | `src/app/core/facades/liquidaciones.facade.ts` |
| `NotificationsFacade` | `SupabaseService`, `AuthFacade`, `ToastService` | — | `src/app/core/facades/notifications.facade.ts` |
| `PagosFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | — | `src/app/core/facades/pagos.facade.ts` |
| `PromocionesFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService` | — | `src/app/core/facades/promociones.facade.ts` |
| `PublicEnrollmentFacade` | `ErrorSanitizerService`, `SupabaseService` | — | `src/app/core/facades/public-enrollment.facade.ts` |
| `RelatoresFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService` | — | `src/app/core/facades/relatores.facade.ts` |
| `ReportesContablesFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `ToastService` | `isLoading`, `isExporting`, `isRegistrando`, `error`, `filtros`, `gastosFijos`, `kpis`, `ingresosCategoria`, `gastosCategoria`, `evolucionMensual`, `detalleDiario`, `diasConMovimientos`, `escuela` | `src/app/core/facades/reportes-contables.facade.ts` |
| `SecretariasFacade` | `ErrorSanitizerService`, `SupabaseService`, `ToastService`, `BranchFacade` | — | `src/app/core/facades/secretarias.facade.ts` |
| `ServiciosEspecialesFacade` | `ErrorSanitizerService`, `SupabaseService`, `AuthFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `catalogo`, `ventas`, `selectedServicio`, `isLoading`, `isExporting`, `error`, `kpis` | `src/app/core/facades/servicios-especiales.facade.ts` |
| `StudentClasesFacade` | `SupabaseService`, `AuthFacade`, `StudentEnrollmentContextFacade` | — | `src/app/core/facades/student-clases.facade.ts` |
| `StudentEnrollmentContextFacade` | `SupabaseService` | — | `src/app/core/facades/student-enrollment-context.facade.ts` |
| `StudentHomeFacade` | `SupabaseService`, `AuthFacade`, `ToastService`, `StudentEnrollmentContextFacade` | — | `src/app/core/facades/student-home.facade.ts` |
| `StudentHorarioFacade` | `SupabaseService`, `AuthFacade`, `StudentEnrollmentContextFacade` | — | `src/app/core/facades/student-horario.facade.ts` |
| `StudentPaymentFacade` | `SupabaseService` | — | `src/app/core/facades/student-payment.facade.ts` |
| `TasksFacade` | `SupabaseService`, `AuthFacade`, `BranchFacade`, `NotificationsFacade`, `ToastService` | — | `src/app/core/facades/tasks.facade.ts` |
| `WebsiteConfigFacade` | `SupabaseService`, `ToastService`, `CoursesFacade` | — | `src/app/core/facades/website-config.facade.ts` |

<!-- AUTO-GENERATED:END -->
