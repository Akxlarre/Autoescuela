# Registro de Componentes (Atomic Design)

> **Regla de Actualización (OBLIGATORIA):** El Agente DEBE usar sus herramientas de sistema (escritura de archivos) para agregar los nuevos componentes a estas tablas. Solo si el entorno no los soporta, usa el bloque `<memory_update>` para que el humano lo copie.

## Átomos (Atoms)
*Elementos UI básicos e indivisibles (botones, inputs, badges).*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-logo` | Brand / Logo | Ninguna (Smart interno) — lee de AuthFacade/BranchFacade para mostrar el nombre de sede dinámicamente | `shared/components/logo/logo.component.ts` | ✅ Estable |
| `skeleton-block` | Loading | `variant` (`rect`, `circle`, `text`), `width`, `height` | `shared/components/skeleton-block/skeleton-block.component.ts` | ✅ Estable |
| `app-icon` | Ícono (Lucide) | `name` (requerido, kebab-case), `size` (default 16), `color` (default currentColor), `ariaHidden` (default true) | `shared/components/icon/icon.component.ts` | ✅ Estable |
| `app-date-input` | Input de fecha (wrapper p-datepicker) | `value` (string ISO req), `label` (string, ''), `id` (string, 'date'), `required` (boolean, false), `disabled` (boolean, false), `readonlyInput` (boolean, false — si `true`, bloquea tipeo y fuerza uso del calendar picker), `min` (string ISO, ''), `max` (string ISO, ''), `placeholder` (string). Output: `valueChange` (string ISO). API ISO pura — convierte internamente a `Date` vía `isoToDate()`. Formato display: `dd/mm/yy`. **Máscara automática al tipear:** solo acepta dígitos, auto-inserta `/` tras el día y el mes, límite 8 dígitos raw. Comportamiento del calendario sin cambios respecto al default de PrimeNG. | `shared/components/date-input/date-input.component.ts` | ✅ Estable |
| `app-email-input` | Input de email con validación | `value` (string, req), `id` (string, 'email'), `label` (string, 'Email'), `required` (boolean, false), `placeholder` (string), `forceDirty` (boolean, false). Output: `valueChange` (string). Feedback post-blur: borde rojo + error / borde verde + confirmación. Normaliza a minúsculas en blur vía `normalizeEmail()`. ARIA: `aria-required`, `aria-invalid`, `aria-describedby`. | `shared/components/email-input/email-input.component.ts` | ✅ Estable |
| `app-phone-input` | Input de teléfono con selector de prefijo | `value` (string, ''), `id` (string, 'phone'), `label` (string, 'Teléfono / WhatsApp'), `required` (boolean, false), `forceDirty` (boolean, false). Output: `valueChange` (string — emite E.164, ej: `"+56912345678"`). **Custom dropdown** (botón + panel con `ClickOutsideDirective`) — 9 prefijos con banderas SVG reales via `.fi.fis.fi-{cc}` (flag-icons assets, `/flags/*.svg`). CL +56 default. Otros: AR, PE, BO, CO, VE, ES, US, Otro (globo). Chevron animado (rotate-180 al abrir). Hover/selected via CSS vars. Placeholder contextual por prefijo. Keydown prevention. Feedback verde/rojo post-blur. ARIA: `role=listbox/option`, `aria-expanded`, `aria-haspopup`, `aria-selected`. `data-llm-action="toggle-dial-code-dropdown"`, `data-llm-description` en digits. Estilos hover/selected inline via `styles[]` CSS vars. | `shared/components/phone-input/phone-input.component.ts` | ✅ Estable |
| `app-async-btn` | Botón con loading state | `label` (string, req), `icon` (string\|null, kebab-case Lucide), `loading` (boolean, false), `disabled` (boolean, false), `loadingLabel` (string, 'Procesando...'). Muestra spinner `loader` giratorio + `loadingLabel` cuando `loading=true`. Cursor pointer activo, `cursor-not-allowed` + `opacity-50` en estado disabled/loading. Usar `(click)` nativo del host. | `shared/components/async-btn/async-btn.component.ts` | ✅ Estable |
| `app-tabs` | Navegación / Tabs | `tabs` (`TabOption[]`, req), `activeId` (string, req), `variant` (`'line'\|'segmented'\|'pill'`, default 'line'), `uppercase` (boolean, false). Output: `activeIdChange` (string). Exporta `TabOption` (id/label/count/icon/disabled) y `TabVariant`. Standalone OnPush; estado activo con `var(--ds-brand)`, íconos vía `app-icon`, scroll horizontal oculto. | `shared/components/tabs/tabs.component.ts` | ✅ Estable |
| `btn-primary` | CSS Utility global (`@utility`) | Clase Tailwind global para botón primario. Usa tokens `--btn-primary-bg`, `--btn-primary-text`, `--btn-primary-radius`, `--btn-primary-shadow`. Adapta automáticamente claro/oscuro. Incluye `:hover:not(:disabled)`, `:active:not(:disabled)` (scale) y `:disabled` (opacity 0.7 + cursor not-allowed). **Funciona en `<button>` y `<a>`** — usar `:not(:disabled)` en lugar de `:enabled` para compatibilidad con elementos no-form. Combinar con `flex-[2]` o `h-11 px-8` para sizing en drawers. | `src/tailwind.css` → `@utility btn-primary` | ✅ Estable |
| `btn-secondary` | CSS Utility global (`@utility`) | Clase Tailwind global para botón secundario/cancelar. Usa tokens `--btn-secondary-bg`, `--btn-secondary-border`, `--btn-secondary-text`. Fondo transparente con borde en claro; blanco translúcido en oscuro. `:hover:not(:disabled)` funciona en `<button>` y `<a routerLink>`. Combinar con `flex-1` en drawers con layout flex. | `src/tailwind.css` → `@utility btn-secondary` | ✅ Estable |
| `btn-ghost` | CSS Utility global (`@utility`) | Clase Tailwind global para botón terciario/ghost. Usa tokens `--btn-ghost-bg`, `--btn-ghost-text`, `--btn-ghost-text-hover`, `--btn-ghost-bg-hover`. Fondo transparente sin borde; hover aplica `bg-subtle` y sube el texto a `text-primary`. Ideal para acciones secundarias discretas dentro de filas de tabla o listas. | `src/tailwind.css` → `@utility btn-ghost` | ✅ Estable |
| `btn-warning-soft` | CSS Utility global (`@utility`) | Botón de acción de estado warning. Fondo `--state-warning-bg`, texto `--state-warning`, borde `--state-warning-border`. Hover: `brightness(0.96)`. Usar para acciones de transición de estado (ej: "Iniciar tarea"). | `src/tailwind.css` → `@utility btn-warning-soft` | ✅ Estable |
| `btn-success-soft` | CSS Utility global (`@utility`) | Botón de acción de estado success. Fondo `--state-success-bg`, texto `--state-success`, borde `--state-success-border`. Hover: `brightness(0.96)`. Usar para acciones de confirmación positiva (ej: "Completar tarea"). | `src/tailwind.css` → `@utility btn-success-soft` | ✅ Estable |
| `app-dms-viewer-modal` | Organismo / Visor | `doc` (`DmsViewerDocument`, req) — outputs: `closed`. Visor de documentos premium (PDF e imágenes) que se renderiza globalmente en `AppShell`. | `shared/components/dms-viewer-modal/dms-viewer-modal.component.ts` | ✅ Estable |
| `app-task-status-badge` | Badge / Atom | `status` (`TaskStatus`, req). Pill inline con ícono Lucide + label; colores por estado: `pending`→warning, `in_progress`→brand, `completed`→success, `seen`→muted. | `shared/components/task-status-badge/task-status-badge.component.ts` | ✅ Estable |
| `app-task-card` | Molécula / Card | `task` (`TaskRow`, req), `loading` (boolean, false). Output: `cardClicked` (string = task.id). Skeleton integrado vía `@if(loading())`. Muestra: subject, body preview 80c, status badge, type icon, due_date, sender→recipient, replyCount, badge "Vencida" (`isOverdue`), badge "Destinatario inactivo" (`recipientInactive`). `[appCardHover]` + `data-llm-action="open-task-detail"`. | `shared/components/task-card/task-card.component.ts` | ✅ Estable |
| `app-task-reply-thread` | Molécula / Thread | `replies` (`TaskReply[]`, req), `taskStatus` (`TaskStatus`, req), `currentUserId` (number, req). Output: `replySent` (string = body). Hilo cronológico con autor/tiempo relativo. Textarea de reply visible solo si `taskStatus !== 'completed'` (AC9). `data-llm-action="add-reply-to-task"`. Dumb puro. | `shared/components/task-reply-thread/task-reply-thread.component.ts` | ✅ Estable |
| `app-media-upload-control` | Átomo / Form Control | `label` (string, req), `buttonLabel` (string), `accept` (string), `isUploading` (boolean), `previewType` ('image'\|'video'). Componente DRY que implementa `ControlValueAccessor` (CVA). Emite `fileSelected(File)` para delegar la subida al padre, mientras mantiene la URL como string. Integra previsualización, botón de subida PrimeNG (`p-button`) y fallback manual. | `shared/components/media-upload-control/media-upload-control.component.ts` | ✅ Estable |

## Moléculas (Molecules)
*Agrupación de átomos que forman una unidad funcional simple (search bar, card preview).*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-kpi-card` | KPI / Métrica | `value` (**string\|number**, req), `label` (string, req), `suffix` (''), `prefix` (''), `trend` (number|undefined), `trendLabel` (''), `accent` (false), `icon` (string|undefined), `size` ('lg'|'md'|'sm', default 'lg'), `color` ('default'|'success'|'warning'|'error', default 'default'). Strings vacíos → "—". GSAP solo en values numéricos. | `shared/components/kpi-card/kpi-card.component.ts` | ✅ Estable |
| `app-kpi-card-variant` | KPI / Métrica con Carga | Igual a `app-kpi-card` + `loading` input con skeleton integrado (Single-Component Skeleton). `iconBg()` usa tokens `var(--state-*-bg, rgba(...))`. Animación GSAP via `effect()` (dispara al salir de loading, no solo al montar). **Preferir esta variante sobre `app-kpi-card`**. | `shared/components/kpi-card/kpi-card-variant.component.ts` | ✅ Estable |
| `app-stat-box` | Métrica / Info Card | `label` (string, req), `value` (string\|number, req), `variant` ('default'\|'success'\|'warning'\|'error'\|'brand'\|'surface'), `icon` (string), `suffix` (string), `compact` (boolean), `useMono` (boolean). Componente premium para desgloses financieros y métricas secundarias en bento grids o drawers. | `shared/components/stat-box/stat-box.component.ts` | ✅ Estable |
| `app-action-kpi-card` | KPI / Métrica Interactiva | Variante de KPI interactiva: hover GSAP, cursor pointer, `pulse` en ícono, slot `[footer]`. `bento-card` + `[loading]` con skeleton. `iconBg()` unificado con `var(--state-*-bg, ...)` — mismo token que `kpi-card-variant`. Sin `CommonModule`. | `shared/components/kpi-card/action-kpi-card.component.ts` | ✅ Estable |

| `app-tabs` | Navegación / UI | `tabs` (TabOption[], req), `activeId` (string, req), `variant` ('line'\|'segmented'\|'pill', default 'line'), `uppercase` (boolean, false). Outputs: `activeIdChange` (string). Reemplaza las implementaciones estáticas de "tablist". Soporta contadores numéricos e íconos (kebab-case) nativamente. Adaptable y accesible. | `shared/components/tabs/tabs.component.ts` | ✅ Estable |

## Moléculas — Feedback
*Comunicación de estados del sistema al usuario.*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-empty-state` | Estado vacío | `message` (string, req), `subtitle` (string), `icon` (Lucide kebab-case), `actionLabel` (string), `actionIcon` (default 'plus'), `(action)` output | `shared/components/empty-state/empty-state.component.ts` | ✅ Estable |
| `app-alert-card` | Alerta / Feedback | `title` (string, req), `severity` ('error'|'warning'|'info'|'success', default 'info'), `actionLabel` (string), `dismissible` (boolean), `(action)` output, `(dismissed)` output. Body via `ng-content`. | `shared/components/alert-card/alert-card.component.ts` | ✅ Estable |

## Moléculas — Shell / Topbar
*Dropdowns y paneles del shell de la aplicación.*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-notifications-panel` | Dropdown / Panel | `notifications` (Notification[], req), `unreadCount` (number, 0) — outputs: `markRead` (string), `markAllRead` (void). Dumb: sin servicios. Entrada animada con `[appAnimateIn]` desde TopbarComponent. | `shared/components/notifications-panel/notifications-panel.component.ts` | ✅ Estable |
| `app-search-panel` | Command Palette global | inputs: `groups` (SearchResultGroup[], req), `rightPx` (number, 24). outputs: `queryChange` (string), `closed` (void), `resultSelected` (SearchResult). Dumb puro sin inyección de facades. Chips rápidos en vacío, grupos Acciones/Alumnos con navegación ↑↓↵, footer de atajos. **Alumnos expandibles:** click en fila → menú contextual horizontal con 4 chips de acción rápida (ver ficha, pago, agenda, matrícula); Enter colapsado=expande / expandido=ver ficha; cambio de query colapsa automáticamente. Coordinado por `AppShellComponent` + `GlobalSearchFacade`. | `shared/components/search-panel/search-panel.component.ts` | ✅ Estable |
| `app-user-panel` | Dropdown / Panel | `user` (User, req) — outputs: `action` (string), `logout` (void). Dumb: sin servicios ni lógica de confirmación. El segundo paso de logout (`ConfirmModalService.confirm()`) es responsabilidad del TopbarComponent (Smart parent). Menú desplegable para opciones de usuario desde TopbarComponent animado con `[appAnimateIn]`. | `shared/components/user-panel/user-panel.component.ts` | ✅ Estable |

## Moléculas — Matrícula Pública
*Componentes del wizard de matrícula pública (sin auth).*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-public-wizard-shell` | Organismo / Flujo Público | `steps` (PublicStepConfig[], req), `currentStep` (string, req), `title` (string, req), `subtitle` (string\|null), `brandName` (string, 'Autoescuela'), `whatsappUrl` (string\|null). Output: `helpClick`. Hero con `.surface-hero` + gradiente de sede, sheen diagonal, 3 orbs ambientales fixed, progress bar nombrada (número+label+conectores), card glassmorphism con overlap. Entrada animada con `GsapAnimationsService.animateHero()`. Content projection vía `<ng-content>`. | `shared/components/public-enrollment-steps/public-wizard-shell/public-wizard-shell.component.ts` | ✅ Estable (spec 0009) |
| `app-public-license-type` | Dumb / Flujo Público | `availableFlows` (PublicFlowType[], ['class_b','professional']). Outputs: `flowSelect` (PublicFlowType), `next`. Cards seleccionables Clase B / Profesional con ícono, badge "Más popular", estado activo con borde brand + check. `computed visibleCards()` filtra los flows disponibles. CTA "Continuar" deshabilitado hasta selección. **`infoNote` en FlowCard:** card "Clase Profesional" muestra badge info azul "Pre-inscripción — La escuela te contactará para completar la matrícula." (hotfix-004/M6). `data-llm-action="select-license-type-{flow}"` y `"confirm-license-type"`. | `shared/components/public-enrollment-steps/public-license-type/public-license-type.component.ts` | ✅ Estable (hotfix-004) |
| `app-public-orientation` | Dumb / Flujo Público | `siteLinks` (PublicOrientationLink[], []). Pantalla de orientación cuando no hay `branchId` válido (AC-E1/AC-E3): ícono, mensaje "Accede desde el sitio de tu escuela", enlaces a las webs con `target="_self"` y `data-llm-nav="enrollment-site-{theme}"`. Sin selector de sede. Exporta `PublicOrientationLink { label, url, theme }`. | `shared/components/public-enrollment-steps/public-orientation/public-orientation.component.ts` | ✅ Estable (spec 0009) |
| `app-public-context-banner` | Dumb / Flujo Público | `context` (PublicEnrollmentContext, req). Output: `editRequested`. Banner de contexto de inscripción (AC3/AC5): ícono de curso con gradiente de marca, nombre del curso, nombre+dirección de sede, precio en gradiente (`--gradient-primary` clip-text), botón "Editar selección". `data-llm-action="edit-enrollment-selection"`. Sin servicios ni lógica derivada. | `shared/components/public-enrollment-steps/public-context-banner/public-context-banner.component.ts` | ✅ Estable (spec 0009) |
| `app-public-personal-data` | Dumb / Flujo Público | `data` (EnrollmentPersonalData, req), `context` (PublicEnrollmentContext\|null), `loading` (boolean, default false). Outputs: `dataChange`, `next`, `back`. Integra `app-public-context-banner` si hay contexto (AC3/AC5). **Campos (fix-001):** RUT* (con `validateRut()` + auto-formato + borde rojo/verde), Género* (select M/F), Nombres*, Apellido paterno*, Apellido materno (Opcional), Email* (`<app-email-input>`), Teléfono* (mín 8 chars), Fecha nacimiento*, Dirección (Opcional). **Validación edad:** `ageStatus()` computed con `calcAge()` — bloquea <17 años, bloquea <20 en profesional, warning 17-18 (autorización notarial). **`canAdvance()`:** delega a `canAdvanceFn()` (Functional Core exportada). **Functional Core exportado:** `getAgeStatus(birthDate, courseType): AgeAlertStatus`, `canAdvanceFn(data, courseType): boolean` — testeables sin framework (20 tests). `rutBorderStyle()` computed para feedback visual inline. **Estado de carga (fix-020):** `_isLoading = computed(() => loading() || _submitting())`. `_submitting` se resetea a `false` antes de emitir `next` (evita botón atascado si el padre detecta bloqueo y no avanza de paso). El padre debe pasar `[loading]="facade.isLoading()"` para mostrar estado async del servidor. | `shared/components/public-enrollment-steps/public-personal-data/public-personal-data.component.ts` | ✅ Estable (fix-020) |
| `app-public-payment-mode` | Dumb / Flujo Público | `options` (PublicPaymentModeOption[], req). Outputs: `modeSelect` (PaymentMode), `next`, `back`. Cards total/abono con precio en gradiente + sessions. Check animado. Info de abono contextual. CTA bloqueado hasta selección. Exporta `PublicPaymentModeOption`. | `shared/components/public-enrollment-steps/public-payment-mode/public-payment-mode.component.ts` | ✅ Estable (spec 0009) |
| `app-public-schedule` | Organismo / Flujo Público | `data` (EnrollmentAssignmentData, req), `loading` (boolean). Outputs: `dataChange`, `next`, `back`. Shell del paso: selector de instructor (`p-select` premium), estado "elige instructor", y delega la grilla semanal a **`<app-schedule-grid>`** (la lógica de nav/slots/resumen vive ahí, ya no aquí). CTA "Confirmar horario" bloqueado hasta `selectionComplete()`. | `shared/components/public-enrollment-steps/public-schedule/public-schedule.component.ts` | ✅ Estable (fix-018) |
| `app-public-documents` | Dumb / Flujo Público | `data` (EnrollmentDocumentsData, req), `isUploading` (boolean, default false). Outputs: `fileSelected ({ type, file })`, `next`, `back`. Guía visual inline foto válida/inválida (AC12). Preview si hay foto; área de upload si no. CTA bloqueado hasta upload. Área upload se deshabilita/opaca y muestra spinner+texto "Subiendo imagen..." mientras `isUploading`; botón Continuar cambia a "Subiendo..." con spinner. | `shared/components/public-enrollment-steps/public-documents/public-documents.component.ts` | ✅ Estable (spec 0009) |
| `app-public-contract` | Dumb / Flujo Público | `data` (EnrollmentContractData, req), `loading` (boolean). Outputs: `dataChange`, `generateContract`, `next`, `back`. Genera/muestra PDF contrato. Upload del contrato firmado. CTA "Continuar al Pago" bloqueado hasta `canAdvance`. | `shared/components/public-enrollment-steps/public-contract/public-contract.component.ts` | ✅ Estable (spec 0009) |
| `app-public-payment` | Organismo / Flujo Público | `summary` (PublicPaymentSummary, req). Outputs: `proceed`, `back`. Resumen alumno+escuela+curso+modalidad, desglose de clases con fecha/hora (AC10), total en gradiente, trust note (AC9), CTA Webpay con `data-llm-action="proceed-to-payment"`. Exporta `PublicPaymentSummary`. | `shared/components/public-enrollment-steps/public-payment/public-payment.component.ts` | ✅ Estable (spec 0009) |
| `app-public-confirmation` | Dumb / Matrícula | `type` (PublicConfirmationType, req: 'class_b' \| 'pre-inscription'), `enrollmentNumber` (string\|null), `message` (string\|null). Shows success state with next steps, different content per type. | `shared/components/matricula-steps/public-confirmation/public-confirmation.component.ts` | ✅ Estable |
| `app-psych-test` | Dumb / Matrícula | `answers` ((boolean\|null)[], req — 81 elementos EPQ), `loading` (boolean). Outputs: `answersChange` ((boolean\|null)[]), `next` (void), `back` (void). Paginado: 6 páginas de ~14 preguntas SI/NO. Barra de progreso interna (`answeredCount / 81` + `Página N/6`). Botón "Enviar test" en última página solo si todas respondidas. Estado de página interno (`currentPage` signal). **Footer sticky:** nav "← Volver / Siguiente →" tiene `position: sticky; bottom: 0; background: var(--bg-surface-elevated)` — visible sin scroll en mobile (hotfix-002). | `shared/components/matricula-steps/psych-test/psych-test.component.ts` | ✅ Estable |

## Organismos (Organisms)
*Secciones complejas y autónomas compuestas por moléculas y átomos.*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `RegistrarGastoFijoDrawerComponent` | Smart / Drawer — Registrar Gasto Fijo | Sin inputs. Renderizado vía `LayoutDrawerFacadeService.open()` desde `AdminContabilidadReportesComponent`. Inyecta `ReportesContablesFacade` para persistir y refrescar datos. Formulario: categoría (p-select), descripción (text), monto (number), fecha (date). Footer sticky con "Cancelar" y "Guardar Gasto" (`cursor-pointer`). Usa `DrawerContentLoaderComponent` + GSAP de entrada. Cierra vía `layoutDrawer.close()`. | `features/admin/contabilidad-reportes/registrar-gasto-fijo-drawer.component.ts` | ✅ Estable |
| `app-drawer-form` | Layout / Drawer shell (Dumb) | `hasFooter` (boolean, default true), `maxWidthRem` (number, default 36). Shell canónico del **cuerpo de un drawer de formulario** (encapsula `form-ux §6`): superficie `bg-surface` tipo card, cuerpo scrolleable con columna centrada `max-w-xl`, y footer fijo abajo vía slot `[drawer-form-footer]` (proyectar con `ngProjectAs`). Solo layout — los tokens `.field-*`/`.section-title` los aporta el drawer. Resuelve la inconsistencia de fondo/ancho/footer entre drawers de formulario. Campos a ancho completo de la card (`maxWidthRem` opcional). Consume las clases globales `.field-*`/`.section-title` (`styles/components/_form-fields.scss`). Consumido por **prácticamente todos los drawers de la app** (fix-024/fix-025/fix-026, propagación completa). Formulario (footer canónico proyectado): `task-create`, `admin-relator-crear/editar`, `admin-instructor-crear/editar`, `admin-secretarias-crear/editar`, `admin-promocion-crear/editar`, `registrar-pago`, `registrar-egreso`, `registrar-gasto-fijo`, `registrar-anticipo`, `admin-curso-singular-crear`, `vehicle-form`, `maintenance-form`, `dms-upload`, `dms-template`, `admin-inasistencia`, `admin-editar-perfil`, `agregar-servicio`, `registrar-venta`, `configurador-horarios-drawer`, `ajustes-drawer`. Solo lectura / sin footer único (`[hasFooter]="false"`): `admin-curso-singular-cobro/-inscribir`, `admin-sesion-drawer`, `admin-curso-singular-detalle-drawer`, `admin-pago-detalle-drawer`, `admin-pre-inscrito-drawer`, `recent-activity-drawer`, `alerts-drawer`, `admin-instructor-horas/-horario-drawer`. Ver-detalle con footer de acción única (editar): `admin-instructor-ver`, `admin-promocion-ver`, `admin-relator-ver`, `admin-secretarias-ver`, `agenda-slot-detail-drawer`, `student-drawer-detail`, `alumnos-por-vencer-drawer`, `vehicle-agenda-drawer`, `vehicle-documents-drawer`. Únicos que **NO** lo usan: `iniciar-clase`/`finalizar-clase` (excluidos explícitamente, pendiente evaluación futura). | `shared/components/drawer-form/drawer-form.component.ts` | ✅ Estable |
| `app-agenda-semanal` | Organismo / Agenda (Dumb) | `weekData`, `filteredDays`, `timeRows`, `isLoading`, `isCurrentWeek`, `instructors`, `selectedInstructorId`, `showKpis` (boolean, default true — ya no renderiza celdas KPI separadas; KPIs embebidos en hero strip vía `[kpis]`), `showHero` (boolean) — outputs: `weekNext`, `weekPrev`, `weekToday`, `instructorFilterChange`, `slotClick`. Hero `density="slim"` con KPI strip integrado (`heroKpis` computed: Clases Agendadas, Alumnos esta semana, Vehículos Disponibles, Clases Completadas), toolbar nav + filtro instructor (sin opción "Todos"). **Label contextual:** `"Mostrando horario de:"` fijo. CSS grid Lun-Vie. **Indicador de hora actual:** `nowTimeRow` → `border-top` rojo en celda + punto rojo en etiqueta de hora. | `shared/components/agenda-semanal/agenda-semanal.component.ts` | ✅ Estable |
| `app-agenda-slot` | Átomo / Agenda (Dumb) | `slot` (`AgendaSlot`, req), `compact` (boolean, false) — output: `slotClicked` (AgendaSlot). available: muestra solo hora (sin nombre de instructor — redundante con el filtro activo). scheduled/in_progress: hora + nombre alumno. completed: hora + check. no_show: tachado. | `shared/components/agenda-semanal/agenda-slot.component.ts` | ✅ Estable |
| `app-asistencia-teoria-drawer` | Smart / Asistencia Drawer | Abierto via `LayoutDrawerFacadeService` desde Admin/Secretaria Asistencia al presionar "Ver Lista" en una clase teórica. Inyecta `AsistenciaClaseBFacade`. **Info sesión:** tema + horario + instructor. **Enlace Zoom:** input URL + botón "Guardar" inline (`saveTeoriaZoomLink`). **Lista alumnos:** `localAlumnos` signal (copia local editable), toggle 3 estados por alumno (Presente/Ausente/Justificado con colores semánticos), campo justificación inline condicional, "Todos presentes" bulk action, contador `X/N presentes`. **Footer:** botón "Guardar Asistencia" (`saveTeoriaAsistencia`) + banner éxito 1.5s → cierre automático. Skeleton filas en carga. | `features/admin/asistencia/asistencia-teoria-drawer.component.ts` | ✅ Estable |
| `app-agenda-slot-detail-drawer` | Smart / Agenda Drawer (detalle, solo lectura) | Cargado via `NgComponentOutlet`. Lee `AgendaFacade.selectedSlot()`. Pill de estado (color semántico — incluye `available`→"Disponible"), horario 1.5rem bold (label "Horario disponible" si el slot está libre), tarjeta instructor+vehículo con icons, nombre del alumno + clase N° (solo si ocupado). **fix-017:** ahora se abre tanto para slots ocupados como disponibles (en la Agenda ya no se agendan clases — solo se consulta el detalle). Solo lectura. | `features/agenda/agenda-slot-detail-drawer.component.ts` | ✅ Estable |
| `app-registrar-venta-drawer` | Smart / Drawer | Inyecta `ServiciosEspecialesFacade`. Formulario de venta con lógica de auto-completado de precios y validación de deudores. Cierra el drawer al completar el registro. | `shared/components/servicios-especiales-content/drawers/registrar-venta-drawer.component.ts` | ✅ Estable |
| `app-agregar-servicio-drawer` | Smart / Drawer | Inyecta `ServiciosEspecialesFacade`. Formulario para añadir nuevos servicios al catálogo. | `shared/components/servicios-especiales-content/drawers/agregar-servicio-drawer.component.ts` | ✅ Estable |
| `app-task-create-drawer` | Smart / Drawer — Tareas | Inyecta `TasksFacade`, `LayoutDrawerFacadeService`. ReactiveForm: type (p-select: task/observation/question), recipient (p-select filtrado por `canSendTo()` + branch desde `facade.recipients()`), subject (req, max 200), body (opt, max 2000), due_date (input[date], visible solo si `type='task'`). Submit → `facade.createTask()` (incluye toast.success internamente) + cierra drawer. Error facade visible en template. `data-llm-action="submit-create-task"` + `data-llm-description` en campos críticos. OnPush. | `features/tareas/task-create-drawer.component.ts` | ✅ Estable |
| `app-task-detail-modal` | Smart / Modal — Tareas | Inyecta `TasksFacade`, `AuthFacade`. Lee `selectedTask()` + `selectedTaskReplies()`. Header: subject, type, status badge, sender→recipient, due_date. Body completo. Botones condicionales: "Marcar en progreso", "Marcar completada", "Marcar como vista", "Editar". Auto-`markSeen()` en efecto si `type='observation'` y destinatario (AC5). `app-task-reply-thread` integrado. `data-llm-action` en cada mutación. | `features/tareas/task-detail-modal.component.ts` | ✅ Estable |
| `app-alumnos-list-content` | Organismo / Tabla (Dumb) | `basePath` (string, req), `alumnos` (`AlumnoTableRow[]`, []), `isLoading` (boolean, false), `alumnosPorVencer` (`AlumnoTableRow[]`, []), **`trashView` (boolean, false)**. Outputs: `refreshRequested`, `claseOnlineAction`, `preInscritosRequested`, `archivarRequested` (id), **`trashViewToggled`** (void), **`restaurarRequested`** (id). **Papelera:** hero dinámico — back button `← Alumnos` via `backClickable` cuando `trashView=true`; subtítulo computed cambia a `"Papelera — Alumnos archivados"`. **Toolbar (estilo certificados):** buscador plain `input` con tokens inline (`var(--border-default)`, `var(--bg-surface)`), `h-9`, icon absoluto; 3 `p-select` con `optionLabel`/`optionValue` y primera opción `value:''` (reset a "Todos los cursos / estados / Expediente: Todos"); botones `btn-secondary flex items-center gap-2 text-sm` con `app-icon` — "Exportar" (download) y "Actualizar" (refresh-cw / loader-circle animado). Sin `pInputText` ni container-query CSS legacy. Columna Acciones: vista normal → eye/download/trash-2 (**el botón `award` de Certificado fue eliminado** — la gestión del certificado se hace desde la ficha individual del alumno); vista papelera → solo `rotate-ccw` verde. | `shared/components/alumnos-list-content/alumnos-list-content.component.ts` | ✅ Estable |
| `app-eliminar-alumno-modal` | Organismo / Modal (Dumb) | `visible` (boolean, req), `alumnoNombre` (string, req), `hasHistory` (boolean, false), `isDeleting` (boolean, false). Outputs: `confirmado` (), `cancelado` (). **Dos modos:** sin historial → confirmación simple; con historial → advertencia contable + campo de texto que exige escribir "borrarlo". `canConfirm` computed. Overlay fixed con backdrop blur. **Fondo:** `background: white` explícito (rompe cascade de `surface-hero`). **Botones:** `btn-neutral` (Cancelar) + `btn-danger-solid` (Archivar). **Estilos scoped:** `.confirm-input` + `.confirm-input--error` con focus ring `var(--state-error)`. | `shared/components/eliminar-alumno-modal/eliminar-alumno-modal.component.ts` | ✅ Estable |
| `app-branch-selector` | Átomo / Selector de sede | `branches` (BranchOption[], req), `selectedBranchId` (number\|null), `topbarMode` (boolean, true — dropdown compacto con glassmorphism para topbar; false = pills horizontales para wizard). Output: `branchChange` (number\|null). En `topbarMode=true`: chip trigger con dot de color + chevron animado + panel flotante glass (appAnimateIn + appClickOutside). Solo se pasa al componente cuando el usuario es admin — secretarias reciben `[]` y nunca lo ven. | `shared/components/branch-selector/branch-selector.component.ts` | ✅ Estable |
| `app-section-hero` | Organismo / Hero | `title` (req), `subtitle`, `contextLine`, `icon?`, `actions` (req), `backRoute`, `backLabel`, `chips`, `backClickable` (false), **`density` ('full'\|'slim', default 'full')**, **`kpis` (SectionHeroKpi[], [])**, **`loading` (boolean, false)**, **`loadingKpiCount` (number, 0)** — slim: barra ≈52px + skeleton shimmer interno + KPIs con sparkline inline (usa `loadingKpiCount` para skeletons estáticos); `hiddenOnMobile` en actions oculta secundarias en <sm; `menu: SectionHeroMenuItem[]` en acción full-mode dispara panel flotante custom (fix-019). full: bento-hero 180px gradient. Outputs: `actionClick`, `backClicked`, `kpiClick`. **AI-ready.** | `shared/components/section-hero/section-hero.component.ts` | ✅ Estable |
| `app-alumnos-list-content` | Organismo / Tabla | `basePath` (string, req), `alumnos`, `isLoading`, `alumnosPorVencer`, `trashView` — Lista con `app-section-hero density="slim"` (subtítulo dinámico + back button automático en Papelera via `backClickable`), KPI strip en hero (`heroKpis` computed: total/activos/deuda) + `app-action-kpi-card` "Por Vencer" bento-square, búsqueda, filtros (RF-085), tabla PrimeNG paginada, exportar ficha (RF-086). Drawer "Por Vencer". | `shared/components/alumnos-list-content/alumnos-list-content.component.ts` | ✅ Estable |
| `app-branch-selector` | Átomo / Selector de sede | `branches` (BranchOption[], req), `selectedBranchId` (number\|null), `showAllOption` (false), `topbarMode` (true — dropdown glassmorphism; false = pills wizard), `disabledBranchIds` (number[], [] — sedes deshabilitadas), `allOptionDisabled` (false — bloquea "Todas las escuelas"), `lockReason` (string\|null — texto de contexto en panel). Output: `branchChange` (number\|null). En `topbarMode=true`: chip + dot + panel flotante glass. Items disabled muestran ícono `lock` + tooltip con `lockReason`. Badge de razón visible en header del panel. Solo para admin — secretarias no lo ven. | `shared/components/branch-selector/branch-selector.component.ts` | ✅ Estable |
| `app-branch-gate` | Organismo / Gate de sede | `branches` (BranchOption[], req), `reason` (string — subtítulo explicativo), `professionalOnly` (false — filtra solo `hasProfessional=true`). Output: `branchSelected` (number). Pantalla de selección explícita de sede antes de arrancar una vista branch-scoped. Muestra cards bento por sede (primera con `.card-accent`), badge "Clase Profesional" condicional, animación GSAP de entrada. Dumb. Usar cuando `requiresSpecificBranch=true && selectedBranchId=null`. | `shared/components/branch-gate/branch-gate.component.ts` | ✅ Estable |
| `app-personal-data-step` | Organismo / Matricula | `data` (input req): `EnrollmentPersonalData` — incluye `courses: CourseOption[]`. `branches` (BranchOption[], []): sedes para admin — vacío oculta selector. `selectedBranchId` (number\|null). **2-level selector:** `selectedCategory = linkedSignal<CourseCategory\|null>()`; `filteredCourses computed` filtra por categoría. `canAdvance` requiere curso en `filteredCourses`. Computed: `courseMeta`, `coursePriceLabel`, `rutValid`, `ageStatus`. Outputs: `dataChange`, `next`, `cancel`, `branchChange` (number — solo admin), `rutBlur` (string — fix-020: emite el RUT al perder foco si es válido, para precargar un alumno existente). | `shared/components/matricula-steps/personal-data/personal-data.component.ts` | ✅ Estable |
| `app-flota-list-content` | Organismo / Flota (Dumb) | `vehicles` (`VehicleTableRow[]`, []), `kpis` (`FlotaKpis`), `isLoading` (false), `basePath` — outputs: `newVehicle`, `editVehicle` (id), `viewAgenda` (id), `viewMaintenances` (id), `printRouteSheet` (id), `printAllRouteSheets`, `typeFilterChange`, `statusFilterChange`. KPI strip en hero (`heroKpis` computed: total/disponibles/en-clase) + `app-action-kpi-card` "En Taller" bento-square (con click handler), tabla PrimeNG con filtros tipo/estado, acciones por fila (agenda, mantenimientos, editar, imprimir). | `shared/components/flota-list-content/flota-list-content.component.ts` | ✅ Estable |
| `app-alumnos-list-content` | Organismo / Tabla (Dumb) | `basePath` (string, req), `alumnos` (`AlumnoTableRow[]`, []), `isLoading` (boolean, false), `alumnosPorVencer` (`AlumnoTableRow[]`, []) — outputs: `refreshRequested`, `claseOnlineAction` ('zoom'\|'asistencia'), `preInscritosRequested` (void). Lista de alumnos con `app-section-hero`, bento KPIs, búsqueda, filtros, tabla PrimeNG con paginación. Botones hero "Enviar enlace Zoom" y "Registrar asistencia" emiten `claseOnlineAction`. "Ver Pre-inscritos" emite `preInscritosRequested` — el Smart padre maneja la navegación a `/pre-inscritos`. | `shared/components/alumnos-list-content/alumnos-list-content.component.ts` | ✅ Estable |
| `app-personal-data-step` | Organismo / Matricula | `data` (input req): `EnrollmentPersonalData` — incluye `courses: CourseOption[]`. **2-level selector:** `selectedCategory = signal<CourseCategory\|null>()` + `effect()` para sincronizar en re-edición; `availableCategories` muestra siempre las 3 categorías (non-professional/professional/singular); `filteredCourses computed` filtra `data().courses` por categoría activa. `canAdvance` requiere curso dentro de `filteredCourses`. Computed: `courseMeta`, `coursePriceLabel`, `rutValid`, `ageStatus`. | `shared/components/matricula-steps/personal-data/personal-data.component.ts` | ✅ Estable |
| `app-assignment-step` | Organismo / Matricula | `data` (input req): `EnrollmentAssignmentData` — incluye `instructors[]`, `scheduleGrid`, `scheduleLoading`, `promotionGroups: PromotionGroup[]`, `totalSessions` (12 para Clase B). **Clase B:** selector instructor + semana + slots. Navegación de semanas: `currentWeekIndex`, `weeks` (agrupa `daysFromGrid()` por lunes vía `getMondayKey()`), `prevWeek/nextWeek`. Counter `X/N` = `requiredCount` (12 full, 6 deposit). **Restricción:** máx 1 clase por día. **Auto-avance:** `selectSlot()` detecta adición (`wasAdded`) y llama `advanceToNextAvailableDay()` (busca el próximo día con slots libres en la semana; si no hay, salta a siguiente semana). **Panel resumen:** `selectedSlotsDisplay` computed (`{ id, label }[]` ordenado cronológicamente, formato `lun DD/MM · HH:MM – HH:MM`); se renderiza bajo el grid cuando hay ≥1 slot seleccionado, badge contador verde/naranja según completitud. Descripciones de modalidad de pago dinámicas (`totalSessions` / `totalSessions/2`). **Profesional:** itera `promotionGroups[]` → `@for (group)` → `@for (option)` — vacío muestra estado empty; opción `status=finished` deshabilitada. **Singular:** panel informativo. | `shared/components/matricula-steps/assignment/assignment.component.ts` | ✅ Estable |
| `app-documents-step` | Organismo / Matricula | `data` (input signal req) — Paso 3. Outputs: `fileSelected` (`{ type: string; file: File }`), `lightboxOpen`, `confirmPhoto` (fix-020), `next`, `back`. Sin `dataChange` — subida delegada al Smart padre vía `fileSelected`. Detecta cargas con `isUploaded(type)` interno. **Re-matrícula (fix-020):** cuando `data().photoNeedsConfirmation`, muestra banner con botón "Usar esta foto" (emite `confirmPhoto`) y `allRequiredUploaded` bloquea el avance hasta confirmar o reemplazar. | `shared/components/matricula-steps/documents/documents.component.ts` | ✅ Estable |
| `app-payment-step` | Organismo / Matricula | `data` (input req): `EnrollmentPaymentData` — Paso 4. **Descuentos predefinidos:** `@for (disc of data().availableDiscounts)` → chips toggleables via `selectPredefinedDiscount(id)` (toggle: si ya seleccionado → null). **Descuento manual:** `discountAmountInput`/`discountReason` signals locales → `applyManualDiscount()` limpia `selectedDiscountId`. **Método "pendiente"** deshabilitado cuando `data().isSingularCourse`. Botón continuar usa `data().canAdvance` (pricing + method). Outputs: `dataChange`, `next`, `back`. | `shared/components/matricula-steps/payment/payment.component.ts` | ✅ Estable |
| `app-contract-step` | Organismo / Matricula | `data` (input req): `EnrollmentContractData`. `stepNumber` (number, default 5). `isPublic` (boolean, false) — modo matrícula pública: oculta indicador 3 pasos, instrucciones de impresión y upload; muestra checkbox de aceptación de términos; botón dice "Continuar al Pago". **Admin:** flujo físico 3 pasos (Generar PDF → Imprimir+firmar → Subir escaneado). `canProceed` computed: `isPublic ? _termsAccepted() : data().canAdvance`. Outputs: `dataChange`, `generateContract`, `next`, `back`. | `shared/components/matricula-steps/contract/contract.component.ts` | ✅ Estable |
| `app-confirmation-step` | Organismo / Matricula | `data` (input signal) — Paso 6 del wizard de matrícula. | `shared/components/matricula-steps/confirmation/confirmation.component.ts` | ✅ Refactored (Signals/Control Flow) |
| `app-draft-list` | Organismo / Matricula | `drafts` (input req): `DraftSummary[]` — Lista de borradores. **Card clickeable:** el `.draft-card` completo tiene `(click)="resume.emit()"` + `role="button"` + `cursor: pointer`; el botón Descartar llama a `$event.stopPropagation()` para no activar el retomar accidentalmente. **Stepper rediseñado:** sistema de nodos + conectores (`step-node` + `step-connector`): nodo activo = círculo brand 13px con anillo difuso (`box-shadow: 0 0 0 3px var(--brand-muted)`), completados = 8px brand sólido, pendientes = 6px gris con borde; conectores de 2px brand/gris entre nodos — patrón visual `●─●─◉─○─○─○`. Outputs: `resume` (enrollmentId), `discard` (enrollmentId — padre maneja confirmación vía `ConfirmModalService`), `startNew` (void). CTA `+ Nueva matrícula` ancho completo con borde dashed. | `shared/components/matricula-steps/draft-list/draft-list.component.ts` | ✅ Estable |
| `app-dms-list-content` | Organismo / DMS (Dumb) | `basePath` (string, req), `studentsWithDocs` (`StudentWithDocsRow[]`), `recentDocs` (`DmsDocumentRow[]`), `schoolDocs` (`DmsDocumentRow[]`), `templates` (`TemplateCard[]`), `isLoading` (boolean), `isAdmin` (boolean). Outputs para upload (student, school, template), view, delete, download. 3 pestañas: Alumnos (grid alumnos con chips), Escuela (tabla docs), Plantillas (grid de cards con filtros). Handlers centralizados. | `shared/components/dms-list-content/dms-list-content.component.ts` | ✅ Estable |
| `app-horizontal-bar-chart` | Organismo / Visualización (Dumb) | `title` (string, ''), `subtitle` (string, ''), `data` (`ChartDataGroup[]`, []). Renderiza barra apilada + leyenda grid responsive. Usa colores semánticos (`'brand'|'success'|'warning'|'error'|'info'|'practica'|'teorico'|'ensayo'|'administrativo'`). | `shared/components/horizontal-bar-chart/horizontal-bar-chart.component.ts` | ✅ Estable |
| `app-weekly-schedule-grid` | Organismo / Agenda (Dumb) | `schedule` (`WeekSchedule|null`, req), `isLoading` (boolean, false). Outputs: `prevWeek`, `nextWeek`, `today`, `blockClick`. CSS Grid 7 cols, session cards con `[style]` tokens + `getDotStyle()`/`getCardClass()`, live indicator `.indicator-live`, skeleton data-driven, tokens 100% via `[style.color]`, sin CSS transforms. `styles:[]` (ViewEncapsulation). | `shared/components/weekly-schedule-grid/weekly-schedule-grid.component.ts` | ✅ Estable |
| `app-daily-schedule-timeline` | Organismo / Agenda Timeline (Dumb) | `daySchedule` (`DaySchedule\|null`), `weekDays`, `selectedDateString`, `isLoading` (boolean). Outputs: `prevDay`, `nextDay`, `todayNav`, `dateNav`, `blockClick`. **Hero card** "Próxima Clase" con `appCardHover` + `[style.background]="'var(--color-primary)'"`. **Timeline rail** vertical con `getDotStyle()` (Record de tokens CSS), `getCardClass()` (solo clases estructurales), `getCardStyle()` (`border-left-color` por status). `effect()` anima `viewChildren('timelineNode')` vía `gsap.staggerListItems()` sin `setTimeout`. Tokens 100% via `[style]`/`[style.color]`; sin CSS transforms, sin `animate-in`, sin `!border-l-*`. `color-mix()` para overlays en skeleton hero. AI-ready: `data-llm-action`. | `shared/components/daily-schedule-timeline/daily-schedule-timeline.component.ts` | ✅ Renovada |
| `app-egreso-modal` | Organismo / Modal (Dumb) | `isOpen` (boolean, false), `isSaving` (boolean, false). Outputs: `guardar` (`EgresoFormData`), `cerrado` (void). Modal overlay con formulario reactivo. **DEPRECADO**: reemplazado por `RegistrarEgresoDrawerComponent` vía LayoutDrawer. | `shared/components/egreso-modal/egreso-modal.component.ts` | ⚠️ Deprecado |
| `RegistrarEgresoDrawerComponent` | Smart / Drawer — Registrar Egreso Cuadratura | Sin inputs. Renderizado vía `LayoutDrawerFacadeService.open()`. Inyecta `CuadraturaFacade` para persistir y refrescar datos. Formulario: tipo (select: 'gasto'\|'anticipo'), monto (number, req), descripcion (text, req), fecha (display-only hoy). Tokens `.field-label`/`.field-input`/`.field-error` + `.btn-primary`/`.btn-secondary` canónicos. Cierra vía `layoutDrawer.close()`. | `features/admin/contabilidad-cuadratura/registrar-egreso-drawer.component.ts` | ✅ Estable |
| `app-cuadratura-content` | Organismo / Cuadratura (Dumb) | `pagosHoy` (`IngresoRow[]`, req), `gastosHoy` (`EgresoRow[]`, req), `fondoInicial` (number, 50000), `totalIngresosHoy` (number, req), `totalEgresosHoy` (number, req), `saldoTeorico` (number, req), `cajaYaCerrada` (boolean, false), `isLoading` (boolean, false), `isSaving` (boolean, false). Output: `guardarCierre` (`CierrePayload`). UI completa de cuadratura diaria. Usa `display: contents` para ser transparente al bento-grid padre. | `shared/components/cuadratura-content/cuadratura-content.component.ts` | ✅ Bento Compliant |
| `app-asistencia-clase-b-content` | Organismo / Asistencia Clase B (Dumb) | Pestañas **Prácticas \| Ciclos Teóricos** (Spec 0001) con patrón **segmented tabs** (`bg-subtle` + botón activo en `var(--bg-surface)` con sombra sutil — mismo patrón que `alumno-clases.component.ts`, no pills de color brand). Inputs prácticas: `kpis`, `clasesPracticas`, `alertas`, `instructores`, `selectedDate`, `isLoading`, `isSaving`. Inputs ciclos: `cycles`, `selectedCycleId`, `clasesCiclo`, `rosterCiclo`, `addableStudents`, `isLoadingCiclos`, `isLoadingCycle`. Embebe `app-ciclos-teoricos-content`. | `shared/components/asistencia-clase-b-content/asistencia-clase-b-content.component.ts` | ✅ Bento Compliant |
| `app-ciclos-teoricos-content` | Organismo / Ciclos Teóricos (Dumb) | **Spec 0001** (+ fix-021/022/023). Selector de ciclo (RF-11), 6 clases con tema opcional + enlace Zoom y envío con destinatarios preseleccionables (RF-14/15/16), roster (RF-12) con reasignación entre ciclos. "Incorporar alumno de otro ciclo" es un `p-dialog` modal (no panel embebido) con spinner por fila (`addingEnrollmentId` signal + `onAddStudent()`, limpiado vía `effect()` cuando `isSaving()` pasa de `true` a `false`, mismo patrón que `sendingClassId`/envío Zoom). | `shared/components/ciclos-teoricos-content/ciclos-teoricos-content.component.ts` | ✅ Nuevo |
| `app-cuadratura-content` | Organismo / Cuadratura (Dumb) | `pagosHoy` (`IngresoRow[]`, req), `gastosHoy` (`EgresoRow[]`, req), `fondoInicial` (number, 50000), `totalIngresosHoy` (number, req), `totalEgresosHoy` (number, req), `saldoTeorico` (number, req), `cajaYaCerrada` (boolean, false), `isLoading` (boolean, false), `isSaving` (boolean, false). Output: `guardarCierre` (`CierrePayload`). UI completa de cuadratura diaria: tabla ingresos (N° Boleta/Glosa/Clase B/Clase A/Sensom./Otros/Total), tabla egresos/retiros, arqueo de caja física (billetes+monedas con inputs qty), justificación descuadre, barra inferior fija con KPIs (INGRESOS/EGRESOS/DEBE HABER/EFECTIVO ARQUEO/DIFERENCIA en colores semánticos) y botón "Cerrar Caja y Guardar". Estado de arqueo interno con signals. Diferencia: verde=0, rojo<0, naranja>0. | `shared/components/cuadratura-content/cuadratura-content.component.ts` | ✅ Estable |
| `app-live-classes-panel` | Organismo / Dashboard (Dumb) | `classes` (`LiveClassModel[]`, []). Output: `actionClick` (LiveClassModel). Widget horizontal con scroll snap para el dashboard. Muestra tarjetas con alumno, instructor, hora, y estado (pending, in_progress, completed). Incluye EmptyState integrado y hover interactivo (`CardHoverDirective`). | `shared/components/live-classes-panel/live-classes-panel.component.ts` | ✅ Estable |
| `app-asistencia-clase-b-content` | Organismo / Asistencia Clase B (Dumb) | `kpis` (`AsistenciaClaseBKpis\|null`), `clasesTeorias` (`ClaseTeoricoRow[]`), `clasesPracticas` (`ClasePracticaRow[]`), `alertas` (`AlertaFaltaConsecutiva[]`), `instructores` (`InstructorOption[]`), `selectedDate` (string YYYY-MM-DD), `isLoading` (boolean), `isSaving` (boolean). Outputs: `markAttendance` ({sessionId, status}), `justifyAbsence` ({sessionId, reason}), `removeSchedule` (enrollmentId), `reactivateSchedule` (enrollmentId), `sendReminder` (enrollmentId), `viewAtendanceList` (`ClaseTeoricoRow` — abre drawer lista alumnos teórica), `dateChange` (string YYYY-MM-DD), `exportExcel`, `refreshRequested`, `configureZoom`. **Secciones:** (1) `app-section-hero density="slim"` con KPI strip (`heroKpis` computed: tasa asistencia / inasistencias hoy / clases en curso / pendientes por iniciar); (2) Alertas faltas consecutivas; **(4) Tabla asistencia prácticas** (primera) con date picker, filtros status (pills) + instructor (select `cursor-pointer`) + acciones inline. **Columnas tabla prácticas:** Agendada (`scheduled_at`) / **Inicio** (`horaInicioReal`, `—` si null) / **Fin** (`horaFinReal`, `—` si null) / Sede / Instructor / Alumno / **Vehículo** (patente bold + marca·modelo en muted) / Estado / Acciones; **(5) Tabla clases teóricas Zoom**. **Orden de secciones:** Prácticas → Teóricas. | `shared/components/asistencia-clase-b-content/asistencia-clase-b-content.component.ts` | ✅ Estable |
| `app-historial-cuadraturas-content` | Organismo / Calendario Cuadraturas (Dumb) | `cierres` (`HistorialCierre[]`, []), `isLoading` (boolean), `mesActual` (number), `anioActual` (number), `isExporting` (boolean), `backRoute` (string\|null), `backLabel` (string) — outputs: `mesAnterior`, `mesSiguiente`, `exportarMes` ('excel'\|'pdf'), `cierreClicked` (HistorialCierre). **Estructura (hotfix-011):** `app-section-hero density="slim"` hijo directo del bento-grid + barra toolbar `bento-banner card` con navegador prev/next-mes y botón Exportar (con dropdown inline). Calendario mensual CSS Grid 7 cols (LUN-DOM). `computed calendarDays()` genera celdas con offset lunes-primero. Celdas: badge Cuadrado (verde) / Descuadre (rojo) + monto diferencia. Día actual → círculo brand. Click en celda emite `cierreClicked`. Vista mobile: lista vertical. | `shared/components/historial-cuadraturas-content/historial-cuadraturas-content.component.ts` | ✅ Estable |
| `app-detalle-cuadratura-modal` | Organismo / Drawer Content (Smart-ish) | Inyecta `HistorialCuadraturasFacade`. Muestra detalle de un cierre arqueado con Bento-Grid layout, KPI cards con counter y desglose de denominaciones. | `shared/components/detalle-cuadratura-modal/detalle-cuadratura-modal.component.ts` | ✅ Premium |
| `app-pago-instructor-modal` | Organismo / Drawer (Smart-ish) | **Migrado a LayoutDrawer**. Inyecta `LiquidacionesFacade` para leer `selectedRow()`. Resumen de liquidación, selector método (Efectivo|Transferencia) y confirma pago directamente en el Facade. Cierra vía `LayoutDrawerFacadeService`. | `shared/components/pago-instructor-modal/pago-instructor-modal.component.ts` | ✅ Estable |
| `app-servicios-especiales-content` | Organismo / Servicios Especiales (Dumb) | `catalogo`, `ventas`, `kpis`, `isLoading`, `backRoute`. Outputs: `requestRegistrarVenta` (ServicioEspecial?), `requestNuevoServicio`, `cobroRegistrado`, `exportarHistorial`. Hero `density="slim"` con KPI strip (`heroKpis` computed: ventas mes / total recaudado / pend. cobro / total registros; `formatCLP` para valores monetarios), catálogo en grid con tarjetas de acción, historial de ventas con filtros. Delega formularios al `LayoutDrawer`. RF-037. | `shared/components/servicios-especiales-content/servicios-especiales-content.component.ts` | ✅ Estable |
| `app-liquidaciones-content` | Organismo / Liquidaciones (Dumb) | `liquidaciones`, `isLoading`, `isSaving`, `kpis`, `mesActual`, `anioActual`. Hero `density="slim"` con KPI strip integrado (`heroKpis` computed: Total Nómina / Anticipos / Pagados — elimina `app-kpi-card-variant` bento-squares anteriores). Tabla desktop: columnas Instructor / **Clases Impartidas** / **Horas Equivalentes** / Base / Anticipos / Total / Acciones. `LiquidacionRow` expone `practicalSessions` y `totalHours` (sesiones × 0.75 h). Vista móvil: horas + conteo clases entre paréntesis. | `shared/components/liquidaciones-content/liquidaciones-content.component.ts` | ✅ Estable |
| `app-horizontal-bar-chart` | Organismo / Visualización (Dumb) | `title` (string, ''), `subtitle` (string, ''), `data` (`ChartDataGroup[]`, []). Cada item: `label`, `value` (horas), `color` (`'brand'\|'success'\|'warning'\|'error'\|'info'\|'practica'\|'teorico'\|'ensayo'\|'administrativo'` — mapeado a tokens CSS), `percent`. Renderiza barra apilada + leyenda grid responsive. **IMPORTANTE:** usar `color` semántico (NO `colorHex`). | `shared/components/horizontal-bar-chart/horizontal-bar-chart.component.ts` | ✅ Estable |
| `app-reportes-contables-content` | Organismo / Reportes Contables (Dumb) | RF-030/RF-031. Inputs: `kpis`, `ingresosCategoria`, `gastosCategoria`, `evolucionMensual`, `detalleDiario`, `diasConMovimientos`, `escuela`, `isLoading`, `isExporting`, `gastosFijos`, `filtros`. Outputs: `aplicarFiltros`, `exportRequested`, `registrarGastoClick` (void — el botón "+ Registrar Gasto Fijo" delega la apertura del drawer al Smart padre), `verDetalle`. Drawer de gastos fijos removido — se abre vía `LayoutDrawerFacadeService`. | `shared/components/reportes-contables-content/reportes-contables-content.component.ts` | ✅ Estable |
| `app-certificacion-clase-b-content` | Organismo / Certificación (Dumb) | Hero `density="slim"` con KPI strip (`heroKpis` computed: total/generados/pend.generación/pend.envío). Inputs: `alumnos`, `kpis`, `log`, `isLoading`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes`. Outputs: `generarCertificado`, `verCertificado`, `enviarEmail`, `generarPendientes`, `enviarEmailsMasivo`, `exportar`. **Señales internas:** `searchQuery`, `estadoFilter`, `currentPageAlumnos`, `currentPageLog`, `emailConfirmId`, `masivoPanelVisible`, `pendingConfirmId`, `pendientesPanelVisible`. **Computed:** `alumnosPendientes` (filtra `certificadoStatus==='pendiente'`). **Métodos locales:** `abrirPanelPendientes()`, `cancelarPendientes()`, `confirmarPendientes()` (emite `generarPendientes` y cierra panel), `abrirPanelMasivo()`, `cancelarMasivo()`, `confirmarMasivo()`. **Estructura (card unificado):** toolbar (buscador h-9 + filtro p-select + Generar Pendientes + Enviar Masivo + Exportar todos) + panel pendientes (two-step: file-check verde / alert-triangle advertencia para teoría<100%) + panel masivo (lista destinatarios) + tabla 9 cols — todo dentro de un único `bento-banner card overflow-hidden`, separados con `border-bottom`. Log es un `bento-banner` separado. **Email individual:** `onClickEmail` abre fila confirmación inline (brand-tinted); muestra "Reenviar"+"mail-check" si `emailEnviado`. **Búsqueda:** insensible a tildes, RUT normalizado. **Paginación:** PAGE_SIZE=10, barra prev/next, oculta si 1 página. | `shared/components/certificacion-clase-b-content/certificacion-clase-b-content.component.ts` | ✅ Estable |
| `app-certificacion-profesional-content` | Organismo / Certificación Profesional (Dumb) | Inputs: `promociones`, `cursos`, `selectedPromocionId`, `selectedCursoId`, `alumnos`, `kpis`, `log`, `isLoading`, `isLoadingAlumnos`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes`. Outputs: `promocionSelected`, `cursoSelected`, `generarCertificado`, `verCertificado`, `enviarEmail`, `generarPendientes`, `enviarEmailsMasivo`, `exportar`. **Señales internas:** `searchQuery`, `estadoFilter`, `currentPageAlumnos`, `currentPageLog`, `masivoPanelVisible`, `pendingConfirmId`, `emailConfirmId`, `pendientesPanelVisible`. **Computed:** `pendientesElegibles` (alumnos elegibles con cert pendiente), `pendientesNoElegibles` (bloqueados: teoría<75%, nota<75, pago pendiente), `alumnosParaEnvioMasivo`. **Métodos locales:** `abrirPanelPendientes()`, `cancelarPendientes()`, `confirmarPendientes()`, `onClickEmail()`, `confirmarEmail()`, `cancelarEmail()`, `abrirPanelMasivo()`, `cancelarMasivo()`, `confirmarMasivo()`. **Estructura idéntica a Clase B:** toolbar + panel pendientes (two-step: elegibles en verde file-check / no-elegibles en x-circle con razón) + panel masivo + tabla — dentro de un único `bento-banner card overflow-hidden`. Log separado. **Email individual:** `onClickEmail` → fila confirmación inline brand-tinted; muestra "Reenviar"+"mail-check" si `emailEnviado`. **Lógica generar:** `elegible` (teoría≥75%+pago+nota≥75); práctica<100% → fila confirmación inline. **Cascada:** selección promoción → curso → contenido. | `shared/components/certificacion-profesional-content/certificacion-profesional-content.component.ts` | ✅ Estable |

## Admin — Pestañas de Configuración Web (spec-0013)
*Sub-componentes de `AdminConfiguracionWebComponent`. Reciben un slice `FormGroup`/`FormArray` por referencia; las mutaciones propagan al formulario raíz automáticamente (Angular by-reference). No tienen ruta propia.*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-general-tab` | Smart-lite / Tab | `brandGroup` (FormGroup, req), `socialGroup` (FormGroup, req), `branchId` (number, req). Inyecta `WebsiteConfigFacade`, `ToastService`. Maneja subida de Logo/Favicon/OG-Image vía `optimizeImage` + `facade.uploadAsset`. `.field-label`/`.field-input` locales (ViewEncapsulation.Emulated). | `features/admin/configuracion-web/tabs/general-tab.component.ts` | ✅ Estable (spec-0013) |
| `app-hero-tab` | Smart-lite / Tab — Config Studio | `heroGroup` (FormGroup, req), `branchId` (number, req). Inyecta `WebsiteConfigFacade`, `ToastService`. Icon picker con lazy-load (`visibleIconsCount` signal + scroll detection), 7 listas de íconos, `filteredIcons` computed, `@HostListener('document:click')` para close-on-outside. Accede a grupo hermano vía `heroGroup().root.get('brand.theme')`. Subida hero media y background con `optimizeImage`. CSS Studio embebido (~60 reglas en `styles:[]`). | `features/admin/configuracion-web/tabs/hero-tab.component.ts` | ✅ Estable (spec-0013) |
| `app-cursos-tab` | Smart-lite / Tab | `coursesArray` (FormArray, req), `pricingFooterGroup` (FormGroup, req). Inyecta `CoursesFacade`, `FormBuilder`. Browser del catálogo de cursos con selección y override de precio. `asFormGroup()` helper para iteración de FormArray. | `features/admin/configuracion-web/tabs/cursos-tab.component.ts` | ✅ Estable (spec-0013) |
| `app-promo-tab` | Dumb / Tab | `promoGroup` (FormGroup, req). Sin inyecciones. Formulario de configuración de la sección promocional (título, subtítulo, imagen, CTA). | `features/admin/configuracion-web/tabs/promo-tab.component.ts` | ✅ Estable (spec-0013) |
| `app-contacto-tab` | Smart-lite / Tab | `contactGroup` (FormGroup, req), `hoursArray` (FormArray, req). Inyecta `FormBuilder`. `addHour()`/`removeHour()` mutan el FormArray. Formulario de datos de contacto + horarios de atención. | `features/admin/configuracion-web/tabs/contacto-tab.component.ts` | ✅ Estable (spec-0013) |
| `app-faqs-tab` | Smart-lite / Tab | `faqsArray` (FormArray, req). Inyecta `FormBuilder`. `addFaq()` pushea `fb.group({question, answer})` con `Validators.required`; `removeFaq(index)` llama `removeAt()`. Llama `markAsDirty()` tras cada mutación. | `features/admin/configuracion-web/tabs/faqs-tab.component.ts` | ✅ Estable (spec-0013) |

## Layout (Shell)
*Componentes estructurales del shell de la aplicación — no son páginas enrutables.*

| Componente | Tipo | Propósito | Ubicación | Estado |
|------------|------|-----------|-----------|--------|
| `AppShellComponent` | Smart | Layout principal: sidebar + topbar + router-outlet; drawer animado en mobile. Renderiza el modal global de `ConfirmModalService` (z-70, `surface-glass`). Sin View Transitions ni animatePageEnter (evitar race conditions). | `layout/app-shell.component.ts` | ✅ Estable |
| `SidebarComponent` | Smart | Sidebar de navegación con grupos (`NavGroup[]`): headers de sección + pills GSAP, theme toggle y avatar de usuario | `layout/sidebar.component.ts` | ✅ Estable |
| `TopbarComponent` | Smart | Barra superior: selector DEV de rol (ROL dropdown), búsqueda global (Ctrl+K), cambio de tema, badge notificaciones (`animateBell`), avatar de usuario, selector de sede (`app-branch-selector`) visible para admin y secretaria con grant multi-sede (computed `canSeeBranchSelector`, spec 0017) | `layout/topbar.component.ts` | ✅ Estable |
| `LayoutDrawerComponent` | Smart (Adaptativo) | Panel lateral dinámico. **Desktop (≥768px):** layout-shift (empuja el contenido). **Mobile (<768px):** fullscreen fixed + backdrop GSAP + scroll lock. Renderiza componentes via `NgComponentOutlet`. Controlado por `LayoutDrawerService` (usa `LayoutDrawerFacadeService` desde UI). | `layout/layout-drawer.component.ts` | ✅ Estable |
| `AjustesDrawerComponent` | Smart / Drawer | Panel de ajustes con 3 tabs. **Mi Perfil:** identity header (avatar initials/foto, role badge por rol con ícono Lucide, email chip), 2 `app-stat-box` (Sede + Estado), contraseña colapsable. **Ajustes:** theme toggle, landing editor, branch switcher (solo admin). **Seguridad:** log auditoría (solo admin). Computed `branchLabel()` resuelve `branchId → name` vía `BranchFacade`. | `shared/components/ajustes-drawer/ajustes-drawer.component.ts` | ✅ Estable |
| `ConfiguradorHorariosDrawerComponent` | Smart / Drawer | Asistente para generar automáticamente los bloques horarios de la base de datos (`schedule_blocks`). Permite configurar reglas matemáticas por turnos (hora inicio, hora fin, duración, descanso) y previsualizar/eliminar excepciones. Consume `AdminHorariosFacade`. Inyectado a través de `AjustesDrawerComponent` (push en el stack). | `features/admin/configuracion-horario/configurador-horarios-drawer.component.ts` | ✅ Implementado |

## Moléculas — Auth
*Formularios y flujos de autenticación.*

| Componente | Tipo/Categoría | Props principales | Ubicación | Estado |
|------------|----------------|-------------------|-----------|--------|
| `app-login-card` | Auth / Form | `mode` ('login'|'reset'), `loading` (false), `errorMsg` (''), `successMsg` ('') — outputs: `modeChange`, `formSubmit` (LoginFormData). Sin `@if` para layout: contraseña y mensajes siempre en DOM, GSAP anima `height`+`opacity` vía `effect()` + `viewChild`. `LoginFormData`: `{ email, password }`. | `shared/components/login-card/login-card.component.ts` | ✅ Estable |

## Páginas / Vistas (Pages)
*Componentes enrutables (Smart components) que consumen Servicios.*

| Ruta / Componente | Propósito | Servicios Inyectados | Ubicación | Estado |
|-------------------|-----------|-----------------------|-----------|--------|
| `/app/admin/configuracion-web` y `/app/secretaria/configuracion-web` — `AdminConfiguracionWebComponent` | Vista premium Bento-Grid para configurar y publicar en caliente las Landing Pages (Azul y Roja). **Branch wiring:** usa `BranchFacade.selectedBranchId()` como única fuente de verdad — admin sin sede muestra empty state (seleccionar en topbar); secretaria ve chip read-only con su sede. Tabs: General, Hero Config Studio (layouts, media, CTA, Trust Badge), Cursos (FK al catálogo operacional + override de precio), Promo, Contacto, FAQs. | `WebsiteConfigFacade`, `CoursesFacade`, `AuthFacade`, `BranchFacade`, `GsapAnimationsService`, `ToastService` | `features/admin/configuracion-web/admin-configuracion-web.component.ts` | ✅ Estable |
| `/app/admin/dashboard` — `DashboardComponent` | Dashboard principal (referencia canónica del DS). Usa `app-section-hero` **`density="slim"`** con `[kpis]="heroKpis()"` (4 KPIs inline — alumnos, clases, ingresos, vehículos). Sin `app-kpi-card-variant` separados (eliminados en spec-0015). Actividad reciente y alertas en bento. Hero con acciones rápidas (abre wizard matrícula vía `LayoutDrawerFacadeService`). | `DashboardFacade`, `LayoutDrawerFacadeService` | `features/dashboard/dashboard.component.ts` | ✅ Estable |

| `/login` — `LoginComponent` | Login y reset de contraseña. Orbs decorativos + `surface-glass` card. Entrada con `GsapAnimationsService.animateHero()`. Botón dev `isDevMode()`. | `AuthFacade`, `GsapAnimationsService` | `features/auth/login/login.component.ts` | ✅ Estable |
| `/inscripcion` — `PublicEnrollmentComponent` | **Rediseñado (spec 0009).** Smart wizard premium sin auth. Host `[attr.data-public-theme]` (azul/roja desde `branchIdToTheme`). Lógica de entrada: `resolveEntry(branchId, courseId)` → gate `resolving` / draft restore / orientation / wizard. Shell premium `app-public-wizard-shell` + `@switch` con 10 steps (license-type, personal-data, payment-mode, schedule, documents, contract, payment, psych-test-intro, psych-test, confirmation/pre-confirmation). Computed: `context()` (PublicEnrollmentContext para banners), `paymentModeOptions()`, `paymentSummary()`, `step[N]Data()`. Señales locales: `resolving`, `_step1Form`, `_contractStatus`, `_contractPdfUrl`, `_signedContractUpload`. | `PublicEnrollmentFacade`, `ActivatedRoute` | `features/public-enrollment/public-enrollment.component.ts` | ✅ Estable |
| `/inscripcion/retorno` — `PublicEnrollmentRetornoComponent` | **Rediseñado (spec 0009, T4.3).** Retorno Webpay premium con tematización de sede. Lee `branchId` de `pec_pending` (sessionStorage) vía `readPendingBranchId()`. `[attr.data-public-theme]="theme()"`. 4 estados premium: `loading`, `success` (folio `.kpi-value` + gradiente de sede + CTA WhatsApp **primario** / portal secundario / inicio terciario — AC11), `rejected`, `error`. `whatsappHref` = `wa.me/` (TODO: URL real cuando `branches` exponga WhatsApp). | `PublicEnrollmentFacade`, `ActivatedRoute` | `features/public-enrollment/retorno/public-enrollment-retorno.component.ts` | ✅ Estable |
| `/**` — `NotFoundComponent` | Página 404 | — | `features/not-found/not-found.component.ts` | ✅ Estable |
| **— PORTAL ADMIN (24 stubs) —** | | | | |
| `/app/admin/alumnos/ex-alumnos` — `AdminExAlumnosComponent` | **Gestión de Ex-Alumnos (Admin)** — **Layout canónico `bento-grid[appBentoGridLayout]`**. Hero con `backRoute="/app/admin/alumnos"` (← Alumnos) + acciones "Exportar" / "Ver Alumnos Activos". 4 KPIs `bento-square` (`app-kpi-card-variant`): total, Clase B, Profesional, con abono pendiente. `bento-banner card` con tabla custom HTML: búsqueda + filtros compactos (año, licencia) + 4 columnas (EGRESADO, LICENCIA badge, AÑO/SEDE, ESTADO CUENTA). Skeleton 5 filas. `app-admin-stats-panel` + `app-admin-ex-alumnos-comments` como `bento-wide` (50/50 en desktop). `computed()` para filtrado client-side. `effect()` recarga al cambiar sede. **Re-matrícula (fix-020):** botón "Re-matricular" por fila (`reEnroll`) → `/app/admin/matricula?rut=<rut>`. | `ExAlumnosFacade`, `BranchFacade` | `features/admin/alumnos/ex-alumnos/admin-ex-alumnos.component.ts` | ✅ UI Ready |
| `/app/admin/alumnos` | **Base de Alumnos Clase B** (spec 0016) — Reutiliza `AlumnosListContentComponent`. Solo muestra alumnos `license_group='class_b'` (el facade filtra y elige el enrollment representativo dentro de B). Ya **no** expone acceso a Pre-inscritos (es profesional, movido a la base Profesional); acción de archivo histórico renombrada a "Ex-Alumnos B". | `AdminAlumnosFacade`, `BranchFacade` | `features/admin/alumnos/admin-alumnos.component.ts` | ✅ UI Ready |
| `/app/admin/clase-profesional/pre-inscritos` | **Pre-inscritos Clase Profesional** (spec 0016: ruta movida desde `/alumnos/pre-inscritos`) — Usa `app-section-hero` canónico (`icon="users"`, `backRoute="/app/admin/clase-profesional/alumnos"`). 3 KPIs via `app-kpi-card-variant`, búsqueda + filtros por estado/clase. Click en fila → `facade.select(row)` + `resetPromocionesCache()` + `layoutDrawer.open(AdminPreInscritoDrawerComponent, nombre, 'eye')`. `effect()` recarga al cambiar sede. | `AdminPreInscritosFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/admin/alumnos/pre-inscritos/admin-pre-inscritos.component.ts` | ✅ Estable |
| `/app/admin/clase-profesional/alumnos` · `/app/secretaria/profesional/alumnos` | **Base de Alumnos Profesional** (spec 0016) — Smart admin (`AdminAlumnosProfesionalComponent`) + secretaría (`SecretariaAlumnosProfesionalComponent`). Inyectan `AdminAlumnosProfesionalFacade`; reutilizan el Dumb `app-alumnos-profesional-list-content`. `effect(branchId)` + `destroyRef.dispose()`. Archivar vía `app-eliminar-alumno-modal`; navegan a pre-inscritos profesional. | `AdminAlumnosProfesionalFacade`, `BranchFacade` | `features/{admin/alumnos-profesional/admin-alumnos-profesional, secretaria/alumnos-profesional/secretaria-alumnos-profesional}.component.ts` | ✅ UI Ready |
| `/app/admin/ex-alumnos-profesional` · `/app/secretaria/ex-alumnos-profesional` | **Ex-Alumnos Profesional** (spec 0016) — Smart admin (`AdminExAlumnosProfesionalComponent`) + secretaría (`SecretariaExAlumnosProfesionalComponent`). Thin smarts sobre el Dumb compartido `app-ex-alumnos-profesional-content`, consumen `ExAlumnosFacade.egresadosProfesionalList()`. **Re-matrícula (fix-020):** cada smart maneja `(reEnroll)` del dumb → navega a `/app/{admin,secretaria}/matricula?rut=<rut>`. | `ExAlumnosFacade`, `BranchFacade` (admin) | `features/{admin/ex-alumnos-profesional/admin-ex-alumnos-profesional, secretaria/ex-alumnos-profesional/secretaria-ex-alumnos-profesional}.component.ts` | ✅ UI Ready |
| `/app/secretaria/ex-alumnos` — `SecretariaExAlumnosComponent` | **Ex-Alumnos B (Secretaría)** (spec 0016) — reconstruido de stub a funcional; espejo de `AdminExAlumnosComponent` con rutas de secretaría (`backRoute="/app/secretaria/alumnos"`), `egresadosClaseBList`, reusa `app-admin-stats-panel` + `app-admin-ex-alumnos-comments`. Carga vía `ngOnInit`. **Re-matrícula (fix-020):** cada fila tiene botón "Re-matricular" (`reEnroll`) que navega a `/app/secretaria/matricula?rut=<rut>` → el wizard precarga al alumno (mismo motor, sin flujo paralelo). | `ExAlumnosFacade` | `features/secretaria/ex-alumnos/secretaria-ex-alumnos.component.ts` | ✅ UI Ready |
| `AdminPreInscritoDrawerComponent` | Smart / Drawer — Abierto vía `LayoutDrawerFacadeService`. `afterNextRender → detectChanges()` para fix OnPush+NgComponentOutlet. 3 tabs: (1) **Datos** — read-only: nombre, RUT, email, teléfono, sede, clase, fecha nacimiento, género, dirección (capturados en form público), vencimiento; (2) **Test Psicológico** — 81 preguntas EPQ (scroll del drawer padre) + formulario Apto/No Apto con motivo; llama `facade.evaluarTest()` directamente; (3) **Completar Matrícula** — solo si status='approved'; `selectMatriculaTab(licencia)` dispara `cargarPromocionesParaLicencia()` directamente (sin effect); promociones con 3 estados: loading/vacío/select; form: licencia previa + fechas + pago; `completarMatricula()` → `layoutDrawer.close()`. `birth_date`, `gender`, `address` provienen de la pre-inscripción (no los pide el admin). | `AdminPreInscritosFacade`, `LayoutDrawerFacadeService` | `features/admin/alumnos/pre-inscritos/admin-pre-inscrito-drawer.component.ts` | ✅ Estable |
| `AdminClaseOnlineDrawerComponent` | Smart / Drawer — Enviar Zoom & Registrar Asistencia (RF-054) | `isOpen` (boolean, req), `mode` ('zoom'\|'asistencia', default 'zoom') — outputs: `closed`, `saved`. Inyecta `ClaseOnlineFacade`. Al abrir: `effect()` → `facade.cargarDatos()`. **Modo zoom:** input URL + textarea mensaje; pre-rellena el link si la sesión ya tiene uno. **Modo asistencia:** lista de alumnos con checkboxes individuales + "Seleccionar todos". `canSave` computed: zoom requiere link no vacío, asistencia requiere ≥1 seleccionado. Banner éxito inline + auto-cierre 1.2s. | `ClaseOnlineFacade` | `features/admin/alumnos/clase-online-drawer/admin-clase-online-drawer.component.ts` | ✅ Estable |
| `/app/admin/alumnos/:id` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`. **3 estados como hijos directos del grid:** (1) Skeleton: células `bento-hero/tall/wide/banner` con `app-skeleton-block` dentro (sin nested grid). (2) Error: `bento-banner` con error card. (3) Vista principal: `app-section-hero` (full-width), luego bento items directos: `bento-tall` Info Personal, 2×`bento-wide` progreso práctico/teórico, `bento-banner` inasistencias, `bento-hero` ficha técnica, `bento-tall` historial pagos. **Acciones hero — botón Certificado con 3 estados:** (a) `file-check` "Ver Certificado" si `certPdfPath` existe → llama `certFacade.verCertificado()`; (b) `file-plus` "Generar Certificado" si tiene 12/12 prácticas y sin PDF → muestra `ConfirmModalService` si teoría < 100% → llama `certFacade.generarCertificado()` + `facade.refresh()`; (c) `file-text` "Certificado (X/12)" deshabilitado si < 12 clases. **Acciones hero — carnet Clase B con 4 estados:** (a) "Generando..." (loading) durante generación; (b) `puedeActualizarCarnet()` true (carnet existe + sesiones 7-12 agendadas) → dos botones: `eye` "Ver Carnet" + `refresh-cw` "Actualizar Carnet" → `handleActualizarCarnet()` → `facade.generarCarnet()` regenera PDF con 12 clases (Edge Function usa `sesiones.length > 6 → totalClases=12`, `upsert:true`); (c) carnet existe sin upgrade → `id-card` "Ver Carnet"; (d) sin carnet → `id-card` "Generar Carnet". **Banner `necesitaAgendarSegundaEtapa`** (computed: `licensePdfPath` set + completadas≥6 + ninguna sesión 7-12 agendada): `bento-banner` info azul con icono `calendar-plus`, texto explicativo y botón "Ir a Agenda" → `navigateToAgenda()` → `/app/admin/agenda`. `(reprogramarRequested)` → `openReprogramarDrawer()` → `facade.setReprogramarTarget()` + `layoutDrawer.open(AdminReprogramarClaseDrawerComponent)`. **Grilla "Clases Prácticas" (RF-053):** 5 estados visuales mutuamente excluyentes por celda, en este orden de prioridad — verde/check (`completada`), rojo/x (`ausente`, `status='no_show'` — subtítulo dinámico: "Inasistencia — Justificada" si `clase.justificada`, si no "Inasistencia"; **el color se mantiene rojo aun justificada**, solo cambia el texto), **ámbar/ban (`cancelada`, `status='cancelled'` — pendiente de reagendar; bug corregido: antes caía en el bucket azul porque `cancelled` conserva `scheduled_at`)**, azul/calendar-clock (agendada futura, tiene `fecha` sin completar/ausente/cancelada), gris/clock (sin agendar). **Banner "Inasistencias Registradas" bifurcado por `licenseGroup`:** Clase B lee `facade.inasistenciasClaseB()` (automático, RF-053 — sin botón "Registrar Nueva"; fila justificada muestra badge verde "Justificado" + caption `Motivo: {{justificacion}}` truncado con `pTooltip` (texto completo al hover); fila no justificada muestra botón "Justificar" → modal inline (`justificarClaseBOpen` + textarea) → `facade.justificarInasistenciaClaseB(id, motivo)`); Profesional mantiene el flujo previo sin cambios (`facade.inasistencias()` / `absence_evidence` + botón "Registrar Nueva" → `AdminInasistenciaDrawerComponent`). **Acción hero "Reagendar Clases" (RF-053):** visible solo si `licenseGroup==='class_b' && facade.puedeReagendarPenalizacion()` (hay `class_b_sessions` en `status IN ('cancelled', 'no_show')` — el alumno debe recuperar tanto lo cancelado por la penalización como las inasistencias que la gatillaron); label incluye el conteo (`Reagendar Clases (N)` vía `facade.clasesPendientesReagendarCount()`), icono `calendar-plus` → `openReagendarClasesDrawer()` → `layoutDrawer.open(AdminReagendarClasesDrawerComponent)`. | `AdminAlumnoDetalleFacade`, `CertificacionClaseBFacade`, `ConfirmModalService` | `features/admin/alumno-detalle/admin-alumno-detalle.component.ts` | ✅ Estable |
| `AdminInasistenciaDrawerComponent` | Smart / Drawer — Registrar inasistencia | `isOpen` (boolean, req) — outputs: `closed`, `saved`. Inyecta `AdminAlumnoDetalleFacade` para leer `alumno().enrollmentId` y llamar `insertAbsenceEvidence()`. Formulario reactivo: `document_date` (date, req), `document_type` (select, req), `description` (textarea), file input simulado. Valida con `markAllAsTouched()`. Resetea y emite `saved`+`closed` al guardar con éxito. | `AdminAlumnoDetalleFacade` | `features/admin/alumno-detalle/inasistencia-drawer/admin-inasistencia-drawer.component.ts` | ✅ Estable |
| `AdminEditarPerfilDrawerComponent` | Smart / Drawer — Editar perfil del alumno | `isOpen` (boolean, req) — outputs: `closed`, `saved`. `effect()` hace `patchValue` desde `facade.alumno()` al abrir. Formulario: `first_names` (req), `paternal_last_name` (req), `maternal_last_name`, `email` (req + email validator), `phone`. Llama `facade.actualizarPerfilAlumno(userId, data)` → `.update()` en tabla `users`. Muestra banner éxito inline y auto-cierra tras 1.2s; emite `saved` para que el padre recargue la ficha. | `AdminAlumnoDetalleFacade` | `features/admin/alumno-detalle/editar-perfil-drawer/admin-editar-perfil-drawer.component.ts` | ✅ Estable |
| `AdminReprogramarClaseDrawerComponent` | Smart / Drawer — Reprogramar clase práctica desde la Ficha Técnica. Abierto vía `LayoutDrawerFacadeService.open()` con icono `calendar-clock`. `ngOnInit` → `facade.loadInstructores()`. **Step 1:** cards de instructor (igual que wizard de matrícula paso 2); al seleccionar → `facade.loadScheduleGrid(id)`, resetea slot/semana/día. **Step 2:** navegación de semanas (`prevWeek/nextWeek`), tabs de días, grid 2-col de slots disponibles/ocupados, leyenda. `onConfirm()` → `facade.reprogramarClase({sessionId,enrollmentId,claseNumero,instructorId,scheduledAt})` → `layoutDrawer.close()`. `canConfirm` computed: instructor + slot + !isSaving. | `AdminAlumnoDetalleFacade`, `LayoutDrawerFacadeService` | `features/admin/alumno-detalle/reprogramar-clase-drawer/admin-reprogramar-clase-drawer.component.ts` | ✅ Estable |
| `AdminReagendarClasesDrawerComponent` | Smart / Drawer — **RF-053, Paso 1 de 2 (solo selección).** Checklist de clases pendientes tras penalización: `class_b_sessions.status='cancelled'` **y** `status='no_show'` (las inasistencias que la gatillaron), fuente `facade.clasesPendientesReagendar()`. Abierto vía hero action `reagendar-clases` (solo visible si `facade.puedeReagendarPenalizacion()`, icono `calendar-plus`). **3er rediseño:** ya NO tiene `p-select`/`p-datepicker` por fila (2ª iteración — resultó lenta/con calendarios superpuestos para reagendar 10 clases). Ahora es SOLO checkbox + info estática (N° de clase, fecha original, badge rojo "Inasistencia"/ámbar "Cancelada") — **todas pre-marcadas por defecto** (`selected: true` al mapear en `ngOnInit`), la secretaria desmarca lo que NO quiera reagendar aún. Botón footer "Seleccionar Horarios (N)" (deshabilitado si 0 marcadas) → `facade.setReagendarSeleccion(selectedRows)` + `layoutDrawer.push(AdminReagendarHorariosDrawerComponent, ...)` — `push` (no `open`/`close`) navega dentro del mismo drawer sin perder contexto y habilita "Volver" al checklist. **UI compacta (4º ajuste):** lista de clases en `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2` (antes `flex-col` — 12 filas apiladas obligaban a scroll); cards con `p-2.5` (antes `p-4`), fecha en `text-[10px]` sin prefijo. `badgeLabel(row)`: si `origen==='no_show'` y `row.justificada` → `"Inasistencia — Justificada"` (el badge NO tenía antes forma de saber esto — `ClasePendienteReagendarUI.justificada` se agregó reusando el mismo mapa `attendanceBySessionId` de `ClasePracticaUI.justificada`); sigue en rojo (`data-origen="no_show"` sin cambios), solo cambia el texto. | `AdminAlumnoDetalleFacade`, `LayoutDrawerFacadeService` | `features/admin/alumno-detalle/reagendar-clases-drawer/admin-reagendar-clases-drawer.component.ts` | ✅ Estable |
| `AdminReagendarHorariosDrawerComponent` | Smart / Drawer — **RF-053, Paso 2 de 2 (agendamiento masivo).** Reutiliza **exactamente** `app-assignment-step` + `app-schedule-grid` (mismo componente del Step 2 del wizard de Matrícula) con `hidePaymentMode=true` y `nextLabel="Guardar Reagendamiento"` — un solo instructor + grilla de disponibilidad para agendar TODAS las clases seleccionadas en el Paso 1 de una vez (sin inputs por fila). `assignmentData` computed arma el `EnrollmentAssignmentData` con `slotSelection.requiredCount = facade.reagendarSeleccion().length` (exige exactamente esa cantidad). `onDataChange()` dispara `facade.loadScheduleGrid(instructorId)` al cambiar de instructor. `onSave()` → `facade.reagendarClasesPenalizadas({enrollmentId, instructorId, selectedSlotIds})` → `layoutDrawer.close()`. `(back)` → `layoutDrawer.back()` (vuelve al checklist del Paso 1, no cierra). **Nota de nomenclatura PrimeNG:** el proyecto usa `p-select`/`p-datepicker` (PrimeNG v21) — `<p-dropdown>`/`<p-calendar>` son selectores de v14 y anteriores, ya NO existen en `node_modules/primeng` (su `exports` map no tiene `./dropdown` ni `./calendar`) y romperían el build. | `AdminAlumnoDetalleFacade`, `LayoutDrawerFacadeService` | `features/admin/alumno-detalle/reagendar-clases-drawer/admin-reagendar-horarios-drawer.component.ts` | ✅ Estable |
| `/app/admin/agenda` | **Agenda Semanal** — Smart wrapper de `app-agenda-semanal`. Inyecta `AgendaFacade` + `LayoutDrawerFacadeService`. `ngOnInit` llama `initialize()`. Al clicar slot disponible sin filtro de instructor activo, calcula `slotsAtTime` (todos los slots disponibles del mismo día+hora) y los pasa a `setSelectedSlot()` para que el drawer muestre el selector de instructor. | `AgendaFacade`, `LayoutDrawerFacadeService` | `features/admin/agenda/admin-agenda.component.ts` | ✅ UI Ready |
| `/app/admin/asistencia` — `AdminAsistenciaComponent` | **Control de Asistencia Clase B (Admin)** — Reactivo a `BranchFacade.selectedBranchId()` via `effect()` → llama `facade.setBranchFilter()` + `reload()` al cambiar sede. Reutiliza `AsistenciaClaseBContentComponent`. Abre `AsistenciaTeoriaDrawerComponent` (ver lista), `AgendarTeoriaDrawerComponent` (agendar nueva clase), `AdminIniciarClaseDrawerComponent` (iniciar práctica), `AdminFinalizarClaseDrawerComponent` (finalizar práctica). **Datos reales de Supabase.** | `AsistenciaClaseBFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/admin/asistencia/admin-asistencia.component.ts` | ✅ Estable |
| `AgendarTeoriaDrawerComponent` | **Drawer Smart — Agendar nueva clase teórica.** Selector de sede (admin sin sede fija), fecha, hora inicio/fin (con validación visual: borde rojo + mensaje si fin ≤ inicio), tema, enlace Zoom (opcional), lista de alumnos con matrículas activas + checkboxes (marcar todos). Botón cambia de `"Agendar Clase"` a `"Agendar clase y enviar enlace"` cuando hay zoom + alumnos. Spinner `loader-circle animate-spin` y botón deshabilitado durante `isSaving()`. Banner de resultado inline al guardar (verde/amarillo según errores de envío). Crea `class_b_theory_sessions` + `class_b_theory_attendance`. | `AsistenciaClaseBFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/admin/asistencia/agendar-teoria-drawer.component.ts` | ✅ Estable |
| `AsistenciaTeoriaDrawerComponent` | **Drawer Smart — Ver/editar clase teórica.** Campos editables: tema, hora inicio, hora fin (con validación visual fin ≤ inicio), botón `"Guardar cambios"` visible solo si `infoChanged()` → llama `facade.updateTeoriaInfo()`. Campo enlace Zoom con botón `"Guardar"` / `"Guardar y enviar"` (spinner `loader-circle animate-spin` + deshabilitado durante `isSaving()`). Banner resultado Zoom: `"Enlace guardado · Correos enviados a N alumnos"`. Lista de alumnos con botones de estado (Presente/Ausente/Justificado) + campo justificación. Modo solo-lectura si `isFutureDate()`. Guarda asistencia vía `facade.saveTeoriaAsistencia()`. | `AsistenciaClaseBFacade`, `LayoutDrawerFacadeService` | `features/admin/asistencia/asistencia-teoria-drawer.component.ts` | ✅ Estable |
| `AdminIniciarClaseDrawerComponent` | **Drawer Smart — Iniciar clase práctica (admin/secretaria).** Lee `facade.selectedPractica()`. Ticket resumen con clase N°, alumno, instructor, patente + marca/modelo reactivos al vehículo seleccionado. **Selector de vehículo** (dropdown con vehículos de la sede vía `facade.loadVehiclesByBranch()`; pre-selecciona el vehículo de la sesión). Al cambiar vehículo, el odómetro se actualiza con `vehicles.current_km` del nuevo vehículo. Llama `facade.startClass(sessionId, kmStart, vehicleId?)` — pasa `vehicleId` solo si difiere del original. **Tras éxito:** abre automáticamente `AdminFinalizarClaseDrawerComponent` (via `layoutDrawer.open()`) en lugar de cerrar, para que el admin finalice la clase de inmediato con el estado ya actualizado en `_selectedPractica`. | `AsistenciaClaseBFacade`, `LayoutDrawerFacadeService` | `features/admin/asistencia/admin-iniciar-clase-drawer.component.ts` | ✅ Estable |
| `AdminFinalizarClaseDrawerComponent` | **Drawer Smart — Finalizar clase práctica (admin/secretaria).** Lee `facade.selectedPractica()`. Ticket resumen + km final (con validación > km inicial; muestra km de salida como referencia vía `DecimalPipe` cuando `kmStart !== null`) + calificación 1–5 (cursor-pointer) + checklist `EvaluationChecklistComponent` (cursor-pointer en ítems) + observaciones + firmas opcionales (`SignaturePadComponent` x2, colapsables — botón "Mostrar/Ocultar" con cursor-pointer). Llama `facade.finishClass(FinishClassPayload)` → registra asistencia `present` + cierra drawer. | `AsistenciaClaseBFacade`, `LayoutDrawerFacadeService` | `features/admin/asistencia/admin-finalizar-clase-drawer.component.ts` | ✅ Estable |
| `/app/admin/matricula` | **Wizard 6 Pasos (Nueva Matrícula)** — Thin wrapper que reutiliza `SecretariaMatriculaComponent`. Pasos: Datos Personales, Adscripción, Documentos, Pago/Descuentos, Contrato (RF-083), Confirmación. | — | `features/admin/matricula/admin-matricula.component.ts` | ✅ UI Ready |
| `/app/admin/pagos` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout #bentoGrid]`, hero full-width. 4 KPIs como `bento-square` individuales: Ingresos Hoy/Mes (K/M + subValue CLP), Pagos Pendientes (warning), Boletas Emitidas. `bento-banner` con `flex flex-col gap-6` para: tabla "Alumnos con deuda" (6 cols, skeleton inline, empty state), grid `lg:grid-cols-12` Pagos Recientes (col-8, tabla 7 cols, paginación 5/página) + sidebar Métodos de Pago (col-4, barras %). Error state al final. GSAP: `animateHero` + `animateBentoGrid` sobre `#bentoGrid`. CSS `.force-compact` para modo drawer-abierto. | `PagosFacade`, `LayoutDrawerFacadeService` | `features/admin/pagos/admin-pagos.component.ts` | ✅ Estable |
| `AdminPagoDetalleDrawerComponent` | Smart / Drawer — Estado de Cuenta por Matrícula | Cargado via `LayoutDrawerFacadeService.open()`. Lee `facade.enrollmentSeleccionado()` en `ngOnInit` y llama `facade.cargarEstadoCuenta(id)`. **Secciones:** (1) Ficha alumno: nombre, RUT, email, teléfono, curso, badge `paymentStatus`. (2) 3 KPI mini-cards: Total Curso (tachado si descuento), Pagado (verde + % del total), Saldo (warning/success según valor). (3) Botón "+ Registrar Pago" solo si `saldoPendiente > 0` → abre `RegistrarPagoDrawerComponent` vía `layoutDrawer.push()`. (4) Tabla historial de pagos: columnas Fecha/Concepto/Método/N°Doc/Monto/Estado + fila total. Sincronización automática vía Facade. Skeleton de 3 fases (ficha + KPIs + historial). | `PagosFacade` | `features/admin/pagos/admin-pago-detalle-drawer.component.ts` | ✅ Estable |
| `RentabilidadCursosComponent` | Presentacional / Reporte RF-040 — Tabla "Rentabilidad Estimada por Tipo de Curso". Sin inyección de facade. `datosRentabilidad` signal con mock data (4 cursos). `totales` computed: suma ingresos/gastos/margen + rentabilidad promedio. `mesActual` computed. Barra de progreso coloreada con `[style.width.%]` y `[style.background]`. Badge rentabilidad. Nota al pie con fondo atenuado. Integrado en `app-reportes-contables-content` (último bloque `bento-banner`). | — | `shared/components/rentabilidad-cursos/rentabilidad-cursos.component.ts` | ✅ UI Ready (mock) |
| `RegistrarPagoDrawerComponent` | Smart / Drawer — Registrar Pago (Dual Mode) | **Modo Contextual** (`enrollmentId !== null`): pre-muestra alumno info card (nombre + saldo). **Modo Global** (`enrollmentId === null`): selector `<select>` de `facade.alumnosConDeuda()`; info card reactiva del alumno seleccionado via getter `selectedAlumno`. `enrollment_id` FormControl con `Validators.required` solo cuando hay deudores en modo global. `sumMatchesTotalValidator` a nivel FormGroup valida que suma canales = total. Balance card live (getter, no signal). `onSubmit()` resuelve `enrollmentId` y `montosActuales` según modo. `resetForm()` ajusta validators según modo al abrir. Navegación vía `LayoutDrawerFacadeService.back()`. | `PagosFacade`, `ToastService` | `features/admin/pagos/registrar-pago-drawer.component.ts` | ✅ Estable |
| `/app/admin/contabilidad/reportes` — `AdminContabilidadReportesComponent` | **Reportes Contables RF-030/RF-031 (Admin)** — Thin Smart wrapper. `ngOnInit` llama `facade.initialize()` (SWR). Reutiliza `app-reportes-contables-content`. Escucha outputs `(aplicarFiltros)`, `(exportRequested)`, `(registrarGastoClick)` → abre `RegistrarGastoFijoDrawerComponent` vía `LayoutDrawerFacadeService.open()`, `(verDetalle)`. | `ReportesContablesFacade`, `LayoutDrawerFacadeService` | `features/admin/contabilidad-reportes/admin-contabilidad-reportes.component.ts` | ✅ Estable |
| `/app/admin/contabilidad/cuadratura` — `AdminContabilidadCuadraturaComponent` | **Cuadratura Diaria (Admin)** — Gestión de ingresos/egresos hoy y cierre de caja físico con arqueo de billetes/monedas. | `CuadraturaFacade` | `features/admin/contabilidad-cuadratura/admin-contabilidad-cuadratura.component.ts` | ✅ Estable |
| `/app/admin/contabilidad/historial` — `AdminContabilidadHistorialCuadraturasComponent` | **Historial de Cuadraturas (Admin)** — Calendario mensual de cierres de caja con visualización de arqueos históricos y exportación. | `HistorialCuadraturasFacade` | `features/admin/contabilidad-historial-cuadraturas/admin-contabilidad-historial-cuadraturas.component.ts` | ✅ Estable |
| `/app/admin/contabilidad/anticipos` | Stub PLANO | — | `features/admin/contabilidad-anticipos/admin-contabilidad-anticipos.component.ts` | 🚧 Stub |
134: | `/app/admin/contabilidad/cursos` — `AdminContabilidadCursosComponent` | **Cursos Singulares RF-035** — Inyecta `CursosSingularesFacade`, `BranchFacade` + `LayoutDrawerFacadeService`. **Reactividad de sede (fix-016):** `effect()` en constructor trackea `branchFacade.selectedBranchId()` → `facade.initialize()` (el facade detecta cambio via `_lastBranchId` y recarga con skeleton). **Secciones:** (1) `app-section-hero` compact con `contextLine="Contabilidad"`. (2) Bento **4 KPIs** (`app-kpi-card-variant`): Cursos Activos, Total Inscritos, **Ingresos Cobrados** (real, success) y **Por Cobrar** (warning si > 0). (3) Tabla HTML con filtros tipo/estado via `computed()`, columna **"Cobrado"** (real + sub-línea "$X pendiente" warning), footer con `totales().cobrado`. Hovers de fila/acciones via clases CSS scoped (`.curso-row`, `.accion-btn--*` con `color-mix` de tokens — sin `setAttribute` inline). Acciones por fila: `onVerDetalle()` / `onRegistrarCobro()` → drawers. (4) Hero action "Nuevo Curso" → `AdminCursoSingularCrearDrawerComponent`. GSAP `animateBentoGrid()`. | `CursosSingularesFacade`, `BranchFacade`, `LayoutDrawerFacadeService`, `GsapAnimationsService` | `features/admin/contabilidad-cursos/admin-contabilidad-cursos.component.ts` | ✅ Estable (fix-016) |
135: | `AdminCursoSingularDetalleDrawerComponent` | Smart / Drawer — Detalle Curso Singular RF-035 | Cargado via `LayoutDrawerFacadeService`. Lee `facade.selectedCurso()`. **Secciones:** (1) Ficha `card-tinted`: nombre, tipo badge, fecha inicio con icono, grid 4 datos. (2) 3 KPI mini estandarizados (`app-kpi-card`): Inscritos/Cupos, Ingreso Est. (success), Cupos libres. (3) Lista inscriptos con badge de `paymentStatus` coloreado con `color-mix`. | `CursosSingularesFacade` | `features/admin/contabilidad-cursos/admin-curso-singular-detalle-drawer.component.ts` | ✅ Estable |
136: | `AdminCursoSingularCobroDrawerComponent` | Smart / Drawer — Registrar Cobro RF-035 | Cargado via `LayoutDrawerFacadeService`. Lee `facade.selectedCurso()`. **Secciones:** (1) Resumen `card-tinted` horizontal: nombre, precio, facturación, badge compacto. (2) 3 KPI mini estandarizados (`app-kpi-card`): Pendientes (warning), Cobrados (success), Total (success). (3) Lista de cobros con botón "Marcar cobrado" → `facade.marcarEnrollmentPagado()`. | `CursosSingularesFacade` | `features/admin/contabilidad-cursos/admin-curso-singular-cobro-drawer.component.ts` | ✅ Estable |
| `/app/admin/contabilidad/anticipos` — `AdminContabilidadAnticiposComponent` | **Anticipos a Instructores RF-038** — Smart page. `ngOnInit` → `facade.initialize()` (SWR). `ngAfterViewInit` → `gsap.animateBentoGrid()`. **Hero:** `app-section-hero` con acción "Registrar Anticipo" → abre `RegistrarAnticipoDrawerComponent`. **3 KPI cards custom:** Pendientes (warning border-left), Total Histórico (info), Ya Descontados (success full-border). **Tabla "Cuenta Corriente por Instructor":** columnas INSTRUCTOR\|TIPO (badge)\|ANTICIPOS TOTALES\|SALDO PENDIENTE (naranja si>0, verde si=0)\|ÚLTIMO ANTICIPO (brand color)\|ESTADO (badge Saldo pendiente/Al día)\|ACCIONES (file-text + check). **Tabla "Historial de Anticipos":** columnas FECHA\|INSTRUCTOR\|MOTIVO\|MONTO (warning)\|ESTADO (badge Pendiente/Descontado). Footer con nota de regla de negocio. Skeleton 5 filas en ambas tablas. Formatter CLP via `Intl.NumberFormat`. | `AnticiosFacade`, `LayoutDrawerFacadeService`, `GsapAnimationsService` | `features/admin/contabilidad-anticipos/admin-contabilidad-anticipos.component.ts` | ✅ Estable |
| `RegistrarAnticipoDrawerComponent` | Smart / Drawer — Registrar Anticipo a Instructor (RF-038) | Sin inputs. Renderizado vía `LayoutDrawerFacadeService.open()`. Inyecta `AnticiosFacade`. Formulario: instructor (select dinámico desde `facade.instructores()`), fecha (date, default hoy), monto (number, req min 1), motivo (select: salary/allowance/materials/other), descripción (textarea opcional). Cierra vía `layoutDrawer.close()` al guardar con éxito. `data-llm-action` en botones. | `AnticiosFacade`, `LayoutDrawerFacadeService` | `features/admin/contabilidad-anticipos/registrar-anticipo-drawer.component.ts` | ✅ Estable |
135: | `AdminCursoSingularDetalleDrawerComponent` | Smart / Drawer — Detalle Curso Singular RF-035 | Cargado via `LayoutDrawerFacadeService`. Lee `facade.selectedCurso()`. **Secciones:** (1) Ficha `card-tinted`: nombre, tipo badge, fecha inicio con icono, grid 4 datos. (2) 3 KPI mini estandarizados (`app-stat-box`): Inscritos/Cupos, **Cobrado** (`ingresoCobrado` real, success si > 0 — fix-016), Cupos libres. (3) Acciones de estado — visible solo si `active | upcoming`: botones "Marcar como finalizado" y "Cancelar curso" con confirmación inline de 2 pasos (`confirmingAction` signal `'completed'|'cancelled'|null`); llama `facade.marcarCursoFinalizado()` o `facade.cancelarCurso()`. (4) Lista inscriptos con badge de `paymentStatus` coloreado con `color-mix`. | `CursosSingularesFacade` | `features/admin/contabilidad-cursos/admin-curso-singular-detalle-drawer.component.ts` | ✅ Estable |
136: | `AdminCursoSingularCobroDrawerComponent` | Smart / Drawer — Registrar Cobro RF-035 | Cargado via `LayoutDrawerFacadeService`. Lee `facade.selectedCurso()`. **Secciones:** (1) Resumen `card-tinted` horizontal: nombre, precio, facturación, badge compacto. (2) 3 KPI mini estandarizados (`app-kpi-card`): Pendientes (warning), Cobrados (success), Total (success). (3) Lista de cobros con botón "Marcar cobrado" → `facade.marcarEnrollmentPagado()` (cobra `precio − descuento` y sella `paid_at` — fix-016) que refresca `inscriptos` inmediatamente. **fix-016:** `totalCobrado` = Σ `montoPagado` real (ya no `cobrados × precio`); filas pendientes muestran "A cobrar: $X" y línea de descuento (monto + motivo) si existe. **Spinner local:** `_guardandoId` es `signal<number|null>` (no variable de clase) para que el `computed()` lo rastree reactivamente y muestre el loader en el botón correcto. | `CursosSingularesFacade` | `features/admin/contabilidad-cursos/admin-curso-singular-cobro-drawer.component.ts` | ✅ Estable (fix-016) |
| `AdminCursoSingularInscribirDrawerComponent` | Smart / Drawer — Inscribir Alumno en Curso Singular RF-035 | Abierto via `LayoutDrawerFacadeService.push()` desde `AdminCursoSingularDetalleDrawerComponent` (botón visible solo cuando hay cupos). **Wizard 2 pasos internos — escrituras DB diferidas al Paso 2:** (1) Datos del alumno: helper text explica que la búsqueda por RUT **pre-carga datos si el alumno ya existe y que un alumno nuevo se registra en el mismo formulario** (fix-015 AC1). Input RUT con validación en tiempo real (border error/success), botón "Buscar" → `facade.searchByRut()` → pre-llena el formulario **COMPLETO** (nombres, apellidos, email, teléfono, **fecha nacimiento, género, dirección** — fix-015 AC3); banner "Alumno nuevo" si el RUT no existe. `onRutChange()` resetea wizard+formulario si había búsqueda previa. **Fecha de nacimiento requerida** con validación inline edad ≥ 17 (`calcAge` de `age.utils`, espejo del CHECK de BD — fix-015 AC4); `isStep1Valid` la exige. "Siguiente" → `facade.savePersonalData()` (LOCAL ONLY, sin BD). (2) Pago: card resumen con `.surface-hero` (sin colores hardcodeados), precio base y total efectivo. Descuento oculto cuando el método es `'pendiente'`. Grid 2×2 de 4 métodos; `pendiente` → `amount_paid=0`, `payment_status='pending'`; otros → `payment_status='paid'`. "Confirmar" → `facade.inscribirAlumno()` → toast + `layoutDrawer.back()`. **fix-016:** identidad del alumno existente bloqueada (`esAlumnoExistente` computed → `[disabled]` en nombres/apellidos/nacimiento/género; email/teléfono/dirección editables); descuento visible con cualquier método (incluso "pendiente" — se persiste y el cobro posterior es `precio − descuento`); total muestra "Total por cobrar" cuando queda pendiente. | `CursosSingularesFacade`, `AuthFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/admin/contabilidad-cursos/admin-curso-singular-inscribir-drawer.component.ts` | ✅ Estable (fix-016) |
| `AdminCursoSingularCrearDrawerComponent` | Smart / Drawer — Nuevo Curso Singular RF-035 | Renderizado vía `LayoutDrawerFacadeService.open()` desde el hero de `AdminContabilidadCursosComponent`. ReactiveForm: nombre (req, min 3), tipo (p-select SENCE/Particular), facturación (p-select), precio (req, min 0), duración (req, min 1), cupos (req, min 1), fecha inicio (req), **sede (p-select req — fix-016 AC4):** visible solo para admin (opciones de `branchFacade.branches()`, default = sede activa del topbar; en "Todas las sedes" obliga a elegir); secretarias quedan ancladas a su `user.branchId` sin selector. Submit → `facade.crearCurso()` (persiste `branch_id` + `registered_by`) → cierra drawer. | `CursosSingularesFacade`, `AuthFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/admin/contabilidad-cursos/admin-curso-singular-crear-drawer.component.ts` | ✅ Estable (fix-016) |
| `/app/admin/contabilidad/liquidaciones` — `AdminContabilidadLiquidacionesComponent` | **Liquidaciones de Instructores (Admin)** — Thin Smart wrapper con `page-wide`. `ngOnInit` llama `facade.initialize()`. Reutiliza `app-liquidaciones-content` (organismo full-page: no usa bento-grid en el wrapper externo ya que el organismo gestiona su propio layout interno con KPIs uniformes `kpi-card-variant` + tabla). | `LiquidacionesFacade` | `features/admin/contabilidad-liquidaciones/admin-contabilidad-liquidaciones.component.ts` | ✅ Estable |
| `/app/admin/flota` — `AdminFlotaComponent` | **Gestión de Flota** — Smart page principal. Inicializa `FlotaFacade` (SWR). Reutiliza `app-flota-list-content`. Coordina drawers: `app-vehicle-form-drawer` (nuevo/editar), `app-vehicle-agenda-drawer` (agenda diaria). Navega a subrutas de mantenimientos e impresión. | `FlotaFacade` | `features/admin/flota/admin-flota.component.ts` | ✅ UI Ready |
| `/app/admin/flota/:id/mantenimientos` — `VehicleMaintenancesComponent` | **Historial de Mantenimientos** — Smart page. Breadcrumb de retorno a flota. KPIs (total, gasto total, promedio, km). Tabla PrimeNG historial + tarjetas de documentos/programados. Drawer `app-maintenance-form-drawer`. | `FlotaDetalleFacade` | `features/admin/flota/vehicle-maintenances/vehicle-maintenances.component.ts` | ✅ UI Ready |
| `/app/admin/flota/hoja-de-ruta/:id` — `RouteSheetComponent` | **Hoja de Ruta Imprimible** — Página standalone A4 con `@media print`. Auto-imprime al cargar. Grilla 08:00–18:00 con 11 filas. Botones \"Imprimir\" / \"Cerrar\" visuales (ocultos al imprimir). | `FlotaFacade` | `features/admin/flota/route-sheet/route-sheet.component.ts` | ✅ UI Ready |
| `app-vehicle-form-drawer` | Smart / Drawer — Nuevo/Editar Vehículo | `isOpen` (req), `vehicleId` (null=nuevo) — outputs: `closed`, `saved`. `effect()` pre-carga datos en edición. Campos: patente, marca, modelo, año, KM, estado. | `FlotaFacade` | `features/admin/flota/vehicle-form-drawer/vehicle-form-drawer.component.ts` | ✅ Estable |
| `app-vehicle-agenda-drawer` | Smart / Drawer — Agenda Diaria | `isOpen` (req), `vehicleId` — output: `closed`. Timeline vertical 08:00–18:00. Selector de fecha p-datepicker. Slots: clase (rojo), mantenimiento (amarillo), disponible (verde), vacío. | `FlotaFacade` | `features/admin/flota/vehicle-agenda-drawer/vehicle-agenda-drawer.component.ts` | ✅ Estable |
| `app-maintenance-form-drawer` | Smart / Drawer — Registrar Mantenimiento | `isOpen` (req), `vehicleId` — outputs: `closed`, `saved`. Campos: tipo, KM, costo CLP, taller, fecha realización, fecha próximo servicio, observaciones. | `FlotaDetalleFacade` | `features/admin/flota/maintenance-form-drawer/maintenance-form-drawer.component.ts` | ✅ Estable |
| `app-vehicle-documents-drawer` | Smart / Drawer — Documentos del Vehículo | `isOpen` (req), `vehicleId` — outputs: `closed`, `saved`. Gestiona los 4 docs: SOAP, Rev. Técnica, Permiso Circ., Seguro. Upsert en `vehicle_documents`. Muestra estado actual de docs con badges de color. | `FlotaFacade` | `features/admin/flota/vehicle-documents-drawer/vehicle-documents-drawer.component.ts` | ✅ Estable |
| `app-dms-upload-drawer` | Smart / Drawer — Subir Documento DMS | `isOpen` (req), `uploadMode` ('student'\|'school'), `preselectedStudentId` (number\|null) — outputs: `closed`, `saved`. Drag & drop, validación file type/size. Selector de alumno reactivo si mode=student. | `features/admin/documentos/dms-upload-drawer/dms-upload-drawer.component.ts` | ✅ Estable |
| `app-dms-template-drawer` | Smart / Drawer — Nueva Plantilla DMS | `isOpen` (req) — outputs: `closed`, `saved`. Admin-only. Carga plantillas con nombre, categoría, descripción. Detección automática de formato (PDF, DOCX, etc). | `features/admin/documentos/dms-template-drawer/dms-template-drawer.component.ts` | ✅ Estable |
| `/app/admin/instructores` — `AdminInstructoresComponent` | **Gestión de Instructores (Admin)** — Inyecta `InstructoresFacade`. Header con botón "+ Nuevo Instructor". 3 KPIs (total, activos, inactivos). Búsqueda por nombre/email/RUT + filtros (todos, activos, licencias por vencer) con `computed()` client-side. Tabla con avatar (iniciales), nombre, tipo badge, licencia clase+estado, clases activas, estado. Botones acción: ojo (→ ver drawer) y lápiz (→ editar drawer). Paginación client-side (10/página). Aloja 3 `app-drawer`: **Crear**, **Ver** y **Editar** instructor. `switchToEditar()` transiciona de Ver→Editar. | `InstructoresFacade` | `features/admin/instructores/admin-instructores.component.ts` | ✅ Estable |
| `AdminInstructorCrearDrawerComponent` | Smart / Drawer — Nuevo Instructor | Sin inputs. Output: `closed` (void). Formulario 3 secciones: Personal (nombre, RUT, email, teléfono), Licencia (clase p-select, vencimiento p-datepicker con preview estado), Asignación (tipo p-select, vehículo p-select con dots de estado). Parsea nombre completo en firstName/paternalLastName/maternalLastName. Llama `facade.crearInstructor()`. | `InstructoresFacade` | `features/admin/instructores/admin-instructor-crear-drawer.component.ts` | ✅ Estable |
| `AdminInstructorVerDrawerComponent` | Smart / Drawer — Ver Instructor | Sin inputs. Lee `facade.selectedInstructor()`. Avatar+initials, mini KPI cards (clases activas, licencia), info personal, info licencia, asignación actual, historial vehículos. Acciones rápidas: `editar()` → push Editar drawer, `verHorario()` → push Horario drawer. | `InstructoresFacade` | `features/admin/instructores/admin-instructor-ver-drawer.component.ts` | ✅ Estable |
| `AdminInstructorHorarioDrawerComponent` | Smart / Drawer — Horario Instructor | Sin inputs. Accedido vía `push()` desde Ver drawer. `ngOnInit` llama `facade.loadHorario(inst.id)`. Muestra sesiones `scheduled` futuras agrupadas por día (`computed dayGroups`). Encabezado de día con pill coloreado (hoy=brand, mañana=warning, resto=neutro). Cada sesión: hora grande + duración, avatar iniciales alumno, nombre alumno, patente vehículo, badge "Clase N°". Estado vacío con `calendar-check`. | `InstructoresFacade` | `features/admin/instructores/admin-instructor-horario-drawer.component.ts` | ✅ Estable |
| `AdminInstructorEditarDrawerComponent` | Smart / Drawer — Editar Instructor | Sin inputs. Output: `saved` (void). Lee `facade.selectedInstructor()` en `effect()` para pre-rellenar. Campos: nombre, email, teléfono, clase licencia, vencimiento, tipo, vehículo, estado (toggle danger zone). Detección de cambio de email. Historial de vehículos. Llama `facade.editarInstructor(id, payload)`. | `InstructoresFacade` | `features/admin/instructores/admin-instructor-editar-drawer.component.ts` | ✅ Estable |
| `AdminInstructorHorasDrawerComponent` | Smart / Drawer — Horas Trabajadas | Sin inputs. Tabla con `initials` avatar + nombre + clases impartidas + horas equiv. (×0.75 h) por instructor para el mes seleccionado. Navegación mes anterior/siguiente (deshabilitado si mes actual). Fila de totales en `tfoot`. Estado vacío con `calendar-x`. `ngOnInit` llama `facade.loadHorasMensuales()`. | `InstructoresFacade` | `features/admin/instructores/admin-instructor-horas-drawer.component.ts` | ✅ Estable |
| `/app/admin/certificacion` | Stub PLANO | — | `features/admin/certificacion/admin-certificacion.component.ts` | 🚧 Stub |
| `/app/admin/documentos` | **Repositorio Central (DMS)** — Smart page para admins. CRUD completo de documentos de alumnos, institucionales y plantillas. Coordina drawers y delegación a `DmsListContentComponent`. | `DmsFacade`, `ConfirmModalService` | `features/admin/documentos/admin-documentos.component.ts` | ✅ Estable |
| `/app/admin/documentos/alumnos/:id` | **Detalle Docs Alumno** — Lista completa de documentos de un alumno específico. Breadcrumb nav, upload dedicado, delete con confirmación. | `DmsFacade`, `AuthFacade` | `features/admin/documentos/alumno-docs-detalle/admin-alumno-docs-detalle.component.ts` | ✅ Estable |
| `/app/admin/usuarios` | Stub PLANO | — | `features/admin/usuarios/admin-usuarios.component.ts` | 🚧 Stub |
| `/app/admin/secretarias` — `AdminSecretariasComponent` | **Gestión de Secretarias (Admin)** — Layout canónico `.bento-grid` con skeleton states premium. Inyecta `SecretariasFacade`. Header con botón "+ Nueva Secretaria". 3 KPIs (`app-kpi-card-variant`) como celdas bento individuales. Búsqueda + filtros (sede, estado). Lista de secretarias en `bento-wide` y sidebar de permisos en `bento-tall`. Paginación client-side. CRUD vía drawers. | `SecretariasFacade` | `features/admin/secretarias/admin-secretarias.component.ts` | ✅ Estable |
| `AdminSecretariasCrearDrawerComponent` | Smart / Drawer — Nueva Secretaria | Sin inputs. Output: `closed` (void). Formulario: nombre, RUT (formatRut), email, teléfono, sede (p-select), toggle "Acceso a sedes" (grant multi-sede `canAccessBothBranches`, spec 0017). Llama `facade.crearSecretaria()`. Emite `closed` al cancelar o al crear con éxito. | `SecretariasFacade` | `features/admin/secretarias/admin-secretarias-crear-drawer.component.ts` | ✅ Estable |
| `AdminSecretariasVerDrawerComponent` | Smart / Drawer — Ver Secretaria | Sin inputs. Output: `editarClicked` (void). Lee `facade.selectedSecretaria()`. Muestra avatar, nombre, email, sede, estado, último acceso, alias público, acceso a sedes (stat-box read-only del grant `canAccessBothBranches`, spec 0017), panel de rol. Botón "Editar secretaria" emite `editarClicked`. | `SecretariasFacade` | `features/admin/secretarias/admin-secretarias-ver-drawer.component.ts` | ✅ Estable |
| `AdminSecretariasEditarDrawerComponent` | Smart / Drawer — Editar Secretaria | Sin inputs. Output: `saved` (void). Lee `facade.selectedSecretaria()` en `effect()` para pre-rellenar campos. Campos editables: nombre completo, sede (p-select), estado activo/inactivo (toggle), acceso a sedes (toggle del grant `canAccessBothBranches`, spec 0017; pre-rellena desde `sec.canAccessBothBranches`). Llama `facade.editarSecretaria(id, payload)`. Emite `saved` al guardar con éxito o al cancelar. | `SecretariasFacade` | `features/admin/secretarias/admin-secretarias-editar-drawer.component.ts` | ✅ Estable |
| `/app/admin/tareas` — `AdminTareasComponent` | **Tareas Multi-rol (Admin)** — `.bento-grid` + `[appBentoGridLayout]` + GSAP `animateBentoGrid()`. `constructor()`: `effect()` trackea `BranchFacade.selectedBranchId()` → `facade.initialize()` (branch-reactive). `ngOnInit`: `destroyRef.onDestroy(() => facade.dispose())`. **4 KPIs:** `pendingCount`, `overdueCount`, `sentTasks.length`, `receivedTasks.length`. **3 tabs:** "Asignadas por mí" (`sentTasks()`), "Dirigidas a mí" (`receivedTasks()`), "Observaciones" (`observationTasks()`). Skeleton 3 dummyTask cards mientras `isLoading()`. `app-empty-state` si vacío. Hero CTA "Nueva tarea" → `drawer.open(TaskCreateDrawerComponent)`. Click card → `facade.selectTask(id)` + `drawer.push(TaskDetailModalComponent)`. | `TasksFacade`, `BranchFacade`, `LayoutDrawerFacadeService`, `GsapAnimationsService` | `features/admin/tareas/admin-tareas.component.ts` | ✅ Estable |
| `/app/admin/libro-de-clases` | Smart — Libro de Clases Profesional (read-only Fase 1). `.bento-grid` + `[appBentoGridLayout]` + GSAP `animateBentoGrid()`. **Skeletons diferenciados:** `isLoading` → skeleton de selectores en filtros card; `isLoadingSections` → 7 filas slim bento-banner (`skeletonRows`/`skeletonWidths` con anchos variados). Filtros Promoción/Curso en `.bento-banner card`. `heroActions` computed: "Exportar PDF" en Hero solo con datos (sin Layout Shift). `app-empty-state` (icon `book-open`) sin curso seleccionado. Bulk "Expandir/Colapsar todo" vía `allSectionsOpen` computed + `toggleAllSections()`. Solo Cabecera abierta por defecto. **7 secciones colapsables** con `.section-toggle` (hover `color-mix` + `transition`) y `.section-meta` (conteo reactivo muted): cabecera `.card-accent` (SENCE/Horario vía `app-async-btn`), profesores (N módulos), lista alumnos (N alumnos), asistencia (leyenda P/A/J + N semanas + grilla L-S), calendario (N clases), evaluaciones (Escala MTT · Mín. 75), resumen (N alumnos + semáforo %). Branch-scoped (`setProfessionalOnly`). | `LibroDeClasesFacade`, `BranchFacade`, `GsapAnimationsService`, `BentoGridLayoutDirective`, `AsyncBtnComponent`, `EmptyStateComponent` | `features/admin/libro-de-clases/admin-libro-de-clases.component.ts` | ✅ Fase 1 |
| `/app/admin/psicotecnico` | Stub PLANO | — | `features/admin/psicotecnico/admin-psicotecnico.component.ts` | 🚧 Stub |
| `/app/admin/clase-profesional/relatores` — `AdminProfesionalRelatoresComponent` | **Gestión de Relatores (Admin/Secretaria)** — Inyecta `RelatoresFacade`. `app-section-hero` + botón "+ Nuevo Relator". 4 KPIs (`app-kpi-card-variant`): total, activos, especialidad A2, especialidad A3. Búsqueda por nombre/RUT + 2 filtros (especialidad, estado) con `computed()` client-side. Lista card-style con avatar (iniciales), nombre, badges especialidad coloreados, estado, email, RUT. Botones acción: ojo (→ `verDrawerOpen`) y lápiz (→ `editarDrawerOpen`). Paginación client-side (10/página). Sidebar: panel especialidades profesionales (A2/A3/A4/A5) + accesos rápidos. Aloja 3 `app-drawer`: **Crear**, **Ver** y **Editar** relator. `switchToEditar()` transiciona de Ver→Editar. Compartido con secretaria vía thin-wrapper. Implementa `OnDestroy`: `ngOnInit` llama `branchFacade.setProfessionalOnly(true)`, `ngOnDestroy` revierte a `false`. | `RelatoresFacade`, `BranchFacade` | `features/admin/profesional-relatores/admin-profesional-relatores.component.ts` | ✅ Estable |
| `AdminRelatorCrearDrawerComponent` | Smart / Drawer — Nuevo Relator | Sin inputs. Output: `closed` (void). 2 secciones: Personal (nombres, apellido paterno, materno, RUT con `formatRut`, email opcional, teléfono) + Especialidades (chips A2/A3/A4/A5 multi-select). Sin interacción con auth/Edge Functions — INSERT directo en `lecturers`. | `RelatoresFacade` | `features/admin/profesional-relatores/admin-relator-crear-drawer.component.ts` | ✅ Estable |
| `AdminRelatorVerDrawerComponent` | Smart / Drawer — Ver Relator | Sin inputs. Output: `editarClicked` (void). Lee `facade.selectedRelator()`. Muestra avatar, nombre, RUT, badges especialidades coloreados, estado, teléfono, email, lista de especialidades con descripción, fecha registro. Botón "Editar relator". | `RelatoresFacade` | `features/admin/profesional-relatores/admin-relator-ver-drawer.component.ts` | ✅ Estable |
| `AdminRelatorEditarDrawerComponent` | Smart / Drawer — Editar Relator | Sin inputs. Output: `saved` (void). `effect()` pre-rellena desde `facade.selectedRelator()`. Campos: nombres, apellido paterno/materno, email, teléfono, especialidades (chips). Toggle estado activo/inactivo (zona danger). UPDATE directo en `lecturers`. | `RelatoresFacade` | `features/admin/profesional-relatores/admin-relator-editar-drawer.component.ts` | ✅ Estable |
| `/app/admin/clase-profesional/promociones` — `AdminProfesionalPromocionesComponent` | **Gestión de Promociones (Admin/Secretaria)** — Inyecta `PromocionesFacade`. `app-section-hero` + botón "+ Nueva Promoción". 4 KPIs (`app-kpi-card-variant`): total, planificadas, en curso, finalizadas. Búsqueda por nombre/código + filtro estado con `computed()` client-side. Lista card-style con nombre, status badge, código mono, fechas, barra de progreso (en curso), badges de cursos A2/A3/A4/A5 con alumnos, mini-cards de relatores por curso con barras de enrollment. Paginación client-side (5/página). Aloja 3 `app-drawer`: **Crear** (selector de lunes cada 2 semanas, auto-cálculo fecha término, asignación multi-relator por curso), **Ver** (info general, alumnos por categoría, cursos con relatores y barras) y **Editar** (nombre + estado). `switchToEditar()` transiciona de Ver→Editar. Compartido con secretaria vía thin-wrapper. Implementa `OnDestroy`: `ngOnInit` llama `branchFacade.setProfessionalOnly(true)`, `ngOnDestroy` revierte a `false`. | `PromocionesFacade`, `BranchFacade` | `features/admin/profesional-promociones/admin-profesional-promociones.component.ts` | ✅ Estable |
| `AdminPromocionCrearDrawerComponent` | Smart / Drawer — Nueva Promoción | Sin inputs. Output: `closed` (void). 3 secciones: Info general (nombre auto-generado, código auto-generado desde fecha), Fecha inicio (grid de 8 lunes futuros, sugeridos cada 2 semanas con dot, fecha término auto-calculada: sábado 5ta semana), Cursos y relatores (4 cards A2/A3/A4/A5 con p-select multi-relator filtrado por especialización, chips removibles). Panel lateral "Reglas de negocio". INSERT en `professional_promotions` + `promotion_courses` + `promotion_course_lecturers`. | `PromocionesFacade` | `features/admin/profesional-promociones/admin-promocion-crear-drawer.component.ts` | ✅ Estable |
| `AdminPromocionVerDrawerComponent` | Smart / Drawer — Ver Promoción | Sin inputs. Output: `editarClicked` (void). Lee `facade.selectedPromocion()`. Header con nombre+status badge. Banner de progreso (en curso) o completada (finalizada). Info general grid (código, estado, fechas, duración, total alumnos). Alumnos por categoría (4 cards con valores grandes). Cursos detallados con relatores (avatar+initials, role labels), barras de enrollment y **lista expandible de alumnos** por curso (nombre, RUT, initials, badge de estado enrollment) con skeleton loading. Botón "Editar promoción". | `PromocionesFacade` | `features/admin/profesional-promociones/admin-promocion-ver-drawer.component.ts` | ✅ Estable |
| `AdminPromocionEditarDrawerComponent` | Smart / Drawer — Editar Promoción | Sin inputs. `effect()` pre-rellena `name`, `code` y `status` desde `facade.selectedPromocion()`. **Nombre y código editables** via inputs enlazados con getter/setter sobre signals. Fechas readonly. **Transiciones con reglas de negocio** via `computed()`: `availableStatusOptions` siempre incluye el estado actual como opción disabled (visible en gris) + transiciones válidas (`planned`→`in_progress` solo si `start_date ≤ hoy` + siempre `cancelled`; `in_progress`→`finished`\|`cancelled`; `finished`\|`cancelled` sin transiciones); `isTerminalStatus` muestra candado; `plannedButNotStarted` muestra aviso con fecha; `canSave` habilita si nombre/código cambiaron O si hay transición de estado válida. UPDATE en `professional_promotions` (`name`, `code`, `status`); trigger `trg_cascade_promotion_status` propaga status a `promotion_courses`. | `PromocionesFacade` | `features/admin/profesional-promociones/admin-promocion-editar-drawer.component.ts` | ✅ Estable |
| `/app/admin/clase-profesional/asistencia` — `AdminProfesionalAsistenciaComponent` | **Clases y Asistencia Profesional (Admin/Secretaria)** — Inyecta `AsistenciaProfesionalFacade`. `app-section-hero`. 2 filtros: Promoción (p-select, auto-selecciona primera activa) + Curso (p-select A2/A3/A4/A5). 4 KPIs (`app-kpi-card-variant`): sesiones semana, completadas, con asistencia, hoy. Navegación semanal (anterior/hoy/siguiente). Grilla L-S con cards por día. | `AsistenciaProfesionalFacade`, `BranchFacade` | `features/admin/profesional-asistencia/admin-profesional-asistencia.component.ts` | ✅ Estable |
| `AdminSesionDrawerComponent` | Smart / Drawer — Sesión + Asistencia | Output: `closed`. 2 modos via toggle: **Asistencia** (lista de alumnos inscritos con botones present/absent/excused por alumno, bulk "Todos presentes"/"Todos ausentes", `guardarAsistencia()` INSERT/UPDATE), **Editar sesión** (fecha, estado, notas; warning si cancela). `effect()` sincroniza estado local desde facade. Header con tipo, status badge, curso, conteo. | `AsistenciaProfesionalFacade` | `features/admin/profesional-asistencia/admin-sesion-drawer.component.ts` | ✅ Estable |
| `/app/admin/clase-profesional/certificados` | Stub PLANO | — | `features/admin/profesional-certificados/admin-profesional-certificados.component.ts` | 🚧 Stub |
| `/app/admin/clase-profesional/evaluaciones` — `AdminProfesionalEvaluacionesComponent` | Smart Page — Grilla de notas por módulo (escala MTT 10–100, mínimo 75). Selectores de Promoción/Curso, tabla interactiva con `p-inputnumber` por celda, colores dinámicos vía `[style]` (tokens PrimeNG `--p-green-*`/`--p-red-*`), promedio calculado en tiempo real. | `EvaluacionesProfesionalFacade`, `BranchFacade` | `features/admin/profesional-evaluaciones/admin-profesional-evaluaciones.component.ts` | ✅ Estable |
| `/app/admin/clase-profesional/archivo` | **Archivo Profesional** — Historial de promociones finalizadas. .bento-grid + selectores en cascada + tabla de notas 7 módulos + KPIs de aprobación. | `ArchivoFacade`, `BranchFacade` | `features/admin/profesional-archivo/admin-profesional-archivo.component.ts` | ✅ Estable |
| `/app/admin/auditoria` — `AdminAuditoriaComponent` | **Log de Auditoría de Secretarias (Admin-only)** — Inyecta `AuditoriaFacade`. `app-section-hero` con subtitle descriptivo. Panel de filtros: fecha desde/hasta (inputs date), secretaria (p-select), acción (p-select: Crear/Actualizar/Eliminar), módulo (p-select: Usuarios/Matrículas/Pagos/Agenda/Alumnos/…). Botones "Limpiar Filtros", "Exportar Excel" y "Exportar PDF" (stubs). Tabla custom HTML con 6 columnas: FECHA/HORA, USUARIO (nombre+email brand), ACCIÓN (badge coloreado: success=Crear, info=Actualizar, warning=Eliminar), MÓDULO, DETALLES (brand), IP. Skeleton 5 filas con `app-skeleton-block`. Paginación server-side (25/pág) con ellipsis (`visiblePages` computed). Banner informativo sobre política de correos personales vs alias. | `AuditoriaFacade`, `Router` | `features/admin/auditoria/admin-auditoria.component.ts` | ✅ Estable |
| `/app/admin/notificaciones` | Stub PLANO | — | `features/admin/notificaciones/admin-notificaciones.component.ts` | 🚧 Stub |
| **— PORTAL SECRETARIA (23 stubs) —** | | | | |
| `/app/secretaria/dashboard` — `SecretariaDashboardComponent` | **Dashboard (Secretaria)** — Puerto de `DashboardComponent`. BentoGrid + GSAP animateBentoGrid via `effect()` al dejar de cargar. `ngOnInit`: `dashboardFacade.initialize(); dashboardAlertsFacade.initialize()`. KPIs de alumnos, ingresos, clases activas, alertas. Quick actions con rutas secretaria (`app/secretaria/pagos`, etc.). Drawers `RecentActivityDrawerComponent` y `AlertsDrawerComponent` cargados con dynamic `import()` lazy para evitar circularidad. Sin `BranchFacade` (facade maneja branch internamente). | `DashboardFacade`, `DashboardAlertsFacade`, `AuthFacade`, `AgendaFacade`, `AsistenciaClaseBFacade` | `features/secretaria/dashboard/secretaria-dashboard.component.ts` | ✅ UI Ready |
| `/app/secretaria/alumnos` | **Base de Alumnos** — Implementación calco del mockup. Reutiliza `AlumnosListContentComponent`. | — | `features/secretaria/alumnos/secretaria-alumnos.component.ts` | ✅ UI Ready |
| `/app/secretaria/agenda` | **Agenda Semanal** — Thin wrapper idéntico a admin-agenda. Reutiliza `app-agenda-semanal` + `AgendaFacade`. Al clicar slot disponible sin filtro de instructor activo, calcula `slotsAtTime` y los pasa a `setSelectedSlot()` igual que admin-agenda. | `AgendaFacade`, `LayoutDrawerFacadeService` | `features/secretaria/agenda/secretaria-agenda.component.ts` | ✅ UI Ready |
| `/app/secretaria/asistencia` — `SecretariaAsistenciaComponent` | **Control de Asistencia Clase B (Secretaria)** — Fija `branchFilter` desde `AuthFacade.currentUser().branchId` en `ngOnInit`. Reutiliza `AsistenciaClaseBContentComponent`. Abre `AsistenciaTeoriaDrawerComponent` y `AgendarTeoriaDrawerComponent`. **Datos reales de Supabase.** | `AsistenciaClaseBFacade`, `AuthFacade`, `LayoutDrawerFacadeService` | `features/secretaria/asistencia/secretaria-asistencia.component.ts` | ✅ Estable |
| `/app/secretaria/matricula` | **Wizard 6 Pasos (Nueva Matrícula)** — PrimeNG Stepper (`.stepper-premium`). Smart component orquesta 3 facades + AuthFacade. **Paso 4:** `effect()` llama `computePricing()` reactivo al `paymentMode`; `onStep3Next()` dispara `loadAvailableDiscounts()`; `onStep4DataChange` rutar descuento predefinido (`applyPredefinedDiscount`) vs manual (`setDiscount`); `canAdvance` con guard en `onStep4Next()`. **Paso 5 (Físico):** `_signedContractUpload = signal<SignedContractUpload|null>()`; `step5Data.canAdvance = !!upload?.file || contractAccepted()`; `onStep5Next()` solo llama `uploadSignedContract(file)` (ruta física única). **DEV mocks (eliminar con BD real):** `generateDevScheduleGrid()` — 4 semanas × 5 días × 7 slots, ocupación variada por semana. `requiredCount` derivado de `practicalHours × 60 / 45` (fallback: 12). Máx 1 clase por día. | `EnrollmentFacade`, `EnrollmentDocumentsFacade`, `EnrollmentPaymentFacade`, `AuthFacade` | `features/secretaria/matricula/secretaria-matricula.component.ts` | ✅ Estable |
| `/app/secretaria/pagos` — `SecretariaPagosComponent` | **Gestión de Pagos (Secretaria)** — Puerto completo de `AdminPagosComponent`. BentoGrid + GSAP. `ngOnInit`: `facade.initialize()`. 4 KPIs hero: total cobrado, pendiente, alumnos al día, con deuda. Tabla con búsqueda, filtros (estado/mes), paginación. Apertura de drawer de detalle de pago. Sin `BranchFacade` (facade filtra por sede del currentUser). | `PagosFacade`, `AuthFacade`, `LayoutDrawerFacadeService` | `features/secretaria/pagos/secretaria-pagos.component.ts` | ✅ UI Ready |
| `/app/secretaria/contabilidad/cuadratura` | Stub PLANO | — | `features/secretaria/contabilidad-cuadratura/secretaria-contabilidad-cuadratura.component.ts" | 🚧 Stub |
| `/app/secretaria/contabilidad/reportes` | Stub PLANO | — | `features/secretaria/contabilidad-reportes/secretaria-contabilidad-reportes.component.ts" | 🚧 Stub |
| `/app/secretaria/contabilidad/liquidaciones` — `SecretariaContabilidadLiquidacionesComponent` | **Liquidaciones de Instructores (Secretaria)** — Idéntico al admin: thin Smart wrapper con `ngOnInit` → `cargarLiquidaciones()`, escucha `(pagar)` → `registrarPago()`. Reutiliza `app-liquidaciones-content`. | `LiquidacionesFacade` | `features/secretaria/contabilidad-liquidaciones/secretaria-contabilidad-liquidaciones.component.ts` | ✅ Estable |
| `/app/secretaria/certificados` | Stub PLANO | — | `features/secretaria/certificados/secretaria-certificados.component.ts" | 🚧 Stub |
| `/app/secretaria/documentos` | **Repositorio DMS (Secretaria)** — Versión simplificada para secretaría. Solo visualización y carga de documentos de alumnos/escuela. Sin eliminación de registros ni gestión de plantillas (`isAdmin=false`). | `DmsFacade` | `features/secretaria/documentos/secretaria-documentos.component.ts" | ✅ Estable |
| `/app/secretaria/instructores` — `SecretariaInstructoresComponent` | **Gestión de Instructores (Secretaria)** — Calco del admin. Reutiliza los 3 drawers de admin (`AdminInstructorCrearDrawerComponent`, `AdminInstructorVerDrawerComponent`, `AdminInstructorEditarDrawerComponent`). Misma tabla, filtros, paginación y drawer management. | `InstructoresFacade` | `features/secretaria/instructores/secretaria-instructores.component.ts` | ✅ Estable |
| `/app/secretaria/comunicaciones` — `SecretariaComunicacionesComponent` | **Comunicaciones (Próximamente)** — Stub `bento-grid` con card "Próximamente". Sin facade. Ícono `message-square`. | — | `features/secretaria/comunicaciones/secretaria-comunicaciones.component.ts` | 🔜 Próximamente |
| `/app/secretaria/observaciones` — `SecretariaObservacionesComponent` | **Observaciones y Tareas (Secretaria)** — `.bento-grid` + `[appBentoGridLayout]` + GSAP. `ngOnInit`: `facade.initialize()` + `destroyRef.onDestroy(() => facade.dispose())`. **3 KPIs:** pendingMineCount (`sentTasks` con `status=pending`), `receivedTasks().length`, toInstructorTasks (`sentTasks` con `to_role=instructor`). **3 tabs:** "Mis observaciones" (`sentTasks` filtrado `type=observation`), "Tareas recibidas" (`receivedTasks()`), "A instructores" (`sentTasks` filtrado `to_role=instructor`). **2 Hero CTAs:** "Nueva observación" y "Tarea a instructor" → ambos abren `TaskCreateDrawerComponent`. Click card → `facade.selectTask(id)` + `drawer.push(TaskDetailModalComponent)`. | `TasksFacade`, `AuthFacade`, `LayoutDrawerFacadeService`, `GsapAnimationsService` | `features/secretaria/observaciones/secretaria-observaciones.component.ts` | ✅ Estable |
| `/app/secretaria/psicotecnico` | Stub PLANO | — | `features/secretaria/psicotecnico/secretaria-psicotecnico.component.ts" | 🚧 Stub |
| `/app/secretaria/profesional/relatores` — `SecretariaProfesionalRelatoresComponent` | **Thin-wrapper** — Reutiliza `AdminProfesionalRelatoresComponent` directamente. Misma funcionalidad completa (KPIs, búsqueda, filtros, drawers CRUD) compartida con admin. | `RelatoresFacade` | `features/secretaria/profesional-relatores/secretaria-profesional-relatores.component.ts` | ✅ Estable |
| `/app/secretaria/profesional/promociones` — `SecretariaProfesionalPromocionesComponent` | **Thin-wrapper** — Reutiliza `AdminProfesionalPromocionesComponent` directamente. Misma funcionalidad completa (KPIs, búsqueda, filtros, drawers CRUD) compartida con admin. | `PromocionesFacade` | `features/secretaria/profesional-promociones/secretaria-profesional-promociones.component.ts` | ✅ Estable |
| `/app/secretaria/profesional/asistencia` — `SecretariaProfesionalAsistenciaComponent` | **Thin-wrapper** — Reutiliza `AdminProfesionalAsistenciaComponent` directamente. Misma funcionalidad completa (filtros, grilla semanal, drawers asistencia/edición) compartida con admin. | `AsistenciaProfesionalFacade` | `features/secretaria/profesional-asistencia/secretaria-profesional-asistencia.component.ts` | ✅ Estable |
| `/app/secretaria/profesional/notas` — `SecretariaProfesionalNotasComponent` | **Notas y Evaluaciones Profesional (Secretaria)** — Puerto completo de `AdminProfesionalEvaluacionesComponent`. BentoGrid + GSAP. Landing cards con resumen de cursos activos; al seleccionar → gradebook completo. Tabla sticky-header con inputs de nota por módulo (max 3 dígitos, Enter↓/Tab→). `corregirNotaMinima` on blur. `confirmarNotas()` con modal de confirmación (irreversible). Vista mobile con cards por alumno. `computeGradebookStats`, `countModulosCompletos`, `GRADE_PASS`, `MODULE_COUNT`. `ngOnInit`: `branchFacade.setProfessionalOnly(true); facade.loadLanding()`. `ngOnDestroy`: `branchFacade.setProfessionalOnly(false)`. | `EvaluacionesProfesionalFacade`, `BranchFacade` | `features/secretaria/profesional-notas/secretaria-profesional-notas.component.ts` | ✅ UI Ready |
| `/app/secretaria/profesional/certificados` | Stub PLANO | — | `features/secretaria/profesional-certificados/secretaria-profesional-certificados.component.ts" | 🚧 Stub |
| `/app/secretaria/profesional/archivo` | Stub PLANO | — | `features/secretaria/profesional-archivo/secretaria-profesional-archivo.component.ts" | 🚧 Stub |
| `/app/secretaria/notificaciones` | Stub PLANO | — | `features/secretaria/notificaciones/secretaria-notificaciones.component.ts" | 🚧 Stub |
| `/app/secretaria/libro-de-clases` — `SecretariaLibroDeClasesComponent` | **Libro de Clases (Secretaria)** — Implementación completa. BentoGrid + GSAP. Selectores de promoción y curso. 7 secciones colapsables (`openSections signal<Set<string>>`): cabecera (SENCE code editable + horario), profesores, alumnos, asistencia semanal, calendario, evaluaciones, resumen. `ngOnInit`: `branchFacade.setProfessionalOnly(true); facade.reset(); facade.initialize()`. `ngOnDestroy`: `branchFacade.setProfessionalOnly(false)`. `editSenceCode`/`editHorario` sincronizados via `effect()` desde `cabecera()`. `isExporting` + botón exportar PDF. | `LibroDeClasesFacade`, `BranchFacade` | `features/secretaria/libro-de-clases/secretaria-libro-de-clases.component.ts` | ✅ UI Ready |
| `/app/secretaria/ex-alumnos` | Stub PLANO | — | `features/secretaria/ex-alumnos/secretaria-ex-alumnos.component.ts" | 🚧 Stub |
| `/app/secretaria/servicios-especiales` | Stub PLANO | — | `features/secretaria/servicios-especiales/secretaria-servicios-especiales.component.ts" | 🚧 Stub |
| `/app/secretaria/alumnos/pre-inscritos` — `SecretariaAlumnosPreInscritosComponent` | **Pre-inscritos Clase Profesional (Secretaria)** — Puerto completo de `AdminPreInscritosComponent`. BentoGrid + GSAP. `ngOnInit`: `branchFacade.setProfessionalOnly(true); facade.initialize()`. `ngOnDestroy`: `branchFacade.setProfessionalOnly(false)`. 3 KPIs hero: total, pendientes test, aprobados. Búsqueda + filtros por estado/clase. Click fila → `facade.select(row)` + drawer `AdminPreInscritoDrawerComponent` (importado de admin). Sin columna Sede. `backRoute="/app/secretaria/clase-profesional/alumnos"`. | `AdminPreInscritosFacade`, `BranchFacade`, `LayoutDrawerFacadeService` | `features/secretaria/alumnos-pre-inscritos/secretaria-alumnos-pre-inscritos.component.ts` | ✅ UI Ready |
| `/app/secretaria/asistencia-profesional` — `SecretariaAsistenciaProfesionalComponent` | **Asistencia Clase Profesional (Secretaria)** — Puerto completo de `AdminProfesionalAsistenciaComponent`. BentoGrid + GSAP. Selectores de promoción/curso, navegación semanal via `weekOffset`. Grilla de sesiones por día con `SessionDayCardComponent` (importado de admin). Tabla firma semanal con checkbox select-all y `registrarFirmas()`. `AdminSesionDrawerComponent` (importado de admin). `effect()` interno: re-fetch firmas al cambiar semana + cursoId. `ngOnInit`: `branchFacade.setProfessionalOnly(true); facade.initialize()`. `ngOnDestroy`: `branchFacade.setProfessionalOnly(false)`. | `AsistenciaProfesionalFacade`, `BranchFacade` | `features/secretaria/asistencia-profesional/secretaria-asistencia-profesional.component.ts` | ✅ UI Ready |
| `/app/secretaria/asistencia-matriz` — `SecretariaAsistenciaMatrizComponent` | **Asistencia — Matriz General (Próximamente)** — Stub `bento-grid` con card "Próximamente". Sin facade. Ícono `grid-2x2`. | — | `features/secretaria/asistencia-matriz/secretaria-asistencia-matriz.component.ts` | 🔜 Próximamente |
| **— PORTAL INSTRUCTOR (11 stubs) —** | | | | |
| `/app/instructor/dashboard` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. 4 KPIs bento-square: Hoy, Próxima (hora texto), Completadas Mes, Horas Mes. `bento-banner` con grid 2-col: lista clases del día + sidebar (Próximas Clases via `upcomingDays`, Accesos Rápidos, Recordatorio). | `InstructorProfileFacade`, `InstructorClasesFacade`, `InstructorHorasFacade` | `features/instructor/dashboard/instructor-dashboard.component.ts` | ✅ Estable |
| `/app/instructor/alumnos` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. 4 KPIs como `bento-square` directos (sin nested grid). `bento-banner` con buscador + filtros pill + sort + grid `md:grid-cols-2 lg:grid-cols-3` + paginación PAGE_SIZE=9. Student cards con avatar gradiente, barra progreso, `appCardHover`. Drawer via `StudentDrawerDetailComponent`. | `InstructorAlumnosFacade` | `features/instructor/alumnos/instructor-alumnos.component.ts` | ✅ Estable |
| `/app/instructor/alumnos/:id/ficha` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. Skeleton inline por sección. `bento-banner` con 3 cards (`md:grid-cols-3`): Info personal + Clases Prácticas + Clases Teóricas. Tabla desktop (hidden md:block) + Lista mobile (md:hidden) con status badge + CTA contextual. | `InstructorAlumnosFacade` | `features/instructor/ficha/instructor-ficha.component.ts` | ✅ Estable |
| `/app/instructor/alumnos/:id/evaluacion/:sessionId` | Formulario de evaluación con checklist, recuadro de firma (Signature Pad) y registro de nota final / observaciones. | `InstructorClasesFacade` | `features/instructor/evaluacion/instructor-evaluacion.component.ts` | ✅ Estable |
| `/app/instructor/clase/iniciar` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. Contenido en `bento-banner > max-w-3xl mx-auto` (preserva UX de formulario estrecho). Ticket de resumen de clase + formulario odómetro premium + validación inline. | `InstructorClasesFacade` | `features/instructor/clase/instructor-clase.component.ts` | ✅ Estable |
| `/app/instructor/clase-detail/:id` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width (título dinámico por `showFinalStep()`). Contenido en `bento-banner > max-w-4xl mx-auto`. Step 1: checklist evaluación + observaciones. Step 2: km final + calificación por estrellas + firma alumno/instructor (`app-signature-pad`). | `InstructorClasesFacade` | `features/instructor/clase-detail/instructor-clase-detail.component.ts` | ✅ Estable |
| `/app/instructor/horario` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. 4 KPIs bento-square. `bento-banner` con dual-layout: Desktop (`app-weekly-schedule-grid`) + Mobile (`app-daily-schedule-timeline`). Session Cards rutean por estado (scheduled → iniciar, completed → evaluación). | `InstructorHorasFacade` | `features/instructor/horario/instructor-horario.component.ts` | ✅ Estable |
| `/app/instructor/ensayos-teoricos` | **Layout canónico bento-grid. Solo lectura.** Root `.bento-grid[appBentoGridLayout]`, hero full-width sin acciones. 3 KPIs bento-square: Total Registros, Promedio/100, Aprobados (≥80). `bento-banner` con tabla Alumno/RUT/Puntaje/Fecha/Estado + empty state informativo. Sin modal ni formulario — puntajes los registra admin/secretaria. | `InstructorAlumnosFacade` | `features/instructor/ensayos-teoricos/instructor-ensayos-teoricos.component.ts` | ✅ Estable |
| `/app/instructor/liquidacion` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. KPIs condicionales como bento-square directos (`@if` inline en bento-grid). `bento-banner` con gráfico barras horizontales (`app-horizontal-bar-chart`) + tabla registro diario. | `InstructorHorasFacade` | `features/instructor/liquidacion/instructor-liquidacion.component.ts` | ✅ Estable |
| `/app/instructor/notificaciones` | **Layout canónico bento-grid.** Root `.bento-grid[appBentoGridLayout]`, hero full-width. `bento-banner` con feed de notificaciones (card dividida por `divide-y`). Empty state con `app-icon bell-off`. Acción "Marcar todas como leídas" via `NotificationsFacade`. | `NotificationsFacade` | `features/instructor/notificaciones/instructor-notificaciones.component.ts` | ✅ Estable |
| `/app/instructor/tareas` — `InstructorTareasComponent` | **Tareas (Instructor)** — `.bento-grid` + `[appBentoGridLayout]` + GSAP `animateBentoGrid()`. `ngOnInit`: `facade.initialize()` + `destroyRef.onDestroy(() => facade.dispose())`. Sin `effect()` (instructor no cambia de sede). **2 KPIs:** `pendingCount`, `inProgressCount`. **Chips de filtro por tipo** (`p-selectButton` PrimeNG): "Todas" / "Tareas" / "Consultas" / "Observaciones" — `activeFilter = signal<TaskTypeFilter>('all')`. `filteredTasks` computed filtra `receivedTasks()` por tipo. `filterChips` computed: 4 chips con badge `count = activas (≠ completed)` por tipo (reactivo a Realtime). **Empty state contextual** según `activeFilter`. Skeleton 3 dummyTask cards mientras `isLoading()`. Sin Hero CTAs (instructor = receptor puro). Click card → `facade.selectTask(id)` + `drawer.push(TaskDetailModalComponent)`. Spec: `0002-instructor-tareas-vista-por-tipo`. | `TasksFacade`, `LayoutDrawerFacadeService`, `GsapAnimationsService` | `features/instructor/tareas/instructor-tareas.component.ts` | ✅ Estable |
| **— PORTAL ALUMNO —** | | | | |
| `/app/alumno/dashboard` — `AlumnoDashboardComponent` | **Home bento-grid del alumno. Unifica Progreso + Asistencia + Notas + Certificado en una sola vista.** 9 celdas: (1) `app-section-hero` full-width con nombre, chip de estado, acción "Agendar clase" y "Descargar certificado" condicional. (2–5) 4× `app-kpi-card-variant`: Prácticas (X/12), Asistencia Teoría (%), Nota (1–7 Clase B / promedio MTT Profesional), Certificado (estado textual). (6) Panel Progreso: anillo `conic-gradient` con `pctOverall` + grilla 4-col de clases 1..N con íconos `check-circle`/`clock`/`circle`. (7) Panel Notas+Certificado: condicional por `licenseGroup` — Clase B muestra examen único (nota grande + fecha + passed badge); Profesional muestra tabla 7 módulos (nombre, nota, badge); bloque de certificado con estado locked/enabled/issued y botón descarga cuando `issued`. (8) Banner Asistencia: dots coloreados de últimas sesiones + `app-alert-card` si `consecutiveAbsences ≥ 1`. (9) 2 mini-widgets: "Próxima clase" (fecha+hora+instructor) y "Saldo pendiente" (CTA Pagar si > 0, "Al día" si = 0). `afterNextRender` dispara `gsap.animateBentoGrid()` con `setTimeout(50)`. | `StudentHomeFacade`, `GsapAnimationsService`, `Router` | `features/alumno/dashboard/alumno-dashboard.component.ts` | ✅ Estable |
| `/app/alumno/clases` — `AlumnoClasesComponent` | **"Mis Clases" — Historial de clases prácticas y teoría del alumno.** bento-grid con hero (`app-section-hero` icon=clipboard-list + chip teoría%), 2–3× `app-kpi-card-variant` (prácticas completadas/12, asistencia teoría %, próximas agendadas). Panel principal `bento-banner` con tab toggle señal `activeTab` (Prácticas/Teoría): cada sesión muestra número de clase, fecha+hora, duración y badge de estado (sin instructor/vehículo — restricción RLS; sin `evaluation_grade` ni checklist). Profesional: tabs filtran `profSessions` por `kind`. Empty states y skeleton por `loading()`. Sin botón "Agendar". | `StudentClasesFacade`, `GsapAnimationsService` | `features/alumno/clases/alumno-clases.component.ts` | ✅ Estable |
| `/app/alumno/agendar` | Stub PLANO | — | `features/alumno/agendar/alumno-agendar.component.ts" | 🚧 Stub |
| `/app/alumno/pagos` — `AlumnoPagosComponent` | **"Pagos y Clases" — Historial de pagos y resumen financiero del alumno.** Layout full-width (`px-6 py-6 pb-20`) con `app-section-hero` title="Pagos y Clases" (CTA "Pagar $X y agendar clases" condicional vía `heroActions` computed cuando `pendingBalance > 0`), 3× `app-kpi-card-variant` (Total Curso / Ya Pagado / Saldo Pendiente con `toCompact()` + `formatCLP` subValue, color="success"/"warning"+accent), lista cronológica de pagos (icon por tipo, fecha, monto, badge estado). Skeleton integrado en KPI cards con `[loading]`. Navega a `/app/alumno/pagar` al pulsar CTA. | `StudentPaymentFacade`, `Router` | `features/alumno/pagos/alumno-pagos.component.ts` | ✅ Estable |
| `/app/alumno/pagar` — `AlumnoPagarComponent` | **"Agendar y Pagar" — Wizard de pago de segunda mitad Clase B (3 pasos).** Layout full-width (`px-6 py-6 pb-20 space-y-6`) con `app-section-hero` title="Agendar y Pagar" `[actions]="[]"`. Stepper visual: círculos (brand=activo, success+check=completado, border=pendiente) con conectores (success/border según progreso) y labels debajo. Step 1 y 3 usan `grid grid-cols-1 lg:grid-cols-2`. Step 2: tabs de días con `cursor-pointer`, slots con borde siempre visible; contador X/6, máx 1 clase/día. **Auto-avance:** click de slot llama `handleToggleSlot(slotId)` — wrapper que compara `selectedCount()` antes/después y, si creció, llama `advanceToNextAvailableDay()` (itera días restantes de la semana buscando slots libres; si no hay, avanza a semana siguiente). **Panel resumen** visible cuando `selectedCount() > 0`: grid de badges (verde/naranja según completitud) con `selectedSlotsDisplay()` heredado (`lun DD/MM · HH:MM – HH:MM`, ordenado cronológicamente). Step 3: resumen slots + summary pago + CTA "Pagar con Webpay Plus". Redirige a `/app/alumno/pagar/retorno` tras Transbank. | `StudentPaymentFacade` | `features/alumno/pagar/alumno-pagar.component.ts` | ✅ Estable |
| `/app/alumno/pagar/retorno` — `AlumnoPagarRetornoComponent` | **Página de retorno desde Webpay Plus.** Lee `token_ws` / `TBK_TOKEN` de query params. 4 estados: confirmando (skeleton) → éxito (sesiones agendadas + monto pagado) → rechazado → error técnico. CTA "Ver mis clases" o "Intentar nuevamente" según estado. | `StudentPaymentFacade`, `Router` | `features/alumno/pagar/alumno-pagar-retorno.component.ts` | ✅ Estable |
| `/app/alumno/notificaciones` | Stub PLANO | — | `features/alumno/notificaciones/alumno-notificaciones.component.ts" | 🚧 Stub |
| `/app/alumno/horario` — `AlumnoHorarioComponent` | **"Mi Horario" — Calendario semanal read-only del alumno.** bento-grid con hero (contextLine = próxima clase, chip días restantes, sin acciones). Tarjeta destacada "Próxima clase" (`card-tinted` con borde brand, fecha+hora+classNumber+countdown; sin instructor/vehículo — restricción RLS). Grid 7 días (`grid-template-columns: repeat(7, minmax(88px, 1fr))`, overflow-x scroll en mobile): cabecera de día con brand top-border en `isToday`, sesiones con color de borde por estado (`completed`=success, `absent`=error, `scheduled/next`=brand), badge "PRÓXIMA" en la sesión `isNext`. Navegación semana: `goToPrevWeek/goToNextWeek/goToToday()`. Leyenda de estados al pie. Sin botón "Agendar". | `StudentHorarioFacade`, `GsapAnimationsService` | `features/alumno/horario/alumno-horario.component.ts` | ✅ Estable |
| `/app/alumno/pruebas-online` — `AlumnoPruebasOnlineComponent` | **"Pruebas Online" — Hub de simuladores externos con contenido adaptado por tipo de licencia.** Smart: inyecta `StudentHomeFacade` (SWR via `initialize()` en `ngOnInit`). `isProfessional = computed(() => licenseGroup() === 'professional')` determina el contenido. **Clase B:** hero + banner de 4 stats (`card-tinted`, `kpi-value`/`kpi-label`: 35 preg / 45 min / 38 pts / 33 para aprobar) + grid 2 cols con PracticaTest y Educación Vial. **Profesional:** hero con subtítulo distinto, sin stats, solo PracticaTest Profesional (`/examen-teorico`, cubre A2/A3/A4/A5). Cards con `appCardHover`, tag semántico (Gratuito/Popular), CTA "Ir al simulador" + `external-link`. GSAP en `ngAfterViewInit`: `animateHero` + `animateBentoGrid`. | `StudentHomeFacade`, `GsapAnimationsService` | `features/alumno/pruebas-online/alumno-pruebas-online.component.ts` | ✅ Estable |
| `/app/alumno/ayuda` | Stub PLANO | — | `features/alumno/ayuda/alumno-ayuda.component.ts" | 🚧 Stub |
| **— PORTAL RELATOR (6 stubs) —** | | | | |
| `/app/relator/dashboard` | Stub PLANO | — | `features/relator/dashboard/relator-dashboard.component.ts" | 🚧 Stub |
| `/app/relator/alumnos` | Stub PLANO | — | `features/relator/alumnos/relator-alumnos.component.ts" | 🚧 Stub |
| `/app/relator/asistencia` | Stub PLANO | — | `features/relator/asistencia/relator-asistencia.component.ts" | 🚧 Stub |
| `/app/relator/notas` | Stub PLANO | — | `features/relator/notas/relator-notas.component.ts" | 🚧 Stub |
| `/app/relator/maquinaria` | Stub PLANO | — | `features/relator/maquinaria/relator-maquinaria.component.ts" | 🚧 Stub |
| `/app/relator/acta-final` | Stub PLANO | — | `features/relator/acta-final/relator-acta-final.component.ts" | 🚧 Stub |


## Auto-Index — Componentes detectados por AST (generado automáticamente)

<!-- AUTO-GENERATED:BEGIN -->
| Selector | Inputs | Outputs | Archivo |
|----------|--------|---------|---------|
| `app-agenda-semanal` | `weekData`, `filteredDays`, `timeRows`, `isLoading`, `isCurrentWeek`, `instructors`, `selectedInstructorId`, `showKpis`, `showHero` | `weekNext`, `weekPrev`, `weekToday`, `instructorFilterChange`, `slotClick` | `src/app/shared/components/agenda-semanal/agenda-semanal.component.ts` |
| `app-agenda-slot` | `slot`, `compact` | `slotClicked` | `src/app/shared/components/agenda-semanal/agenda-slot.component.ts` |
| `app-ajustes-drawer` | — | — | `src/app/shared/components/ajustes-drawer/ajustes-drawer.component.ts` |
| `app-alert-card` | `severity`, `title`, `actionLabel`, `dismissible` | `action`, `dismissed` | `src/app/shared/components/alert-card/alert-card.component.ts` |
| `app-alumnos-list-content` | `alumnos`, `isLoading`, `isExporting`, `isGeneratingFicha`, `trashView`, `basePath`, `alumnosPorVencer` | `refreshRequested`, `archivarRequested`, `restaurarRequested`, `trashViewToggled`, `exportRequested`, `fichaExportRequested` | `src/app/shared/components/alumnos-list-content/alumnos-list-content.component.ts` |
| `app-alumnos-por-vencer-drawer` | — | — | `src/app/shared/components/alumnos-por-vencer-drawer/alumnos-por-vencer-drawer.component.ts` |
| `app-alumnos-profesional-list-content` | `alumnos`, `isLoading`, `trashView`, `basePath` | `refreshRequested`, `preInscritosRequested`, `archivarRequested`, `restaurarRequested`, `trashViewToggled` | `src/app/shared/components/alumnos-profesional-list-content/alumnos-profesional-list-content.component.ts` |
| `app-asistencia-clase-b-content` | `kpis`, `clasesPracticas`, `alertas`, `instructores`, `isLoading`, `isSaving`, `cycles`, `selectedCycleId`, `clasesCiclo`, `rosterCiclo`, `addableStudents`, `isLoadingCiclos`, `isLoadingCycle`, `sendingClassId`, `selectedDate` | `markAttendance`, `justifyAbsence`, `removeSchedule`, `reactivateSchedule`, `sendReminder`, `dateChange`, `refreshRequested`, `iniciarClase`, `finalizarClase`, `selectCycle`, `saveCicloZoomLink`, `updateCicloTopic`, `sendCicloZoom`, `moveCicloStudent`, `requestAddable`, `addCicloStudent` | `src/app/shared/components/asistencia-clase-b-content/asistencia-clase-b-content.component.ts` |
| `app-async-btn` | `label`, `icon`, `loading`, `success`, `error`, `disabled`, `loadingLabel`, `successLabel`, `errorLabel` | — | `src/app/shared/components/async-btn/async-btn.component.ts` |
| `app-badge` | `variant` | — | `src/app/shared/components/badge/badge.component.ts` |
| `app-branch-gate` | `branches`, `reason`, `professionalOnly` | `branchSelected` | `src/app/shared/components/branch-gate/branch-gate.component.ts` |
| `app-branch-selector` | `branches`, `selectedBranchId`, `showAllOption`, `topbarMode`, `disabledBranchIds`, `allOptionDisabled`, `lockReason` | `branchChange` | `src/app/shared/components/branch-selector/branch-selector.component.ts` |
| `app-certificacion-clase-b-content` | `alumnos`, `kpis`, `log`, `isLoading`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes` | `generarCertificado`, `verCertificado`, `enviarEmail`, `generarPendientes`, `enviarEmailsMasivo`, `exportar` | `src/app/shared/components/certificacion-clase-b-content/certificacion-clase-b-content.component.ts` |
| `app-certificacion-profesional-content` | `promociones`, `cursos`, `selectedPromocionId`, `selectedCursoId`, `alumnos`, `kpis`, `log`, `isLoading`, `isLoadingAlumnos`, `generatingId`, `sendingEmailId`, `sendingMasivo`, `isExporting`, `isGeneratingPendientes` | `promocionSelected`, `cursoSelected`, `generarCertificado`, `verCertificado`, `enviarEmail`, `generarPendientes`, `enviarEmailsMasivo`, `exportar` | `src/app/shared/components/certificacion-profesional-content/certificacion-profesional-content.component.ts` |
| `app-ciclos-teoricos-content` | `cycles`, `selectedCycleId`, `clases`, `roster`, `addableStudents`, `isLoading`, `isLoadingCycle`, `isSaving`, `sendingClassId` | `selectCycle`, `saveZoomLink`, `updateTopic`, `sendZoom`, `moveStudent`, `requestAddable`, `addStudent` | `src/app/shared/components/ciclos-teoricos-content/ciclos-teoricos-content.component.ts` |
| `app-cuadratura-content` | `pagosHoy`, `gastosHoy`, `fondoInicial`, `ingresosEfectivoHoy`, `totalIngresosHoy`, `totalEgresosHoy`, `saldoTeorico`, `cajaYaCerrada`, `isLoading`, `isSaving`, `isExporting`, `isDrawerOpen`, `sanitized` | `guardarCierre`, `abrirIngreso`, `abrirEgreso`, `eliminarIngreso`, `eliminarEgreso`, `exportRequested`, `fondoInicialChange` | `src/app/shared/components/cuadratura-content/cuadratura-content.component.ts` |
| `app-daily-schedule-timeline` | `daySchedule`, `weekDays`, `selectedDateString`, `isLoading` | `daySelect`, `blockClick` | `src/app/shared/components/daily-schedule-timeline/daily-schedule-timeline.component.ts` |
| `app-date-input` | `value`, `label`, `id`, `required`, `disabled`, `inline`, `readonlyInput`, `min`, `max`, `placeholder`, `styleClass`, `inputStyleClass` | `valueChange` | `src/app/shared/components/date-input/date-input.component.ts` |
| `app-detalle-cuadratura-modal` | — | — | `src/app/shared/components/detalle-cuadratura-modal/detalle-cuadratura-modal.component.ts` |
| `app-dms-list-content` | `basePath`, `studentsWithDocs`, `recentDocs`, `schoolDocs`, `templates`, `isLoading`, `isAdmin` | `uploadStudentDoc`, `uploadSchoolDoc`, `uploadTemplate`, `viewStudentDocs`, `viewDocument`, `deleteStudentDoc`, `deleteSchoolDoc`, `deleteTemplate`, `downloadTemplate` | `src/app/shared/components/dms-list-content/dms-list-content.component.ts` |
| `app-dms-viewer-modal` | — | `closed` | `src/app/shared/components/dms-viewer-modal/dms-viewer-modal.component.ts` |
| `app-drawer-content-loader` | — | — | `src/app/shared/components/drawer-content-loader/drawer-content-loader.component.ts` |
| `app-drawer-form` | `hasFooter`, `maxWidthRem` | — | `src/app/shared/components/drawer-form/drawer-form.component.ts` |
| `app-egreso-modal` | `isOpen`, `isSaving` | `guardar`, `cerrado` | `src/app/shared/components/egreso-modal/egreso-modal.component.ts` |
| `app-eliminar-alumno-modal` | `visible`, `alumnoNombre`, `hasHistory`, `isDeleting` | `confirmado`, `cancelado` | `src/app/shared/components/eliminar-alumno-modal/eliminar-alumno-modal.component.ts` |
| `app-email-input` | `value`, `id`, `label`, `required`, `placeholder`, `forceDirty` | `valueChange` | `src/app/shared/components/email-input/email-input.component.ts` |
| `app-empty-state` | `message`, `subtitle`, `icon`, `actionLabel`, `actionIcon` | `action` | `src/app/shared/components/empty-state/empty-state.component.ts` |
| `app-evaluation-checklist` | `items` | `itemsChange` | `src/app/shared/components/evaluation-checklist/evaluation-checklist.component.ts` |
| `app-ex-alumnos-profesional-content` | `egresados`, `isLoading`, `backRoute` | `reEnroll` | `src/app/shared/components/ex-alumnos-profesional-content/ex-alumnos-profesional-content.component.ts` |
| `app-flota-list-content` | `vehicles`, `kpis`, `isLoading`, `basePath` | `newVehicle`, `editVehicle`, `viewAgenda`, `viewMaintenances`, `printRouteSheet`, `printAllRouteSheets`, `manageDocuments`, `typeFilterChange`, `statusFilterChange`, `searchChange`, `refreshRequested` | `src/app/shared/components/flota-list-content/flota-list-content.component.ts` |
| `app-historial-cuadraturas-content` | `cierres`, `isLoading`, `mesActual`, `anioActual`, `backRoute`, `backLabel`, `isExporting` | `mesAnterior`, `mesSiguiente`, `exportarMes`, `cierreClicked` | `src/app/shared/components/historial-cuadraturas-content/historial-cuadraturas-content.component.ts` |
| `app-horizontal-bar-chart` | `title`, `subtitle`, `data` | — | `src/app/shared/components/horizontal-bar-chart/horizontal-bar-chart.component.ts` |
| `app-icon` | `name`, `size`, `color`, `ariaHidden`, `ariaLabel` | — | `src/app/shared/components/icon/icon.component.ts` |
| `app-action-kpi-card` | `value`, `label`, `suffix`, `prefix`, `accent`, `icon`, `size`, `color`, `pulse`, `loading` | — | `src/app/shared/components/kpi-card/action-kpi-card.component.ts` |
| `app-kpi-card-variant` | `value`, `label`, `suffix`, `prefix`, `trend`, `trendLabel`, `trendSuffix`, `subValue`, `accent`, `icon`, `color`, `loading`, `progressPercent` | — | `src/app/shared/components/kpi-card/kpi-card-variant.component.ts` |
| `app-kpi-card` | `value`, `label`, `suffix`, `prefix`, `trend`, `trendLabel`, `accent`, `icon`, `size`, `color` | — | `src/app/shared/components/kpi-card/kpi-card.component.ts` |
| `app-liquidaciones-content` | `liquidaciones`, `kpis`, `isLoading`, `isExporting`, `mesActual`, `anioActual`, `isDrawerOpen` | `mesAnterior`, `mesSiguiente`, `deshacer`, `pagar`, `exportRequested` | `src/app/shared/components/liquidaciones-content/liquidaciones-content.component.ts` |
| `app-live-classes-panel` | `classes`, `loading` | `actionClick` | `src/app/shared/components/live-classes-panel/live-classes-panel.component.ts` |
| `app-login-card` | `mode`, `loading`, `errorMsg`, `successMsg` | `modeChange`, `formSubmit` | `src/app/shared/components/login-card/login-card.component.ts` |
| `app-assignment-step` | `data`, `loading`, `stepNumber`, `hidePaymentMode`, `nextLabel` (default `'Continuar a Documentos'` — override para reutilizar el Step 2 fuera del wizard, ej. RF-053 Reagendar Clases) | `dataChange`, `next`, `back` | `src/app/shared/components/matricula-steps/assignment/assignment.component.ts` |
| `app-confirmation-step` | `data` | `finish`, `downloadReceipt`, `downloadContract` | `src/app/shared/components/matricula-steps/confirmation/confirmation.component.ts` |
| `app-contract-step` | `data`, `loading`, `stepNumber`, `isPublic`, `file` | `dataChange`, `generateContract`, `next`, `back` | `src/app/shared/components/matricula-steps/contract/contract.component.ts` |
| `app-documents-step` | `data`, `loading`, `stepNumber`, `file` | `fileSelected`, `lightboxOpen`, `confirmPhoto`, `next`, `back` | `src/app/shared/components/matricula-steps/documents/documents.component.ts` |
| `app-draft-list` | `drafts` | `resume`, `discard`, `startNew` | `src/app/shared/components/matricula-steps/draft-list/draft-list.component.ts` |
| `app-payment-step` | `data`, `loading` | `dataChange`, `next`, `back` | `src/app/shared/components/matricula-steps/payment/payment.component.ts` |
| `app-personal-data-step` | `data`, `loading`, `hiddenCategories`, `branches`, `selectedBranchId` | `dataChange`, `next`, `cancel`, `branchChange`, `rutBlur` | `src/app/shared/components/matricula-steps/personal-data/personal-data.component.ts` |
| `app-psych-test` | `answers`, `loading` | `answersChange`, `next`, `back` | `src/app/shared/components/matricula-steps/psych-test/psych-test.component.ts` |
| `app-public-confirmation` | `type`, `enrollmentNumber`, `message` | — | `src/app/shared/components/matricula-steps/public-confirmation/public-confirmation.component.ts` |
| `app-media-upload-control` | `label`, `buttonLabel`, `buttonIcon`, `accept`, `isUploading`, `previewType`, `files` | `fileSelected` | `src/app/shared/components/media-upload-control/media-upload-control.component.ts` |
| `app-notifications-panel` | `notifications`, `unreadCount` | `markRead`, `markAllRead`, `notifClicked` | `src/app/shared/components/notifications-panel/notifications-panel.component.ts` |
| `app-pago-instructor-drawer` | — | — | `src/app/shared/components/pago-instructor-modal/pago-instructor-modal.component.ts` |
| `app-phone-input` | `value`, `id`, `label`, `required`, `forceDirty` | `valueChange` | `src/app/shared/components/phone-input/phone-input.component.ts` |
| `app-public-context-banner` | `context` | `editRequested` | `src/app/shared/components/public-enrollment-steps/public-context-banner/public-context-banner.component.ts` |
| `app-public-contract` | `data` | `contractSigned`, `goBack` | `src/app/shared/components/public-enrollment-steps/public-contract/public-contract.component.ts` |
| `app-public-documents` | `data`, `isUploading` | `fileSelected`, `next`, `back` | `src/app/shared/components/public-enrollment-steps/public-documents/public-documents.component.ts` |
| `app-public-license-type` | `availableFlows`, `currentFlow` | `flowSelect`, `next` | `src/app/shared/components/public-enrollment-steps/public-license-type/public-license-type.component.ts` |
| `app-public-orientation` | `siteLinks` | — | `src/app/shared/components/public-enrollment-steps/public-orientation/public-orientation.component.ts` |
| `app-public-payment` | `summary` | `proceed`, `back` | `src/app/shared/components/public-enrollment-steps/public-payment/public-payment.component.ts` |
| `app-public-payment-mode` | `options` | `modeSelect`, `next`, `back` | `src/app/shared/components/public-enrollment-steps/public-payment-mode/public-payment-mode.component.ts` |
| `app-public-personal-data` | `data`, `context`, `loading` | `dataChange`, `next`, `back` | `src/app/shared/components/public-enrollment-steps/public-personal-data/public-personal-data.component.ts` |
| `app-public-schedule` | `data`, `loading` | `dataChange`, `next`, `back` | `src/app/shared/components/public-enrollment-steps/public-schedule/public-schedule.component.ts` |
| `app-public-wizard-shell` | `steps`, `currentStep`, `title`, `subtitle`, `brandName`, `whatsappUrl` | `helpClick` | `src/app/shared/components/public-enrollment-steps/public-wizard-shell/public-wizard-shell.component.ts` |
| `app-rentabilidad-cursos` | — | — | `src/app/shared/components/rentabilidad-cursos/rentabilidad-cursos.component.ts` |
| `app-reportes-contables-content` | `kpis`, `ingresosCategoria`, `gastosCategoria`, `evolucionMensual`, `detalleDiario`, `diasConMovimientos`, `escuela`, `isLoading`, `isExporting`, `gastosFijos`, `filtros` | `aplicarFiltros`, `exportRequested`, `registrarGastoClick`, `verDetalle` | `src/app/shared/components/reportes-contables-content/reportes-contables-content.component.ts` |
| `app-schedule-grid` | `scheduleGrid`, `slotSelection`, `scheduleLoading` | `slotsChange` | `src/app/shared/components/schedule-grid/schedule-grid.component.ts` |
| `app-search-panel` | `groups`, `rightPx` | `queryChange`, `closed`, `resultSelected` | `src/app/shared/components/search-panel/search-panel.component.ts` |
| `app-section-hero` | `title`, `contextLine`, `subtitle`, `icon`, `chips`, `actions`, `backRoute`, `backLabel`, `animateOnInit`, `backClickable`, `density`, `kpis`, `loading` | `actionClick`, `backClicked`, `kpiClick` | `src/app/shared/components/section-hero/section-hero.component.ts` |
| `app-agregar-servicio-drawer` | — | — | `src/app/shared/components/servicios-especiales-content/drawers/agregar-servicio-drawer.component.ts` |
| `app-registrar-venta-drawer` | — | — | `src/app/shared/components/servicios-especiales-content/drawers/registrar-venta-drawer.component.ts` |
| `app-servicios-especiales-content` | `catalogo`, `ventas`, `kpis`, `isLoading`, `isExporting`, `backRoute` | `requestRegistrarVenta`, `requestNuevoServicio`, `cobroRegistrado`, `exportarHistorial` | `src/app/shared/components/servicios-especiales-content/servicios-especiales-content.component.ts` |
| `app-signature-pad` | `label`, `height` | `signatureChange` | `src/app/shared/components/signature-pad/signature-pad.component.ts` |
| `app-skeleton-block` | `variant`, `width`, `height`, `borderRadius` | — | `src/app/shared/components/skeleton-block/skeleton-block.component.ts` |
| `app-stat-box` | `label`, `value`, `variant`, `icon`, `suffix`, `compact`, `useMono` | — | `src/app/shared/components/stat-box/stat-box.component.ts` |
| `app-tabs` | `tabs`, `activeId`, `variant`, `uppercase` | `activeIdChange` | `src/app/shared/components/tabs/tabs.component.ts` |
| `app-task-card` | `task`, `loading` | `cardClicked` | `src/app/shared/components/task-card/task-card.component.ts` |
| `app-task-reply-thread` | `replies`, `taskStatus`, `currentUserId` | `replySent` | `src/app/shared/components/task-reply-thread/task-reply-thread.component.ts` |
| `app-task-status-badge` | `status` | — | `src/app/shared/components/task-status-badge/task-status-badge.component.ts` |
| `app-user-panel` | `user` | `action`, `logout` | `src/app/shared/components/user-panel/user-panel.component.ts` |
| `app-weekly-schedule-grid` | `schedule`, `isLoading`, `selectedDate` | `prevWeek`, `nextWeek`, `today`, `daySelect`, `blockClick` | `src/app/shared/components/weekly-schedule-grid/weekly-schedule-grid.component.ts` |

<!-- AUTO-GENERATED:END -->
