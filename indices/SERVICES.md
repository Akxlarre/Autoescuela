# Registro de Servicios

> **Regla de Actualización (OBLIGATORIA):** El Agente DEBE usar sus herramientas de sistema (escritura de archivos) para registrar el servicio en la tabla correspondiente al crearlo. Solo si el entorno no los soporta, usa el bloque `<memory_update>` para el humano.

## 1. Core & Utility Services
Servicios estructurales compartidos que proveen funcionalidades base, autenticación o UI interactions globales.

| Servicio | Responsabilidad Principal | Ubicación (File Path) | Dependencias | Estado |
|----------|---------------------------|-----------------------|--------------|--------|
| `SupabaseService` | Cliente Supabase singleton — NO inyectar en UI, solo en Facades/Services | `core/services/infrastructure/supabase.service.ts` | @supabase/supabase-js | ✅ Estable |
| `ThemeService` | Modo claro/oscuro/sistema con `[data-mode='dark']` en documentElement; persiste en localStorage | `core/services/theme.service.ts` | GsapAnimationsService, MessageService | ✅ Estable |
| `GsapAnimationsService` | Centraliza TODAS las animaciones GSAP: bento, counters, hover, page enter, reduced-motion. **Sistema de tokens (fix-018):** registra `CustomEase` en constructor; `getCssDuration(token, fallback)` lee tokens `--duration-*` en ms y los convierte a segundos; `getCssEase(token, fallback)` convierte `cubic-bezier(...)` del design system a CustomEase con cache (evita hardcodear motion). **`animateBentoGrid(containerEl, { skipOpacity? })`** retorna `() => void` (cleanup para `DestroyRef`): separa celdas en "hero" (`.card-accent`, `.bento-banner`, `.bento-hero`, `[data-animate-hero]`) → scale+lift, y celdas regulares → stagger, ambas leen tokens `--duration-reveal-hero/cell`, `--stagger-reveal`, `--delay-reveal-grid`, `--ease-out-expo`. `skipOpacity=true` omite la transición de opacidad cuando una View Transition ya cubre el fade. Retira `.is-reveal-pending` en `immediateRender` (sin parpadeo). Fixes perf: `will-change` limpiado en `onComplete`; tokens leídos dinámicamente en `addCardHover`. | `core/services/ui/gsap-animations.service.ts` | gsap, ScrollTrigger, CustomEase | ✅ Estable |
| `LayoutService` | Estado responsive del sidebar drawer en mobile (`sidebarOpen` signal) | `core/services/layout.service.ts` | — | ✅ Estable |
| `RoleService` | Dev utility — signal `currentRole` (`UserRole`), persiste en sessionStorage. Expone `setRole()`. Tipos: `admin\|secretaria\|instructor\|alumno\|relator`. | `core/services/auth/role.service.ts` | — | ✅ Estable |
| `MenuConfigService` | Navegación por rol — `menuItems = computed<NavGroup[]>()` según `AuthFacade.currentUser()?.role`. Interfaces: `NavItem { label, icon, routerLink, badge? }` / `NavGroup { group, items }`. 5 navs: admin, secretaria, instructor, alumno, relator. | `core/services/auth/menu-config.service.ts` | AuthFacade | ✅ Estable |
| `ToastService` | Capa 1 de notificaciones: wrapper delgado sobre PrimeNG `MessageService` para toasts efímeros. Métodos: `success()`, `error()` (6s), `warning()`, `info()`. Consumido por `NotificationsFacade` y otros facades. | `core/services/ui/toast.service.ts` | MessageService (PrimeNG) | ✅ Estable |
| `SearchPanelService` | Estado signal del panel de búsqueda global (open/close/toggle) para `[appSearchShortcut]` | `core/services/search-panel.service.ts` | — | ✅ Estable |
| `BreadcrumbService` | Breadcrumb reactivo; consume `NavGroup[]` de `MenuConfigService` vía `buildFromGroups(url, groups)`; deriva trail desde grupos y sus items | `core/services/breadcrumb.service.ts` | Router, MenuConfigService | ✅ Estable |
| `ConfirmModalService` | Modal de confirmación imperativo con patrón `confirm(ConfirmConfig) → Promise<boolean>`; sin dependencia de PrimeNG. Severidades: `danger\|warn\|success\|info\|secondary`. Renderizado global en `AppShellComponent`. Usado por `enrollmentDraftGuard` y `SecretariaMatriculaComponent`. | `core/services/ui/confirm-modal.service.ts` | — | ✅ Estable |
| `ModalOverlayService` | Teleporta modales al overlay container (z-index > topbar) | `core/services/modal-overlay.service.ts` | — | ✅ Estable |
| `LayoutDrawerService` | Orquesta el drawer arquitectónico del AppShell. Estado via signal. **Navegación en pila:** `open()` (raíz, limpia historial), `push()` (sub-vista sin cerrar panel), `back()` (restaura anterior o cierra), `close()` (cierra + limpia historial). `canGoBack` computed. `clear()` limpia componente post-animación GSAP. | `core/services/ui/layout-drawer.service.ts` | — | ✅ Estable |
| `LayoutDrawerFacadeService` | Interfaz pública para componentes UI hacia `LayoutDrawerService`. Expone: `isOpen`, `component`, `title`, `icon`, `actions`, `canGoBack`. Métodos: `open()`, `push()`, `back()`, `close()`, `setActions()`. | `core/services/ui/layout-drawer.facade.service.ts` | LayoutDrawerService | ✅ Estable |
| `DmsViewerService` | Estado y control del visor global de documentos (PDF/Imágenes). `open(DmsViewerDocument)` abre el visor vía `LayoutDrawerFacadeService` con botón "Descargar" inyectado como acción del drawer. `openByUrl(url, name)` auto-detecta el tipo desde la extensión de la URL (`.pdf` → pdf; `.jpg/.png/...` → image; resto → other). `close()` limpia el signal. `currentDoc` (readonly signal), `isOpen` (computed). | `core/services/ui/dms-viewer.service.ts` | LayoutDrawerFacadeService | ✅ Estable |
| `TaskCreateContextService` | Puente de estado entre la página que abre el drawer de creación de tareas y `TaskCreateDrawerComponent`. `prime(type)` setea el tipo inicial antes de abrir; `reset()` lo restaura a `'task'` tras la lectura. | `core/services/ui/task-create-context.service.ts` | — | ✅ Estable |
| `EpqPrintService` | Abre una ventana de impresión con el test psicológico EPQ en blanco (81 preguntas Sí/No) para que un pre-inscrito profesional que no lo respondió online lo rinda en papel en la sede. `printTest({ studentName?, rut?, licencia? })` → `boolean` (false si el navegador bloqueó el popup). El armado del HTML es la función pura `buildEpqTestHtml` (`core/utils/epq-print.util.ts`). Consumido por `AdminPreInscritosFacade.printBlankTest()`. | `core/services/ui/epq-print.service.ts` | buildEpqTestHtml (util) | ✅ Estable |
| `ErrorSanitizerService` | Mapea errores crudos (PostgREST, HttpErrorResponse, genéricos) a mensajes seguros y amigables. Evita la exposición de datos sensibles. | `core/services/infrastructure/error-sanitizer.service.ts` | — | ✅ Estable |

## 1b. Pure Utilities (Functional Core)
Funciones puras sin estado ni inyección de Angular. Testeables sin framework.

| Utilidad | Funciones exportadas | Ubicación (File Path) | Usada por | Estado |
|----------|---------------------|-----------------------|-----------|--------|
| `rut.utils` | `cleanRut()`, `formatRut()`, `normalizeRutForStorage()`, `validateRut()` | `core/utils/rut.utils.ts` | `PersonalDataComponent`, `EnrollmentFacade` | ✅ Estable |
| `notification.utils` | `mapReferenceToNotificationType(referenceType)`, `mapNotificationDtoToUi(dto)` — Mapeo DTO→UI para notificaciones. `reference_type` → `NotificationType` (document_expiry→warning, payment→info, class_b/professional_session→success). | `core/utils/notification.utils.ts` | `NotificationsFacade` | ✅ Estable |
| `professional-modules` | Catálogo de módulos Clase Profesional (Art. 16 MTT). Constantes: `GRADE_MIN` (10), `GRADE_MAX` (100), `GRADE_PASS` (75), `MODULE_COUNT` (7). Funciones: `getModuleNames(licenseClass)` → array de 7 nombres (módulo 5 varía: A4/A5→Carga, A2/A3→Pasajeros), `getModuleShortLabel(n)` → "Módulo N", `isPassing(grade)` → `grade >= 75`, `roundGrade(value)` → 1 decimal, `calcAverage(grades[])` → promedio ignorando nulos. | `core/utils/professional-modules.ts` | `EvaluacionesProfesionalFacade`, `LibroDeClasesFacade` | ✅ Estable |
| `epq-questions.const` | Cuestionario EPQ (Eysenck Personality Questionnaire) — 81 ítems Sí/No para evaluación psicológica de pre-inscripciones Clase Profesional. Exporta `EPQ_QUESTIONS: readonly string[]`. Compartido entre `psych-test.component` (formulario público) y `admin-pre-inscrito-drawer.component` (evaluación admin). | `core/utils/epq-questions.const.ts` | `PsychTestComponent`, `AdminPreInscritoDrawerComponent` | ✅ Estable |
| `epq-print.util` | `buildEpqTestHtml({ studentName?, rut?, licencia? }) → string` — función pura que arma el documento HTML imprimible del test EPQ (81 preguntas con casillas Sí/No, encabezado con datos del alumno o líneas en blanco, estilos `@media print`). Escapa HTML para evitar inyección. Usada por `EpqPrintService` (que aísla el `window.open`). | `core/utils/epq-print.util.ts` | `EpqPrintService` | ✅ Estable |
| `carnet-menu.util` | `buildCarnetMenu({ initialPath, fullPath, primeras6Completadas }) → SectionHeroMenuItem[]` — función pura (fix-019) que arma el menú desplegable "Carnet" del alumno Clase B: dos grupos (6/12 clases) con labels Generar/Volver a generar según existencia de cada PDF, "Ver" deshabilitado hasta generar, y carnet de 12 bloqueado (con `hint`) hasta completar las primeras 6 clases. | `core/utils/carnet-menu.util.ts` | `AdminAlumnoDetalleComponent` | ✅ Estable |
| `gradebook-stats` | Functional Core del gradebook de evaluaciones profesionales (fix-024). `computeGradebookStats(grilla) → GradebookStats` — deriva `{ alumnosCompletos, totalAlumnos, promedioCurso, enRiesgo, modulosCargados }` para el KPI strip. `countModulosCompletos(fila) → number` — módulos con nota registrada. `isFilaCompleta(fila) → boolean`. Puro/testeable (13 tests), reutilizable por vistas tipo libro de notas (Asistencia, Libro de Clases). | `core/utils/gradebook-stats.ts` | `AdminProfesionalEvaluacionesComponent` | ✅ Estable |
| `evaluaciones-landing` | Functional Core del aterrizaje de evaluaciones profesionales (fix-025). Ensambla la estructura "promoción-padre → cursos con resumen vivo" a partir de filas crudas. `buildCursoResumen(course, enrollments, grades) → CursoResumen` — deriva `estado` (`sin_iniciar`/`en_edicion`/`confirmada`), `promedio` (media de promedios individuales), conteos de alumnos. `buildLanding(promotions, courses, enrollments, grades) → PromocionConCursos[]` — árbol completo para el aterrizaje con totales de grupo. `cursoPromedioAprueba(resumen) → boolean\|null`. Interfaces lite: `PromotionLite`, `CourseLite`, `EnrollmentLite`, `GradeLite`. Puro/testeable (12 tests), sin dependencias de Angular. | `core/utils/evaluaciones-landing.ts` | `EvaluacionesProfesionalFacade` | ✅ Estable |
| `date.utils` | `toISODate(date)` → `YYYY-MM-DD` en hora local. `to24hTime(date)` → `HH:MM` (es-CL). `formatChileanDate(date, opts?)` → fecha legible es-CL. `buildDayLabel(dateStr)` → `"Lun 14 Abr"`. `formatCLP(amount)` → CLP formateado. `getChileDateTimeRange(isoDate)` → `{ start, end }` ISO strings con offset UTC real de `America/Santiago` (maneja DST CLT/CLST automáticamente) — usar en filtros `gte`/`lte` de Supabase sobre campos `timestamptz` para evitar que PostgreSQL interprete los rangos como UTC. | `core/utils/date.utils.ts` | `CuadraturaFacade`, múltiples facades y componentes | ✅ Estable |
| `db-error.utils` | `toFriendlyDbMessage(err, fallback) → string` — sanitiza errores de Supabase/PostgreSQL para la UI (fix-015 AC2). Mapea códigos PG (`23505` duplicado, `23502` not-null, `23514` check/edad mínima, `23503` FK, `42501` RLS) a mensajes amigables en español. **NUNCA** expone el `error.message` crudo (columnas/constraints) al usuario — el error real se loguea a consola en el Facade. Usar en todo `catch` de Facade cuyo `_error` se renderice en pantalla. | `core/utils/db-error.utils.ts` | `CursosSingularesFacade` | ✅ Estable |
| `age.utils` | Cálculo de edad y estado de alerta para el wizard de matrícula. `calcAge(birthDate) → number\|null` — edad en años completos; null si fecha inválida. `isMinor(birthDate) → boolean` — true si < 18 años. `getAgeStatus(birthDate, courseType) → AgeAlertStatus` — fuente única de verdad para alertas de edad: evalúa `under-17`, `under-20-professional` (profesional va ANTES de menor-18), `requires-authorization` (17-18 años en no-profesional), `ok`, `none`. `isInvalidDate(dateStr: string) → boolean` — detecta fechas imposibles (ej: 2001-02-29) usando rollover de `Date` JS. **Usar estas funciones en lugar de reimplementar la lógica en cada componente.** | `core/utils/age.utils.ts` | `PublicPersonalDataComponent`, `PersonalDataComponent` (admin/secretaria), `AdminCursoSingularInscribirDrawerComponent` | ✅ Estable |
| `email.utils` | Validación y normalización de email. `validateEmail(email: string) → boolean` — regex RFC válido con TLD obligatorio, acepta `+tag`. `normalizeEmail(email: string) → string` — `trim().toLowerCase()` para normalizar al blur/submit. | `core/utils/email.utils.ts` | `EmailInputComponent`, `PublicPersonalDataComponent` | ✅ Estable |
| `phone.utils` | Validación y normalización de teléfonos E.164. `DIAL_CODES: DialCode[]` — 9 prefijos: CL +56, AR +54, PE +51, BO +591, CO +57, VE +58, ES +34, US +1, Otro. `validatePhone(digits, dialCode) → boolean` — CL: 8 dígitos (fijo) o 9 dígitos empezando en 9 (móvil); internacional: 7–15 dígitos. `normalizePhone(digits, dialCode) → string` — devuelve E.164 sin espacios (`"+56912345678"`). | `core/utils/phone.utils.ts` | `PhoneInputComponent`, `PublicPersonalDataComponent` | ✅ Estable |
| `name.utils` | Validación y limpieza de nombres propios. `validateName(name: string) → boolean` — mínimo 2 chars tras trim, solo letras Unicode (tildes incluidas), espacios, guiones y apóstrofes. `stripInvalidNameChars(raw: string) → string` — elimina todo char fuera del charset permitido (para auto-strip en tiempo real). Usa regex inline (no compartido) para evitar bug de `lastIndex` con flag `g`. | `core/utils/name.utils.ts` | `PublicPersonalDataComponent` | ✅ Estable |
| `excel.utils` | `downloadExcel(sheetName, headers, rows, filename)` — genera y descarga un `.xlsx` en el navegador usando SheetJS (xlsx@0.18.5). Evita la incompatibilidad de DEFLATE/CRC en el Edge Runtime de Deno. | `core/utils/excel.utils.ts` | xlsx@0.18.5 | ✅ Estable |
| `image.utils` | `toJpeg(file, quality?)` — convierte cualquier `File` de imagen (WEBP, PNG, etc.) a JPEG usando `HTMLCanvasElement`. Retorna el mismo `File` si ya es JPEG. Usar antes de subir fotos a Supabase Storage para garantizar compatibilidad con la Edge Function `generate-student-license-pdf`. | `core/utils/image.utils.ts` | — | ✅ Estable |
| `kpi-display-value.util` | `kpiDisplayValue(value: string\|number) → string` — convierte valor KPI a string de display: number → `String(n)`, string vacío → `"—"`, string no vacío → tal cual. Función pura sin dependencias Angular. Usada por `KpiCardComponent.displayValue` computed. | `core/utils/kpi-display-value.util.ts` | `KpiCardComponent` | ✅ Estable |
| `sede-theme.utils` | `branchIdToTheme(id: number\|null) → SedeTheme` — mapea el `branchId` (tenant) al tema visual del flujo público de inscripción: `1→azul`, `2→roja`; default `azul` para id ausente/inválido. Exporta el tipo `SedeTheme` (`'azul'\|'roja'`) y la constante `DEFAULT_SEDE_THEME`. Mapping determinista espejo de las landing Astro; las ramps de color viven en `styles/themes/_public-enrollment.scss`. La decisión de orientación (sin sede) NO vive aquí, sino en el Facade. | `core/utils/sede-theme.utils.ts` | spec 0009 — wiring en `PublicEnrollmentComponent` (T4.1, pendiente) | ✅ Estable |
| `search-intents` | Diccionario de 13 intenciones de búsqueda (keyword→ruta). `INTENT_ENTRIES: IntentEntry[]` — cada entrada tiene `id`, `keywords[]`, `label`, `description`, `icon`, `path` (relativo al rol), `pathByRole?` (override por portal). `getActionResults(query, rolePrefix, role, maxResults)` → `ActionResult[]` (matching parcial por palabra). Puro: sin dependencias Angular. | `core/utils/search-intents.ts` | — | ✅ Estable |
| `sparkline.utils` | `getSparklinePoints(data: number[], w?, h?) → string` — convierte array de valores normalizados (0-1) a cadena `"x1,y1 x2,y2 ..."` para el atributo `points` de un `<polyline>` SVG. Invierte eje Y (valor 1 = arriba). Retorna `""` si hay menos de 2 puntos. Parámetros default: `w=40, h=20`. Función pura sin dependencias Angular. 8 tests. | `core/utils/sparkline.utils.ts` | `SectionHeroComponent` (slim KPIs) | ✅ Estable |
| `branch-scope.utils` | Functional Core del aislamiento por sede (fix-027 / spec 0017). `resolveBranchScope(role, userBranchId, selectedBranchId, canAccessBothBranches?) → number\|null` — resuelve el `branch_id` para filtrar queries: **admin** (o secretaria con grant `canAccessBothBranches`) respeta el selector de sede (`null` = todas); **secretaria** queda anclada a su `branchId`; si no tiene sede (misconfig) devuelve `NO_BRANCH_SCOPE` (`-1`, centinela que fuerza 0 filas en vez de exponer todas). Exporta la constante `NO_BRANCH_SCOPE`. Puro, sin Angular. 13 tests. Las facades branch-scoped lo consumen vía `getActiveBranchId()`. | `core/utils/branch-scope.utils.ts` | `AdminAlumnosFacade`, `AdminAlumnosProfesionalFacade`, `ArchivoProfesionalFacade`, `CertificacionClaseBFacade`, `CertificacionProfesionalFacade`, `InstructoresFacade` | ✅ Estable |
| `reenrollment.utils` | Functional Core de la regla de re-matrícula (fix-020). `evaluateReenrollment(existingStatuses: (string\|null\|undefined)[]) → 'block'\|'confirm'\|'allow'` — decide si un alumno puede crear una nueva matrícula en un curso a partir de los estados de sus matrículas previas EN ESE MISMO curso: hay una viva (`active`/`pending_payment`/`draft`) → `block`; solo histórico (`completed`/`cancelled`) → `confirm` (re-matrícula legítima); sin previa → `allow`. Estado desconocido → `block` (conservador). Exporta el tipo `EnrollmentStatus` y los conjuntos `BLOCKING_STATUSES`/`HISTORICAL_STATUSES`. Puro, sin Angular. 16 tests. Fuente única en frontend. **Espejo Deno** en `supabase/functions/_shared/reenrollment.ts` (mismo `evaluateReenrollment` + `reenrollmentBlockMessage`) para el flujo público: ahí `confirm` (histórico) NO se auto-permite online → deriva a la autoescuela; tests en `reenrollment.test.ts`. | `core/utils/reenrollment.utils.ts` | `EnrollmentFacade` (`evaluateCourseReenrollment`); Edge Function `public-enrollment` | ✅ Estable |
| `professional-access.utils` | Functional Core del gating de Clase Profesional (fix-028). `canAccessProfessional(role, userBranchId, selectedBranchId, branches, canAccessBothBranches?) → boolean` — decide si el módulo profesional es accesible según la **sede efectiva**: reutiliza `resolveBranchScope` (grant RF-013) → `null` ("Todas") = accesible; sede concreta solo si `hasProfessional`; misconfig (`NO_BRANCH_SCOPE`) = no. Unifica admin y secretaria (con/sin grant). Puro, sin Angular. 10 tests. | `core/utils/professional-access.utils.ts` | `SidebarComponent` (`hasProfessional()`), `professionalBranchGuard` | ✅ Estable |
| `professionalBranchGuard` | Guard de ruta (`CanActivateFn`, fix-029) que protege las rutas profesionales de **secretaría** ante el acceso por URL directa de una secretaria SIN grant cuya sede no ofrece Clase Profesional → redirige a `/app`. Admin y secretaria con grant pasan (tienen selector; `setProfessionalOnly` gobierna). Reutiliza `canAccessProfessional`. 5 tests. | `core/guards/professional-branch.guard.ts` | `app.routes.ts` (11 rutas secretaría profesional) | ✅ Estable |

## 2. Facades & Feature-Specific State
Servicios que median entre la UI (`features/`) y las APIs de datos (`SupabaseService`/`HttpClient`). Manejan el estado del dominio (`toSignal()`).

| Facade | Manejo de Dominio | Ubicación (File Path) | Dependencias | Estado |
|--------|-------------------|-----------------------|--------------|--------|
| `AuthFacade` | Autenticación con Supabase; expone `currentUser`, `isAuthenticated`, `login()`, `logout()`, `whenReady` | `core/facades/auth.facade.ts` | SupabaseService, Router | ✅ Estable |
| `DashboardFacade` | Estado global del Dashboard Admin; expone `loading`, `data`, `error`, `loadDashboardData()`. Mock data temporal. | `core/facades/dashboard.facade.ts` | HttpClient | ✅ Estable (mock) |
| `EnrollmentFacade` | Orquestador del wizard de matrícula (6 pasos). Draft progresivo con `expires_at` +24h. **Step 1:** `findUserByRut(rut)` — **1 sola query con join** `users + students(…)` (fix-020: antes 2 queries secuenciales); `prefillFromStudent(rut)` — precarga datos personales en el wizard sin persistir (blur del RUT + botón Re-matricular); `evaluateCourseReenrollment(studentId, licenseClass)` → veredicto 3-estado `block|confirm|allow` (usa `evaluateReenrollment()` de `core/utils`); `savePersonalData()` → **detecta RUT duplicado antes de persistir**: veredicto `block` muestra modal de bloqueo, `confirm` muestra modal de re-matrícula, `allow` → upsert user+student, crear enrollment draft. **Step 2:** `loadInstructors()` (filtra `vehicle_assignments.end_date IS NULL` → solo vehículo activo), `loadScheduleGrid()` (vista `v_class_b_schedule_availability`; slot IDs son TIMESTAMPTZ directo de BD, hora en `America/Santiago`), `loadPromotions()` (filtra `promotion_courses.status IN ['planned','in_progress']`), `saveAssignment()` → persiste TIMESTAMPTZ sin reconstrucción; **ordena slots cronológicamente y asigna `class_number` 1-N** (1-6 si `paymentMode='partial'`, 1-12 si `total`). `_requiredSlotCount` computed privado (derivado de `paymentMode` + `practical_hours` del curso) valida `isStep2Complete()`. **Realtime:** `loadScheduleGrid()` suscribe a canal Realtime filtrado por `instructor_id` en `class_b_sessions`; cambios disparan re-query debounced (300ms) que auto-deselecciona slots ocupados. `reset()` limpia canal. **Step 5:** `generateContract()` (Edge Function), `uploadSignedContract()` → storage bucket `documents` en path `contracts/{enrollmentId}/contract.{ext}` (path relativo guardado en `_contractFileUrl`); `openContractPdf()` → genera signed URL temporal (1h) vía `storage.createSignedUrl()` y abre en pestaña nueva. `markContractSigned({ signerName, signatureHash, signedAt })` → firma digital sin archivo físico. **Step 6:** `confirmEnrollment()` → genera número YYYY-NNNN, status → active; fire-and-forget: invoca Edge Fn `activate-student-account`. **Wizard:** signals privados (readonly), computed `canAdvance`, `studentSummary`, `sidebarSummary`, `enrollmentBasePrice`. `courseOptions` computed public (`CourseOption[]`). `reset()`, `goToStep()`, `goBack()`. | `core/facades/enrollment.facade.ts` | SupabaseService | ✅ Estable |
| `GlobalSearchFacade` | Command Palette global. Computed signals: `actionResults` (intenciones keyword→ruta), `alumnoResults` (filtro en memoria de `AdminAlumnosFacade`), `groups` (`SearchResultGroup[]`). Métodos: `setQuery(q)`, `navigate(result)`, `reset()`. Rol-aware: prefija rutas con `/app/{role}/`. Sin debounce (búsqueda in-memory). | `core/facades/global-search.facade.ts` | `AdminAlumnosFacade`, `AuthFacade`, `Router` | ✅ Estable |
| `EnrollmentDocumentsFacade` | Step 3 del wizard: documentos + storage. `uploadCarnetPhoto()` (data URL → blob → storage; preview usa dataUrl local, guarda **path relativo** en BD), `uploadDocument()` / `removeDocument()` (profesional: HVC, cédula, licencia; menores: autorización notarial). `validateHvcDate()` (RF-082.3, max 30 días). `loadDocuments()` (re-edición drafts; genera `createSignedUrl` para restaurar preview de carnet). `markDocsComplete()` → actualiza `enrollments.docs_complete`. `allRequiredUploaded()` verifica completitud. Computed `uploadedCount`. `reset()`, `getRequirements()`. | `core/facades/enrollment-documents.facade.ts` | SupabaseService | ✅ Estable |
| `EnrollmentPaymentFacade` | Step 4 del wizard: pagos + descuentos. `computePricing()` (base, deposit 50%, amountDue). `loadAvailableDiscounts()` → tabla `discounts` filtrada por curso y vigencia. `applyPredefinedDiscount()` (porcentaje o fijo). `recordPayment()` → insert en `payments`, insert en `discount_applications`, update `enrollments` (payment\_status, total\_paid, pending\_balance, discount). Computed `totalToPay`, `canConfirmPayment`. `setPaymentMethod()`, `setDiscount()`, `reset()`. | `core/facades/enrollment-payment.facade.ts` | SupabaseService | ✅ Estable |
| `PublicEnrollmentFacade` | Matrícula pública online (sin auth). Separada de `EnrollmentFacade` porque: no depende de `AuthFacade`, escribe via Edge Functions, flujo de pasos diferente. Signals: `branches`, `selectedBranch`, `flowType`, `currentStep`, `steps`, `personalData`, `instructors`, `scheduleGrid`, `selectedSlotIds`, `paymentMode`, `result`, `isSubmitting`, `isLoading`, `error`. **`carnetPhotoUrl`** computed: usa `_carnetPreviewUrl` (blob URL local vía `URL.createObjectURL`; bucket privado → no depende de URL pública). **Check anticipado de duplicados:** `savePersonalData()` invoca la acción `check-duplicate` de la Edge Function antes de avanzar el wizard; si el RUT ya tiene matrícula activa/completada o pre-inscripción pendiente para el mismo curso, setea `_error` y retorna sin avanzar; falla silenciosamente si la EF no responde (el check definitivo ocurre en el submit). **Manejo de errores:** `initiatePayment()`, `submitClaseBEnrollment()` y `submitPreInscription()` usan `data?.message ?? error?.message` para surfacear mensajes de duplicado desde la Edge Function. `reset()`/`clearSubsequentStepData()` revocan el blob URL. Métodos: `loadBranches()`, `uploadCarnetPhoto(file)`, `generateContractPreview()`, `confirmSchedule()`, `initiatePayment()` → Webpay, `submitPreInscription(options?: { skipTest? })` (Edge Fn) — **test psicológico opcional:** si el alumno elige rendirlo presencialmente, `skipTest:true` envía `skipPsychTest:true` sin respuestas y la Edge Function lo guarda como `psych_test_status='not_started'`. **Polling:** `loadScheduleGrid()` inicia polling cada 15s via Edge Function. | `core/facades/public-enrollment.facade.ts` | SupabaseService | ✅ Estable |
| `InstructoresFacade` | Gestión de instructores (admin/secretaria). SWR pattern. Query `instructors` JOIN `users` + `vehicle_assignments` (end_date IS NULL) + `vehicles`. Signals: `instructores`, `isLoading`, `error`, `branches`, `vehicles`, `isSubmitting`, `selectedInstructor`, `assignmentHistory`. **Horas mensuales:** `horasMonth`, `horasYear`, `horasMensuales` (`InstructorHoraRow[]`), `isLoadingHoras`, `isHorasCurrentMonth` (computed). Computed KPIs: `totalInstructores`, `activos`, `inactivos`, `licenciasPorVencer`. Métodos: `initialize()` (SWR), `selectInstructor(inst)`, `loadBranches()`, `loadVehicles()`, `loadAssignmentHistory(instructorId)`, `crearInstructor(payload)` → Edge Fn `create-instructor`, `editarInstructor(id, payload)` → Edge Fn `update-instructor`. **Horas:** `loadHorasMensuales()` → query `instructor_monthly_hours` por `period='YYYY-MM'`, cruza con `_instructores()` para nombre/initials; `navHorasAnterior()` / `navHorasSiguiente()` (bloqueado si mes actual). `InstructorHoraRow`: `{ instructorId, nombre, initials, practicalSessions, totalEquivalent }`. **Horario:** `horario` (`InstructorHorarioSession[]`), `isLoadingHorario`; `loadHorario(instructorId)` → query `class_b_sessions` con `status='scheduled'` y `scheduled_at >= now()`, JOIN `enrollments→students→users` para nombre alumno y `vehicles` para patente, ordenado ASC, límite 60. `InstructorHorarioSession`: `{ id, scheduledAt, classNumber, durationMin, studentName, studentInitials, vehiclePlate }`. | `core/facades/instructores.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `SecretariasFacade` | Gestión de secretarias (admin). SWR pattern. Query `users` filtrado por `roles.name='secretary'` incluyendo `rut`, `branch_id` y `phone`. Signals: `secretarias`, `isLoading`, `error`, `branches`, `isSubmitting`, `selectedSecretaria`. Computed KPIs: `totalSecretarias`, `activas`, `inactivas`. Métodos: `initialize()` (SWR), `selectSecretaria(sec)` (pre-carga para drawers Ver/Editar), `loadBranches()` (lazy, idempotente), `crearSecretaria(payload)` → Edge Fn `create-secretary` (crea usuario en Auth con password = RUT sin dígito verificador + INSERT en `users` con `first_login=true`; rollback borra auth user si INSERT falla), `editarSecretaria(id, payload)` → Edge Fn `update-secretary` (UPDATE en `users`; si `email !== currentEmail` llama `updateUserById` en Auth primero). `SecretariaTableRow` incluye `rut`, `firstName`, `paternalLastName`, `maternalLastName`, `branchId`, `phone`, `email` para edición. `EditarSecretariaPayload` = `{ nombre, phone, branchId, active, email, currentEmail }`. | `core/facades/secretarias.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `AuditoriaFacade` | Log de auditoría de secretarias (solo admin). SWR pattern. Query `audit_log` JOIN `users` filtrado por `roles.name='secretary'`. Signals: `logs` (`AuditLogRow[]`), `isLoading`, `isExporting`, `error`, `secretarias` (`SecretariaOption[]`), `totalCount`, `currentPage`. Computed: `totalPages`, `paginationStart`, `paginationEnd`. Métodos: `initialize()` (SWR, carga secretarias + logs), `setFilters(partial)` (aplica filtros y resetea a página 1), `clearFilters()`, `setPage(n)`, `exportar(format: 'excel'\|'pdf')` → invoca Edge Fn `generate-audit-report` con filtros activos + branch_id; Excel via `downloadExcel()`; PDF via Blob+URL. Paginación server-side (25 por página). Filtros: `fechaDesde`, `fechaHasta`, `secretariaId`, `accion` ('Crear'→INSERT, 'Actualizar'→UPDATE, 'Eliminar'→DELETE), `modulo` (mapeado a entidades BD via `ENTITY_MODULE_MAP`). | `core/facades/auditoria.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `RelatoresFacade` | Gestión de relatores de Clase Profesional (admin/secretaria). SWR pattern. Query directa a tabla `lecturers` (sin FK a `users`). Signals: `relatores` (`RelatorTableRow[]`), `isLoading`, `error`, `isSubmitting`, `selectedRelator`. Computed KPIs: `totalRelatores`, `activos`, `inactivos`. Métodos: `initialize()` (SWR), `selectRelator(rel)`, `crearRelator(payload)` → INSERT en `lecturers`, `editarRelator(id, payload)` → UPDATE en `lecturers`. `RelatorTableRow` incluye `rut`, `firstName`, `paternalLastName`, `maternalLastName`, `email`, `phone`, `specializations`, `estado`, `initials`. | `core/facades/relatores.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `PromocionesFacade` | Gestión de promociones profesionales (admin/secretaria). SWR pattern. Query `professional_promotions` JOIN `promotion_courses` + `promotion_course_lecturers` + `lecturers` + `courses`. **Enrolled students** contados en runtime desde tabla `enrollments` (excluye cancelled/draft) — ya no usa columna `enrolled_students`. **Progreso** (`currentDay`) computado desde `start_date` contando días Lun-Sáb (max 30). Signals: `promociones` (`PromocionTableRow[]`), `isLoading`, `error`, `isSubmitting`, `selectedPromocion`, `relatoresDisponibles` (`RelatorOption[]`), `professionalCourses`, `cursoStudents` (`Record<pcId, PromocionAlumno[]>`), `isLoadingStudents`. Computed KPIs: `totalPromociones`, `planificadas`, `enCurso`, `canceladas` (promotions with status='finished' excluded — managed in ArchivoFacade). Métodos: `initialize()` (SWR), `selectPromocion(promo)` (auto-carga students), `loadStudentsForPromocion(promo)`, `loadRelatoresDisponibles()` (lazy), `loadProfessionalCourses()` (lazy), `crearPromocion(payload)` → INSERT `professional_promotions` + `promotion_courses` + `promotion_course_lecturers` + **`fetchHolidaysInRange()`** (llama `apis.digital.gob.cl/fl/feriados/{año}`, marca sesiones de feriados como `cancelled` via `cancelHolidaySessions()`, falla silenciosamente si API no disponible), `editarPromocion(id, payload)` → UPDATE `professional_promotions` (solo status). | `core/facades/promociones.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `AdminHorariosFacade` | Generador y configuración de la grilla horaria base (`schedule_blocks`). Permite sobreescribir la configuración del catálogo operacional de la sede para uso de nuevos matriculados. Query a `courses` con `type = 'class_b'`. Signals: `courses`, `isLoading`, `isSaving`. Métodos: `loadCourses(branchId)`, `updateScheduleBlocks(courseIds, blocks)`. | `core/facades/admin-horarios.facade.ts` | SupabaseService, ToastService | ✅ Implementado |
| `AsistenciaProfesionalFacade` | Gestión de sesiones y asistencia de Clase Profesional (admin/secretaria). SWR pattern. Query `professional_theory_sessions` + `professional_practice_sessions` por `promotion_course_id`. Cuenta asistencia por sesión desde `professional_theory_attendance` / `professional_practice_attendance`. Signals: `promociones`, `cursos`, `sesiones`, `selectedPromocionId`, `selectedCursoId`, `weekOffset`, `isLoading`, `error`, `selectedSesion`, `asistenciaAlumnos`, `isLoadingAsistencia`, `isSaving`, `resumenAlumnos` (`ResumenAlumnoAsistencia[]`), `isLoadingResumen`. **KPIs computed:** `alumnosMatriculados` (inscritos del curso), `pctAsistenciaSemanal` (% sobre sesiones completadas de la semana visible), `pctAsistenciaTotal` (% acumulado del curso, solo completadas), `sesionesCanceladas` (total del curso). `weekDays` (`WeekDay[]` L-S), `isCurrentWeek`, `weekLabel`. Métodos: `initialize()` (SWR), `selectPromocion(id)`, `selectCurso(id)` (carga sesiones + dispara `fetchResumenAlumnos()`), `nextWeek()`/`prevWeek()`/`goToCurrentWeek()`, `selectSesion(sesion)`, `guardarAsistencia(registros)` (refresca sesiones + resumen en paralelo post-guardado), `editarSesion(sesion, payload)`, `fetchResumenAlumnos(cursoId)` (solo sesiones `completed`; calcula `pctTeoria`, `pctPractica`, `pctAsistencia` por alumno cruzando `professional_theory_attendance` + `professional_practice_attendance`). **ResumenAlumnoAsistencia:** `teoriaAsistida/Total`, `pctTeoria`, `practicaAsistida/Total`, `pctPractica`, `totalAsistida/Sesiones`, `pctAsistencia`. | `core/facades/asistencia-profesional.facade.ts` | SupabaseService, ToastService | ✅ Estable |
| `CuadraturaFacade` | Cuadratura diaria de caja. Patrón SWR. Signals: `pagosHoy` (`IngresoRow[]`), `gastosHoy` (`EgresoRow[]`), `cajaYaCerrada`, `cierreHoy`, `isLoading`, `isSaving`, `isExporting`, `error`, `fondoInicial` (fijo 50.000). Computed: `ingresosEfectivoHoy` (sum cash_amount), `otrosIngresosHoy` (transfer+card), `totalIngresosHoy`, `totalEgresosHoy`, `saldoTeoricoEfectivo` (fondoInicial + efectivo - egresos). Métodos: `cerrarCaja(CierrePayload)` → INSERT en `cash_closings`. `registrarEgreso(EgresoFormData)` → INSERT en `expenses` (tipo gasto) o `instructor_advances` (tipo anticipo) + refresh silencioso. `refresh()` → recarga pública para Smart Components externos (ej. tras guardar pago vía PagosFacade). `exportar(format: 'excel'\|'pdf')` → invoca Edge Fn `generate-cash-closing-report` con `{ format, date: hoy, branch_id }`; Excel descarga via `downloadExcel()`; PDF via Blob+URL. con desglose de billetes/monedas, arqueo_amount, difference, status='closed'. Filtra por `branch_id` del usuario autenticado. | `core/facades/cuadratura.facade.ts` | SupabaseService, AuthFacade, ToastService | ✅ Estable |
| `HistorialCuadraturasFacade` | Historial de cierres de caja (`cash_closings`). **Navegación mensual:** signals `mesActual` (1-12) y `anioActual`; métodos `mesAnterior()`, `mesSiguiente()`, `volverAHoy()` cambian el mes y recargan automáticamente. `cargarHistorial()` (sin params) filtra `date` entre `yyyy-mm-01` y `yyyy-mm-{lastDay}` + JOIN `users(first_names, paternal_last_name)`. **Exportación mensual:** `exportarMes(format: 'excel'\|'pdf'): Promise<void>` → Excel client-side vía `downloadExcel()` con resumen del mes (fecha/cajero/ingresos/egresos/saldos/estado); PDF invoca Edge Fn `generate-cash-history-report` con `{ month, year, branch_id }` y descarga el binario PDF. **Exportación de cierre individual:** `exportarCierre(format: 'excel'\|'pdf'): Promise<void>` → invoca Edge Fn `generate-cash-closing-report` con `cierreSeleccionado().fecha`; mismo output que `CuadraturaFacade.exportar()`. Signal `isExporting` (compartido por ambas exportaciones). `seleccionarCierre(cierre)` establece el `cierreSeleccionado` para el drawer de detalle. Mapeo DTO→UI: `balance`=saldoSistema, `arqueo_amount`=saldoFisico, denomination fields (`qty_bill_*`, `qty_coin_*`), `notes`. `estadoDiferencia` derivado (balanced/surplus/shortage). | `core/facades/historial-cuadraturas.facade.ts` | SupabaseService, AuthFacade, BranchFacade, ToastService, downloadExcel | ✅ Estable |
| `AnticiosFacade` | Cuenta corriente de anticipos a instructores (RF-038). **SWR pattern.** 2 queries paralelas: `instructor_advances` (JOIN `instructors→users` — todos los registros, order date DESC) + `instructors` activos (para cuenta corriente de instructores sin anticipos). **Signals:** `historial` (`AnticipoHistorial[]`), `cuentaCorriente` (`AnticipoCuentaCorriente[]`), `instructores` (`InstructorOption[]`), `isLoading`, `isSaving`, `error`. **KPIs computed:** `totalPendiente` (sum pending), `instructoresConSaldo` (count únicos con saldo > 0), `totalHistorico` (sum all), `totalDescontado` (sum discounted/deducted). `cuentaCorriente` agrupa por instructor con `saldoPendiente`, `anticiposTotales`, `ultimoAnticipo`, `tipoLabel` (theory→Teórico, practice→Práctico, both→Teórico y Práctico). `historial` lista todos ordenados por fecha DESC con `motivo` derivado de `description` o `reason`. **Mutaciones:** `registrarAnticipo(RegistrarAnticipoPayload)` → INSERT `instructor_advances` status='pending' + `refreshSilently()`. Helpers exportados: `tipoLabel(tipo)`, `mapStatus(raw)`. | `core/facades/anticipos.facade.ts` | SupabaseService, AuthFacade, ToastService | ✅ Estable |
| `LiquidacionesFacade` | Liquidaciones mensuales de instructores. **Navegación mensual:** signals `mesActual` (1-12) y `anioActual`; métodos `mesAnterior()`, `mesSiguiente()` cambian el mes y recargan. **4 queries paralelas:** `instructors` (JOIN users con branch_id), `instructor_monthly_hours` (selecciona `instructor_id, practical_sessions, total_equivalent`; filtrado por mes+año — poblado automáticamente por trigger `trg_class_b_sessions_monthly_hours`), `instructor_advances` (filtrado por rango de fechas del mes), `instructor_monthly_payments` (filtrado por mes+año). **Maps O(1):** `hoursMap` (total_equivalent), `sessionsMap` (practical_sessions), `advancesMap` (suma por instructor), `paymentsMap`. Construye `LiquidacionRow[]` con `getInitials()` y `getAvatarColor()` (hash 8 colores). `LiquidacionRow` incluye `practicalSessions` (conteo directo de clases) y `totalHours` (= practical_sessions × 0.75 h). `amountPerHour` desde payment existente o default 5.000. `finalPaymentAmount = max(0, base - advances)`. `status` = `'paid'` si existe registro en `instructor_monthly_payments`. **`registrarPago(row, payload)`:** upsert en `instructor_monthly_payments` con `onConflict: 'instructor_id,period'`; luego UPDATE `instructor_advances.status='discounted'` para los del mes. **`deshacerPago(row)`:** DELETE en `instructor_monthly_payments` + UPDATE advances a `status='pending'`. **`kpis`** computed: totalNomina, totalAnticipos, totalPagados, totalInstructores. **`exportar(format: 'excel'\|'pdf')`** → invoca Edge Fn `generate-payroll-report` con `{ format, month, year, branch_id }`; Excel via `downloadExcel()`; PDF via Blob+URL. Signal `isExporting`. Filtra por `branchId` del usuario autenticado. | `core/facades/liquidaciones.facade.ts` | SupabaseService, AuthFacade, BranchFacade, ToastService | ✅ Estable |
| `ReportesContablesFacade` | Reportes RF-030/RF-031: resumen financiero y Total Neto por rango de fechas. **Filtros:** `FiltrosReporte` (`rango: RangoReporte`, `desde`, `hasta`). **SWR pattern.** 3 queries paralelas: `payments!inner(enrollments(branch_id,license_group))`, `expenses` (operacionales, Sec+Admin), `fixed_expenses` (gastos fijos, solo Admin). Los gastos fijos se concatenan con los operacionales antes de `buildReporte` para que KPIs y categorías reflejen el total real. **Signals:** `kpis`, `ingresosCategoria`, `gastosCategoria`, `evolucionMensual`, `detalleDiario`, `diasConMovimientos`, `escuela`, `gastosFijos` (`GastoFijoRow[]`), `isLoading`, `isExporting`, `isRegistrando`, `error`. `_effectiveBranchId` computed: secretaria usa `AuthFacade.currentUser().branchId`; admin usa `BranchFacade.selectedBranchId()` (null = ambas sedes). **Métodos:** `initialize()` (SWR), `aplicarFiltros(filtros)`, `registrarGastoFijo(payload: RegistrarGastoFijoPayload)→Promise<boolean>` (INSERT `fixed_expenses` + `fetchReporte()`), `exportar(format)`. | `core/facades/reportes-contables.facade.ts` | SupabaseService, AuthFacade, BranchFacade, ToastService | ✅ Estable |
| `reportes-contables.utils` | **Functional Core** para RF-030/RF-031. Funciones puras (sin Angular): `filterPaymentsByBranch(rows, branchId)`, `computeKpis(payments, expenses)→ReporteKpis`, `computeIngresosCategoria(payments, showBranch)→CategoriaIngreso[]`, `computeGastosCategoria(expenses)→CategoriaGasto[]`, `computeEvolucionMensual(payments, expenses)→EvolucionMensual[]`, `computeDetalleDiario(payments, expenses)→DetalleDiario[]`, `buildReporte(payments, expenses, escuela, branchId)→ReporteContable`, **`mapSingularSaleToPaymentRow(dto)→PaymentRow`** (fix-016 AC9 — normaliza cobros de cursos singulares a categoría `standalone` → "Cursos Singulares"). Tipos internos exportados: `PaymentRow`, `ExpenseRow`, `SingularSaleReportDto`. Colores de barra: `class_b→var(--state-info)`, `professional→#7c3aed`, `complement→var(--state-warning)`, `special_service→var(--state-success)`, `standalone→#0d9488`. Labels gastos (operacionales + fijos): fuel→Bencina, rent→Arriendo, cleaning→Aseo, materials→Materiales, salary→Sueldos, utility→Servicios Básicos, insurance→Seguros, repair→Reparaciones, other→Otros. 34 tests unitarios. | `core/utils/reportes-contables.utils.ts` | — | ✅ Estable |
| `CursosSingularesFacade` | Gestión de Cursos Singulares RF-035 (SENCE, Grúa, etc). SWR pattern. Branch-scoped. **Cursos:** signals `cursos`, `selectedCurso`, `inscriptos`, `isLoading`, `isLoadingInscriptos`, `isSaving`, `error`. Computed `kpis` (cursosActivos, totalInscritos, **ingresosCobrados** y **porCobrar** — dinero real, fix-016). Métodos: `initialize()` (SWR + `_lastBranchId` — cambio de sede recarga con skeleton), `selectCurso(curso)` (carga inscriptos), `loadInscriptos(cursoId)` (con descuentos; error visible, no lista vacía), `crearCurso(form)` (**no persiste `status` en BD** — `computeEstado` re-deriva desde fecha al leer; exige `form.branchId` y persiste `registered_by`), `marcarCursoFinalizado(cursoId)`, `cancelarCurso(cursoId)`, `marcarEnrollmentPagado(enrollmentId)` (UPDATE `payment_status='paid'` + **`amount_paid = precio − discount_amount`** + `paid_at=now`; llama `loadInscriptos()` inmediatamente). **Wizard de inscripción:** signals `wizardStep` (1\|2), `isSearching`, `studentSearch` (`SingularStudentSearch\|null` — ficha completa incl. birth_date/gender/address), `studentNotFound`. Métodos: `resetWizard()`, `searchByRut(rut)`, `savePersonalData(form, branchId)` (**solo local**; valida birth_date requerida + edad ≥ 17), `inscribirAlumno(cursoId, payment)` (**toda la escritura BD:** verifica duplicado y cupos ANTES de escribir → upsert user → upsert student → INSERT con `discount_amount`/`discount_reason`/`paid_at`/`registered_by`; toast + reload), `goToWizardStep(step)`. **Errores sanitizados** vía `setSafeError()` → `toFriendlyDbMessage()`. **Patrón seguro:** ningún write a BD hasta confirmar en step 2. | `core/facades/cursos-singulares.facade.ts` | SupabaseService, BranchFacade, AuthFacade, ToastService | ✅ Estable (fix-016) |
| `PagosFacade` | Gestión de Pagos: KPIs + deudores + historial + distribución de métodos + estado de cuenta por matrícula. **Signals globales:** `ingresosHoy`, `ingresosMes`, `boletasMes`, `pagosPendientesTotales`, `alumnosConDeuda` (`AlumnoDeudor[]`), `pagosRecientes` (`PagoReciente[]`, últimos 50), `metodosPagoMes` (`MetodoPago[]`), `totalDeudores` (computed), `isLoading`, `error`. **Signals de detalle:** `enrollmentSeleccionado` (number\|null), `estadoCuentaResumen` (`EstadoCuentaResumen\|null`), `estadoCuentaHistorial` (`EstadoCuentaHistorialItem[]`), `isLoadingDetalle`. **Signals de reporte:** `isGeneratingReport` (boolean). **Métodos de detalle:** `seleccionarEnrollment(id)`, `cargarEstadoCuenta(id)` (Promise.all resumen+historial). **Patrón SWR:** `initialize()` → primera carga con skeleton, revisitas refrescan en background. Helpers puros: `resolveMetodo()`, `METODOS_CONFIG` (color/icono por canal). `registrarNuevoPago()`: INSERT payments → UPDATE enrollments → `refreshSilently()`. `generarReporte({ startDate, endDate, branchId })` → invoca Edge Fn `generate-payment-report`, descarga PDF on-demand con toast de éxito/error. | `core/facades/pagos.facade.ts` | SupabaseService, ToastService | ✅ Estable |

| `StudentEnrollmentContextFacade` | Fuente compartida del enrollment activo en el portal del alumno. Patrón `BranchFacade`: los 3 facades del portal lo inyectan para saber qué enrollment mostrar. **Signals:** `enrollments` (`EnrollmentTab[]`), `activeEnrollmentId` (number\|null). **Métodos:** `initialize(dbId)` (idempotente, carga todos los enrollments active/completed del alumno, setea `_activeId` al más reciente), `setActive(id)`, `reset()` (para logout). Labels de tabs: `"Clase B · #0010"` o `"Prof. A2 · #0012"`. Usado por `StudentHomeFacade`, `StudentClasesFacade`, `StudentHorarioFacade`. | `core/facades/student-enrollment-context.facade.ts` | SupabaseService | ✅ Estable |
| `StudentPaymentFacade` | Wizard de pago de segunda mitad Clase B (alumno autenticado). **SWR pattern** (`_initialized` guard + `refreshSilently()`). Signals: `step` (1–3), `status` (`StudentPaymentStatus\|null`), `payments` (`StudentPaymentHistoryItem[]`), `scheduleGrid`, `selectedSlotIds`, `isLoading`, `isSubmitting`, `error`. Computed: `enrollment`, `instructor`, `selectedCount`, `requiredCount` (6), `selectionComplete`. Métodos: `initialize()` (SWR: primera carga con skeleton; re-visitas refrescan en background sin skeleton), `goToSchedule()` (avanza paso 2 + carga grilla instructor; **restaura selección previa** desde `myHeldSlotIds` que devuelve la edge function si el usuario tenía holds activos de su sesión), `toggleSlot(id)` (máx 1/día, máx 6 total), `isSlotSelectable(id)`, `reserveSlotsAndAdvance()` (reserva temporal + avanza paso 3), `backToSchedule()`/`backToSummary()` (libera holds), `initiatePayment()` (crea sesiones `reserved` + inicia Webpay; redirige `window.location.href`), `confirmPayment(tokenWs)` → `StudentPaymentResult` (confirma commit Webpay, activa sesiones, registra pago; **rota el session token** en localStorage tras éxito para que el próximo flujo empiece limpio), `resetError()`. **Session token:** persistido en `localStorage` bajo la clave `spt_session_token` (`restoreOrCreateToken()` al instanciar); sobrevive recargas de página para que los `slot_holds` de intentos abandonados sean reconocidos como propios y no bloqueen slots al usuario. | `core/facades/student-payment.facade.ts` | SupabaseService | ✅ Estable |
| `AsistenciaClaseBFacade` | Control de Asistencia Clase B (admin/secretaria). SWR pattern. **Queries reales a Supabase.** Signals: `kpis` (`AsistenciaClaseBKpis\|null`), `clasesTeorias` (`ClaseTeoricoRow[]`), `clasesPracticas` (`ClasePracticaRow[]`), `alertas` (`AlertaFaltaConsecutiva[]`), `selectedDate` (string YYYY-MM-DD, default hoy), `teoriaDrawerClase` (`ClaseTeoricoRow\|null`), `teoriaAlumnos` (`TeoriaAlumnoAsistencia[]`), `alumnosElegibles` (`TeoriaAlumnoElegible[]`), `isLoading`, `isSaving`, `isLoadingTeoriaAlumnos`, `isLoadingElegibles`, `error`. Computed: `instructores` (`InstructorOption[]`, deduplicado de prácticas). **Filtro de sede:** `setBranchFilter(id\|null)`. `setDate(iso)` cambia fecha y recarga datos. **KPIs computados** desde datos reales (tasaAsistencia, inasistenciasHoy, totalClasesHoy, alumnosEnRiesgo, horariosEliminados). Métodos: `initialize()` (SWR), `reload()`, `markAttendance()` (upsert `class_b_practice_attendance`), `justifyAbsence()` (update status excused), `removeSchedule()` (cancel scheduled sessions), `reactivateSchedule()`, `sendReminder()`, `openTeoriaDrawer(clase)` (query `class_b_theory_attendance` JOIN students→users), `closeTeoriaDrawer()`, `saveTeoriaZoomLink(sessionId, url)` (UPDATE `class_b_theory_sessions`), `saveTeoriaAsistencia(sessionId, registros[])` (UPSERT `class_b_theory_attendance`), `loadAlumnosElegibles(branchId)` (enrollments activas para drawer agendar), `crearClaseTeorica(payload)` (INSERT `class_b_theory_sessions` + `class_b_theory_attendance`), `clearElegibles()`. **Tablas consultadas:** `class_b_theory_sessions`, `class_b_theory_attendance`, `class_b_sessions`, `class_b_practice_attendance`, `enrollments`, `students`, `users`, `instructors`. | `core/facades/asistencia-clase-b.facade.ts` | SupabaseService, ToastService, AuthFacade, BranchFacade | ✅ Estable |
| `AdminAlumnosFacade` | Listado de alumnos activos (admin/secretaria). SWR pattern + Realtime (`students` + `enrollments`). Signals: `alumnos` (`AlumnoTableRow[]`), `isLoading`, `isExporting`, `isArchiving`, `error`, `drawerMode`. Computed KPIs: `totalAlumnos`, `activos`, `conDeuda`, `alumnosPorVencer`. **Multi-enrollment:** `AlumnoTableRow.nroExpedientes: string[]` (todos los números de expediente del alumno) y `cursos: EnrollmentCurso[]` (todos los cursos con `licenseGroup`). La tabla muestra chips/badges por cada enrollment. Métodos: `initialize()` (SWR), `loadAlumnos()`, `setDrawerMode('zoom'\|'asistencia')`, `setupRealtime()`, `destroyRealtime()`, `checkHistorial(studentId)`, `archivarAlumno(studentId)`, `restaurarAlumno(studentId)`, `setTrashView(bool)`, `exportAlumnos(...)`, `exportarFicha(enrollmentId)`. Filtra por `BranchFacade.selectedBranchId()`. | `core/facades/admin-alumnos.facade.ts` | SupabaseService, BranchFacade, ToastService | ✅ Estable |
| `AdminAlumnoDetalleFacade` | Detalle completo del alumno (admin/secretaria). SWR pattern + Realtime (`class_b_sessions`). **Multi-enrollment:** `enrollmentSummaries` (`EnrollmentSummary[]`) expone todos los enrollments del alumno; `selectEnrollment(id)` cambia el enrollment activo y recarga progreso/clases/pagos sin recargar info personal. El template muestra tabs cuando `enrollmentSummaries.length > 1`. `AlumnoDetalleUI.enrollments` incluye array completo de summaries. **Signals:** `alumno` (`AlumnoDetalleUI\|null`), `enrollmentSummaries`, `clasesPracticas` (`ClasePracticaUI[]`), `historialPagos`, `progresoPractico`, `progresoTeorico`, `certPdfPath`, `licensePdfPath`, `isGeneratingLicense`, `contractGeneratedPath`, `contractSignedPath`, `registrationChannel`, `isLoading`, `error`. **Clase Profesional:** `progresoTeoriaProf`, `progresoPracticaProf`, `notaPromedioProf`, `elegibilidadProf`, `elegibleProf`. **Métodos:** `initialize(studentId)`, `selectEnrollment(id)`, `setReprogramarTarget()`, `loadInstructores()`, `loadScheduleGrid()`, `reprogramarClase()`, `generarCarnet()`, `uploadContract()`, `downloadContract()`, `generateContract()`. | `core/facades/admin-alumno-detalle.facade.ts` | SupabaseService, ToastService, DmsViewerService | ✅ Estable |
| `AsistenciaClaseBFacade` | Control de Asistencia Clase B (admin/secretaria). SWR pattern. **Queries reales a Supabase.** Signals: `kpis` (`AsistenciaClaseBKpis\|null`), `clasesTeorias` (`ClaseTeoricoRow[]`), `clasesPracticas` (`ClasePracticaRow[]`), `alertas` (`AlertaFaltaConsecutiva[]`), `selectedDate` (string YYYY-MM-DD, default hoy), `teoriaDrawerClase` (`ClaseTeoricoRow\|null`), `teoriaAlumnos` (`TeoriaAlumnoAsistencia[]` — keyed by `studentId`, sin `enrollmentId`), `alumnosElegibles` (`TeoriaAlumnoElegible[]`), `selectedPractica` (`ClasePracticaRow\|null`), `emailResult` (`{ sent: number; errors: string[] } \| null` — resultado del último envío de Zoom por correo), `isLoading`, `isSaving`, `isLoadingTeoriaAlumnos`, `isLoadingElegibles`, `error`. Computed: `instructores` (`InstructorOption[]`, deduplicado de prácticas). **Filtro de sede:** `setBranchFilter(id\|null)`. `setDate(iso)` cambia fecha y recarga datos. **`AsistenciaClaseBKpis`:** `tasaAsistencia` (%), `tasaAsistenciaTrend`, `inasistenciasHoy`, `totalClasesHoy`, `clasesEnCurso` (status=`en_curso` ahora mismo), `pendientesPorIniciar` (status=`pendiente` + alumno asignado + hora ya pasó). KPIs calculados en `computeKpis()` sobre los datos del día sin queries adicionales. Métodos: `initialize()` (SWR), `reload()`, `markAttendance()`, `justifyAbsence()`, `removeSchedule()`, `reactivateSchedule()`, `sendReminder()`, `openTeoriaDrawer(clase)`, `closeTeoriaDrawer()`, `saveTeoriaZoomLink(claseId, url)` (UPDATE `class_b_theory_sessions` + llama `callSendZoomEmail()` a los alumnos ya cargados en `_teoriaAlumnos`), `saveTeoriaAsistencia(claseId, registros)→Promise<boolean>` (upsert por `student_id`; retorna `false` en error), `updateTeoriaInfo(claseId, tema, startTime, endTime)→Promise<boolean>` (UPDATE `class_b_theory_sessions.topic/start_time/end_time`; sincroniza `_clasesTeorias` y `_teoriaDrawerClase`), `loadAlumnosElegibles()`, `crearClaseTeorica(payload)` (INSERT `class_b_theory_sessions` + `class_b_theory_attendance`; si `payload.zoomLink` existe, llama `callSendZoomEmail()` y usa el resultado para enriquecer el toast de éxito), `clearElegibles()`, `clearEmailResult()`, `selectPractica(row\|null)`, `startClass(sessionId, kmStart, vehicleId?)`, `finishClass(FinishClassPayload)`. **Email Zoom (`callSendZoomEmail`):** método privado que invoca Edge Fn `send-zoom-email` vía `supabase.client.functions.invoke()`; actualiza `_emailResult` con `{ sent, errors[] }`; falla silenciosamente (no bloquea el flujo principal). **`ClasePracticaRow` extendido:** +`studentId`, +`classNumber`, +`kmStart`, +`vehiclePlate`, +`vehicleBrand`, +`vehicleModel`, +`vehicleId`, +`vehicleCurrentKm`, +`horaInicioReal`, +`horaFinReal`. **`startClass()`** sincroniza `_selectedPractica`. **`loadVehiclesByBranch(branchId)`** → `vehiclesPorSede`. **`finishClass()`** actualiza `vehicles.current_km`. **`FinishClassPayload`** en `core/models/ui/asistencia-clase-b.model.ts`. | `core/facades/asistencia-clase-b.facade.ts` | SupabaseService, ToastService, AuthFacade, BranchFacade | ✅ Estable |
| `AdminPreInscritosFacade` | Gestión de pre-inscripciones Clase Profesional (admin/secretaria). SWR pattern. Query `professional_pre_registrations` JOIN `users` + `branches` + evaluator `users` + `enrollments!converted_enrollment_id(id, number)`. Signals: `preInscritos` (`PreInscritoTableRow[]`), `isLoading`, `isSaving`, `error`, `selected`, `promociones`. Computed KPIs: `total`, `pendientesTest`, `aprobados`. **Flujo de estados:** `approved` → `completarMatricula()` → `pending_contract` (matrícula creada, contrato pendiente; alumno permanece visible en lista) → `uploadSignedContract()` → `enrolled` (alumno desaparece de la lista). Métodos: `initialize()` (SWR), `select(row)`, `evaluarTest(payload)`, `completarMatricula(payload)` → retorna `{ enrollmentId, enrollmentNumber } | null` — crea `students` + activa `users` + INSERT `enrollments` + sube documentos + UPDATE pre-inscripción a `pending_contract` + actualiza `_selected`. `generateContract(enrollmentId)` → Edge Fn `generate-contract-pdf`, retorna `pdfUrl | null`. `uploadSignedContract(enrollmentId, file)` → sube a storage + upsert `digital_contracts` + `contract_accepted=true` + UPDATE pre-inscripción a `enrolled` + `refreshSilently()`. `cargarPromocionesParaLicencia(licencia)` (lazy, idempotente), `resetPromocionesCache()`. Ruta: `/app/admin/alumnos/pre-inscritos`. | `core/facades/admin-pre-inscritos.facade.ts` | SupabaseService, BranchFacade, AuthFacade | ✅ Estable |

| `CertificacionClaseBFacade` | Certificación Clase B (admin/secretaria). SWR pattern. **Query:** `enrollments` (certificate_enabled=true, license_group='class_b', status IN active/completed) + JOIN `v_student_progress_b` para `pct_theory_attendance` + `last_practice_session` + `certificates(created_at)` + `class_b_sessions` (cuenta prácticas completadas con `evaluation_grade IS NOT NULL`). **`fetchData` secuencial:** `fetchAlumnos()` → `fetchLog()` (fetchLog filtra por cert IDs de `_alumnos()` evitando filtros PostgREST anidados no soportados). **Folio:** usa año de `cert.created_at`, no `new Date()`. **`certificadoFolio`** solo se muestra cuando el PDF existe (alineado con `certificadoStatus`). **`clasesCompletadas`** contada en runtime desde `class_b_sessions`. Signals: `alumnos`, `log`, `isLoading`, `error`, `generatingId`, `sendingEmailId` (number\|null), `sendingMasivo` (boolean), `isExporting` (boolean), `isGeneratingPendientes` (boolean). Computed: `kpis`. Métodos: `initialize()` (SWR), `reload()`, `generarCertificado(enrollmentId)` → Edge Fn devuelve `{ pdfUrl (signed 1h), pdfPath }`, persiste path en `certificate_b_pdf_url`, abre DmsViewer; `verCertificado(storagePath, nombre)` → signed URL; `descargarPdf(enrollmentId)` → re-invoca Edge Fn; `enviarEmail(enrollmentId)` → Edge Fn `send-certificate-email`; `enviarEmailsMasivo()` → itera secuencialmente, toast resumen; `generarPendientes()` → itera `_alumnos()` con `certificadoStatus==='pendiente'`, llama `generate-certificate-b-pdf` por cada uno secuencialmente (reutiliza `_generatingId` como cursor), toast resumen de generados/errores + `refreshSilently()`; `exportar()` → invoca Edge Fn `export-certificates-zip` (pasa `branch_id` si aplica), recibe Blob ZIP, dispara descarga via link temporal, toast éxito/error. `CertificacionAlumnoRow` incluye campo `email`. Filtra por `BranchFacade.selectedBranchId()`. | `core/facades/certificacion-clase-b.facade.ts` | SupabaseService, BranchFacade, ToastService | ✅ Estable |
| `CertificacionProfesionalFacade` | Certificación Clase Profesional (admin/secretaria). SWR pattern. **Selección en cascada:** `initialize()` carga promociones finalizadas + log; `selectPromocion(id\|null)` carga cursos; `selectCurso(id\|null)` carga alumnos con elegibilidad. **Queries:** `professional_promotions` (status='finished', order start_date DESC) → `promotion_courses` JOIN `courses` → `enrollments` JOIN `students→users` + `certificates(created_at)` + asistencia + notas. **Bug fixes:** `_alumnosFetchToken` previene race condition en `reload()` vs `fetchAlumnos()` en vuelo. `promocion`/`fechaInicio` derivados de `_promociones()` sin query extra. **Folio:** año desde `cert.created_at`. **`certificadoFolio`** solo visible cuando PDF existe. **`fetchLog`** usa cert IDs de `_alumnos()` cuando disponibles; fallback a `certificates.type='professional'` en carga inicial. Tras `generarCertificado()`: await `fetchAlumnos()` antes de `fetchLog()` para incluir el nuevo cert ID. **Elegibilidad** (2 criterios bloqueantes): teoría ≥75%, pago correcto (pending_balance≤0) → habilita botón. Nota promedio es **informativa** (se muestra en UI pero no bloquea el certificado). Práctica <100% → confirmation prompt inline. Filtra `enrollments.branch_id` por `BranchFacade.selectedBranchId()`. **Signals:** `promociones`, `cursos`, `selectedPromocionId`, `selectedCursoId`, `alumnos`, `log`, `isLoading`, `isLoadingAlumnos`, `error`, `generatingId`, `sendingEmailId` (number\|null), `sendingMasivo` (boolean), `isExporting` (boolean), `isGeneratingPendientes` (boolean). **Computed:** `kpis`. **Métodos:** `initialize()`, `reload()`, `selectPromocion()`, `selectCurso()`, `generarCertificado()`, `verCertificado()`, `enviarEmail()`, `enviarEmailsMasivo()`, `generarPendientes()` → filtra `alumnos` con `elegible===true && certificadoStatus==='pendiente'`, llama `generate-certificate-professional-pdf` por cada uno, toast resumen + `fetchAlumnos(cursoId)` + `fetchLog()`; `exportar()`. | `core/facades/certificacion-profesional.facade.ts` | SupabaseService, BranchFacade, ToastService, DmsViewerService | ✅ Estable |
| `ArchivoFacade` | Archivo histórico de Clase Profesional (admin/secretaria). **Solo lectura.** SWR pattern. Carga únicamente `professional_promotions` con `status='finished'`. **Selección en cascada:** `initialize()` → `selectPromocion(id\|null)` → `selectCurso(id\|null)`. Al seleccionar curso ejecuta 5 queries en paralelo: enrollments, theory/practice sessions (completadas), theory/practice attendance, module grades → consolida `ArchivoAlumnoRow[]` con asistencia (X/Y + %) + 7 notas de módulo + promedio + estado (aprobado: teoría ≥75% AND promedio ≥75). **Signals:** `promociones` (`ArchivoPromocionOption[]`), `cursos` (`ArchivoCursoOption[]`), `selectedPromocionId`, `selectedCursoId`, `alumnos` (`ArchivoAlumnoRow[]`), `isLoading`, `isLoadingAlumnos`, `error`. **Computed:** `kpis` (`totalAlumnos, aprobados, reprobados, pctAprobacion`), `moduleNames` (nombres de los 7 módulos según licenseClass del curso activo). Filtra por `BranchFacade.selectedBranchId()`. | `core/facades/archivo-profesional.facade.ts` | SupabaseService, BranchFacade, ToastService | ✅ Estable |

## 3. Edge Functions (Supabase)
Funciones server-side que requieren `SERVICE_ROLE_KEY` o lógica que no puede ejecutarse en el cliente.

| Edge Function | Responsabilidad | Archivo | Caller | Estado |
|---|---|---|---|---|
| `create-secretary` | Crea usuario en Auth (`admin.createUser`, password = RUT sin dígito verificador, `email_confirm: true`) + INSERT en `public.users`. Rollback: elimina auth user si INSERT falla. Solo admin. | `supabase/functions/create-secretary/index.ts` | `SecretariasFacade` | ✅ Estable |
| `update-secretary` | Actualiza datos de secretaria en `public.users`. Si email cambia: `auth.admin.updateUserById`. Solo admin. | `supabase/functions/update-secretary/index.ts` | `SecretariasFacade` | ✅ Estable |
| `create-instructor` | Crea usuario en Auth + INSERT en `public.users` + `instructors`. Rollback si INSERT falla. Solo admin y secretaria. | `supabase/functions/create-instructor/index.ts` | `InstructoresFacade` | ✅ Estable |
| `update-instructor` | Actualiza datos de instructor. Si email cambia: `auth.admin.updateUserById`. Solo admin y secretaria. | `supabase/functions/update-instructor/index.ts` | `InstructoresFacade` | ✅ Estable |
| `activate-student-account` | Crea cuenta Auth para alumno matriculado via `auth.admin.inviteUserByEmail` + actualiza `users.supabase_uid`. Idempotente: si `supabase_uid` ya existe y `first_login=true` reenvía invitación; si `first_login=false` retorna 409. Body: `{ userId: number, email: string }`. Solo admin y secretaria. | `supabase/functions/activate-student-account/index.ts` | `EnrollmentFacade.confirmWithPayment()` (fire-and-forget, tras la RPC atómica) | ✅ Estable |
| `student-payment` | Orquestador del pago de segunda mitad Clase B (alumno autenticado). **JWT:** validación local (decodifica payload sin llamada HTTP a `/auth/v1/user`). Acciones: `load-enrollment-status` (delega a RPC `get_student_payment_status` — 1 round-trip vs. 5 anteriores), `load-instructor-schedule` (grilla `v_class_b_schedule_availability`; holds ajenos se marcan `occupied`, **holds propios se devuelven como `myHeldSlotIds`** para pre-selección en la UI), `reserve-slots` (crea slot_holds TTL 20min, máx 6, verifica conflictos), `release-slots` (libera holds al retroceder), `initiate-payment` (**NO crea `class_b_sessions`** — solo registra `payment_attempt` con snapshot que incluye `vehicleId` + inicia Webpay Plus; idempotente via session_token), `confirm-payment` (commit Webpay → **crea `class_b_sessions` directamente como `'scheduled'`** ordenando slots cronológicamente y asignando **`class_number` 7-12** — siempre segunda mitad — usando slots y vehicleId del snapshot + registra `payments` → trigger recalcula balances; si rechazado solo marca attempt como `failed` sin sesiones que cancelar). **Cambio arquitectónico:** sesiones se crean solo tras pago confirmado para evitar sesiones huérfanas si el alumno abandona Transbank. Usa tarjetas de prueba Transbank (VISA 4051 8856 0044 6623 / MasterCard 5186 0595 5959 0568). | `supabase/functions/student-payment/index.ts` | `StudentPaymentFacade` | ✅ Estable |
| `public-enrollment` | Orquestador de matrícula pública (anónima). Acciones: `load-instructors`, `load-schedule`, `reserve-slots`, `release-slots`, `submit-clase-b`, `submit-pre-inscription`, `initiate-payment`, `confirm-payment`, `get-carnet-upload-url`, `generate-contract-preview`, `check-duplicate`. **`check-duplicate`** (check anticipado, sin side effects): recibe `{ rut, licenseClass }`, busca el user por RUT → verifica `enrollments` activos/completados para el mismo `license_class` + `professional_pre_registrations` pendientes (solo para profesional); retorna `{ eligible: true }` o `{ eligible: false, error, message }`. Llamado por `PublicEnrollmentFacade.savePersonalData()` al avanzar del paso de datos personales. **Check tardío** (garantía de seguridad): misma lógica ejecutada también al momento del submit final en `initiate-payment`, `submit-clase-b` y `submit-pre-inscription`. Bypasea RLS con service role. | `supabase/functions/public-enrollment/index.ts` | `PublicEnrollmentFacade` | ✅ Estable |
| `generate-contract-pdf` | Genera PDF del contrato de matrícula y lo sube a Storage. Importa lógica de generación desde `_shared/contract-pdf.ts`. | `supabase/functions/generate-contract-pdf/index.ts` | `EnrollmentFacade`, `AdminPreInscritosFacade` | ✅ Estable |
| `_shared/contract-pdf.ts` | Módulo compartido Deno con tipos y utilidades de generación PDF de contratos: `EnrollmentData`, `buildStructuredPdf()`, `assemblePdf()`, `escapePdfWinAnsi()`, `wrapTextToLines()`, `formatDate()`, `formatCurrency()`. Importado por `generate-contract-pdf` y `public-enrollment` (acción `generate-contract-preview`). | `supabase/functions/_shared/contract-pdf.ts` | `generate-contract-pdf`, `public-enrollment` | ✅ Estable |
| `generate-certificate-b-pdf` | Genera PDF de certificado Clase B. | `supabase/functions/generate-certificate-b-pdf/index.ts` | `CertificacionClaseBFacade` | ✅ Estable |
| `generate-certificate-professional-pdf` | Genera PDF de certificado Clase Profesional. | `supabase/functions/generate-certificate-professional-pdf/index.ts` | `CertificacionProfesionalFacade` | ✅ Estable |
| `generate-class-book-pdf` | Genera PDF del libro de clases profesional. | `supabase/functions/generate-class-book-pdf/index.ts` | `LibroDeClasesFacade` | ✅ Estable |
| `send-zoom-email` | Envía el enlace Zoom de una clase teórica por correo a los alumnos indicados. Body: `{ zoomLink, sessionTopic, sessionDate, recipients: { name, email }[] }`. Usa SMTP propio (Secrets: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`) vía `npm:nodemailer@6`. Retorna `{ sent: number; errors: string[] }`. Solo usuarios autenticados. | `supabase/functions/send-zoom-email/index.ts` | `AsistenciaClaseBFacade.callSendZoomEmail()` | ✅ Estable |
| `send-certificate-email` | Envía el certificado de finalización (Clase B o Profesional) por correo al alumno con el PDF adjunto. Body: `{ enrollment_id: number; type?: 'class_b' \| 'professional' }`. Descarga el PDF desde Supabase Storage (signed URL), lo adjunta al email vía `npm:nodemailer@6`. Loguea `email_sent` en `certificate_issuance_log`. Retorna `{ success: true; recipientEmail: string }`. Solo usuarios autenticados. Secrets: mismos SMTP que `send-zoom-email`. | `supabase/functions/send-certificate-email/index.ts` | `CertificacionClaseBFacade.enviarEmail()`, `CertificacionProfesionalFacade.enviarEmail()` | ✅ Estable |
| `export-certificates-zip` | Empaqueta todos los certificados PDF generados en un ZIP y lo retorna como binario (`application/zip`). Body opcional: `{ branch_id?: number; type?: 'class_b' \| 'professional' }`. Obtiene signed URLs (TTL 300s), descarga cada PDF, los añade a JSZip (`npm:jszip@3`) con nombre `CERT[-PROF]-YYYY-NNNN_APELLIDOS_NOMBRES.pdf` (tildes normalizadas). Maneja colisiones de nombre. Retorna ZIP con `Content-Disposition: attachment`. Solo usuarios autenticados. | `supabase/functions/export-certificates-zip/index.ts` | `CertificacionClaseBFacade.exportar()`, `CertificacionProfesionalFacade.exportar()` | ✅ Estable |
| `generate-enrollment-sheet` | Genera la Ficha de Matrícula en PDF **on-demand** (sin almacenamiento). Body: `{ enrollment_id: number }`. Consulta datos actuales: enrollment+student+user+branch+course+payments y rama por `license_group`: Clase B → `v_student_progress_b`; Profesional → `professional_theory/practice_attendance` + `professional_module_grades`. Retorna binario `application/pdf` directamente con `Content-Disposition: attachment`. No guarda en Storage ni requiere columna en BD. Solo usuarios autenticados. | `supabase/functions/generate-enrollment-sheet/index.ts` | `AdminAlumnosFacade.exportarFicha()` | ✅ Estable |
| `generate-cash-closing-report` | Genera reporte de Cuadratura Diaria de Caja en Excel (JSON payload) o PDF multi-página **on-demand** (sin almacenamiento). Body: `{ format: 'excel'\|'pdf', date: 'YYYY-MM-DD', branch_id: number\|null }`. Consulta: `payments` (con JOIN enrollments→students→users para nombre y RUT), `expenses`, `instructor_advances` y `cash_closings` del día. **Excel:** retorna JSON `{ sheetName, rows, filename }` para que el frontend genere el XLSX vía `downloadExcel()`. **PDF:** retorna binario `application/pdf` multi-página con cabecera, 4 KPI boxes, tabla de ingresos (hora/boleta/alumno/RUT/concepto/efectivo/transfer/SENCE/total), tabla de egresos y anticipos, desglose de billetes y monedas (si caja cerrada), resultado del arqueo con estado visual (cuadrado/sobrante/faltante). Solo usuarios autenticados. | `supabase/functions/generate-cash-closing-report/index.ts` | `CuadraturaFacade.exportar()` | ✅ Estable |
| `generate-financial-report` | Genera Reporte Contable RF-030/RF-031 en Excel (JSON payload) o PDF multi-página **on-demand** (sin almacenamiento). Body: `{ format: 'excel'\|'pdf', desde: 'YYYY-MM-DD', hasta: 'YYYY-MM-DD', branch_id: number\|null }`. Consulta: `payments` (JOIN enrollments→branch_id+license_group→students→users) filtrado por `payment_date` en rango; `expenses` filtrado por `date` en rango + `branch_id` opcional. Computa en servidor: KPIs (totalIngresos, totalGastos, totalNeto, margen), ingresosCategoria (agrupado por license_group), gastosCategoria (agrupado por category), evolucionMensual (agrupado por YYYY-MM), detalleDiario (agrupado por fecha). **Excel:** JSON `{ sheetName, rows, filename }` con secciones: encabezado, resumen ejecutivo, ingresos/gastos por categoría, evolución mensual, detalle diario, detalle de pagos individuales, detalle de gastos individuales. **PDF:** binario `application/pdf` multi-página con cabecera azul, 4 KPI boxes, tablas de categorías, evolución mensual (solo si >1 mes) y detalle diario. Solo usuarios autenticados. | `supabase/functions/generate-financial-report/index.ts` | `ReportesContablesFacade.exportar()` | ✅ Estable |
| `generate-payment-report` | Genera Reporte de Pagos en PDF **multi-página on-demand** (sin almacenamiento). Body: `{ start_date, end_date, branch_id: number\|null }`. Requiere auth. Queries: `payments` + `enrollments` en rango de fechas (máx 500); `enrollments` con `pending_balance > 0` (snapshot actual). Secciones: cabecera con período/sede/generador, KPIs resumen (total recaudado, nro. pagos, saldo pendiente, método principal), tabla de pagos, tabla de deudores. `showBranchColumn` automático cuando `branch_id=null` (admin todas las sedes). Retorna binario `application/pdf` con `Content-Disposition: attachment`. | `supabase/functions/generate-payment-report/index.ts` | `PagosFacade.generarReporte()` | ✅ Estable |
| `generate-payroll-report` | Genera Nómina de Instructores en Excel (JSON payload) o PDF **on-demand** (sin almacenamiento). Body: `{ format: 'excel'\|'pdf', month: number, year: number, branch_id: number\|null }`. Requiere auth. **4 queries paralelas:** `instructors` (JOIN users con branch_id+rut), `instructor_monthly_hours` (filtrado por period YYYY-MM), `instructor_advances` (filtrado por rango de fechas del mes), `instructor_monthly_payments` (filtrado por period). Solo incluye instructores con actividad (horas>0 \| anticipos>0 \| registro de pago). Aplica filtro `branch_id` cuando no es null. **Excel:** JSON `{ sheetName, rows, filename }` con encabezado, resumen ejecutivo (KPIs), detalle por instructor y fila de totales. **PDF:** binario `application/pdf` raw (sin deps externas) con barra azul de cabecera, 4 KPI boxes (Total Nómina/Anticipos/Total a Pagar/Instructores), texto de progreso pagados/pendientes, tabla de instructores (Instructor/RUT/HRS/Base/Anticipos/A Pagar/Estado) con alternado de filas y texto de estado en color, fila de totales. Solo usuarios autenticados. | `supabase/functions/generate-payroll-report/index.ts` | `LiquidacionesFacade.exportar()` | ✅ Estable |
| `generate-cash-history-report` | Genera Historial Mensual de Cuadraturas en PDF **on-demand** (sin almacenamiento). Body: `{ month: number, year: number, branch_id: number\|null }`. Requiere auth. Query: `cash_closings` (JOIN `users`) filtrado por rango `yyyy-mm-01` a `yyyy-mm-{lastDay}` + `branch_id` opcional. Construye `CierreRow[]` con: fecha, cajero, fondoInicial, totalIngresos, totalEgresos, saldoSistema, saldoFisico, diferencia, estadoDiferencia (Cuadrado/Sobrante/Faltante). **PDF:** binario `application/pdf` raw (sin deps externas) con barra azul de cabecera (escuela, título "HISTORIAL DE CUADRATURAS", mes/año), 4 KPI boxes (Días con Cierre, Días Cuadrados, Total Ingresos, Total Egresos), tabla con columnas Fecha/Cajero/Ingresos/Egresos/Saldo Sist./Saldo Fís./Estado (texto de estado en color: verde=Cuadrado, naranja=Sobrante, rojo=Faltante), fila de totales, soporte multi-página. Solo usuarios autenticados. | `supabase/functions/generate-cash-history-report/index.ts` | `HistorialCuadraturasFacade.exportarMes('pdf')` | ✅ Estable |
| `generate-audit-report` | Genera Reporte de Log de Auditoría en Excel (JSON payload) o PDF A4 landscape **on-demand** (sin almacenamiento). Body: `{ format: 'excel'\|'pdf', branch_id: number\|null, fecha_desde: string\|null, fecha_hasta: string\|null, secretaria_id: number\|null, accion: string\|null, modulo: string\|null }`. Requiere auth. Query `audit_log` JOIN `users(roles)` filtrado por `roles.name='secretary'`, límite 5.000 registros. Aplica los mismos filtros que `AuditoriaFacade.fetchLogs()` pero sin paginación. Si no hay rango de fechas devuelve todos los registros. **Excel:** JSON `{ sheetName, rows, filename }` con encabezado de metadatos + fila de cabeceras + detalle completo. **PDF:** binario A4 landscape con barra azul de cabecera, metadatos (sede/período/generador/total), tabla (Fecha/Hora/Usuario/Email/Acción/Módulo/Detalle/IP) con colores por acción (verde=Crear, azul=Actualizar, rojo=Eliminar), alternado de filas, fila de totales con conteo C/A/E. Solo usuarios autenticados. | `supabase/functions/generate-audit-report/index.ts` | `AuditoriaFacade.exportar()` | ✅ Estable |
| `generate-student-license-pdf` | Genera el carnet PDF del alumno Clase B replicando el diseño físico de la autoescuela. Body: `{ enrollment_id: number }`. Queries: `enrollments`+`students`+`users`, `class_b_sessions` (para grilla horaria y nombre de instructor desde primera sesión), `student_documents` (type=`id_photo` para foto de carnet). `totalClases = sesiones.length <= 6 ? 6 : 12`. **Layout A5 portrait (421×595pt) dos columnas:** izquierda — tabla de clases prácticas (Nº/Día/Hora inicio/Hora Término/Firma), boxes APROBADO/REPROBADO (solo 6 clases); derecha — encabezado escuela, logo, foto alumno (58×72pt o placeholder), número de matrícula, datos del alumno, "CURSO CLASE B". Foto PNG vía `ASCIIHexDecode`; JPEG vía `[/ASCIIHexDecode /DCTDecode]`. Horarios en zona `America/Santiago` (`scheduled_at` + 45 min = hora término). Sube a `documents/student-licenses/{enrollment_id}/Carnet_{safeName}.pdf`. UPDATE `enrollments.license_pdf_url = storagePath`. Retorna `{ pdfUrl: signedUrl(1h), pdfPath }`. Solo autenticados. | `supabase/functions/generate-student-license-pdf/index.ts` | `AdminAlumnoDetalleFacade.generarCarnet()` | ✅ Estable |

## Auto-Index — Servicios detectados por AST (generado automáticamente)

<!-- AUTO-GENERATED:BEGIN -->
| Clase | Dependencias | Archivo |
|-------|-------------|---------|
| `MenuConfigService` | `AuthFacade` | `src/app/core/services/auth/menu-config.service.ts` |
| `RoleService` | — | `src/app/core/services/auth/role.service.ts` |
| `SupabaseService` | — | `src/app/core/services/infrastructure/supabase.service.ts` |
| `BreadcrumbService` | `Router`, `MenuConfigService` | `src/app/core/services/ui/breadcrumb.service.ts` |
| `ConfirmModalService` | — | `src/app/core/services/ui/confirm-modal.service.ts` |
| `DmsViewerService` | — | `src/app/core/services/ui/dms-viewer.service.ts` |
| `GsapAnimationsService` | `PLATFORM_ID`, `NgZone` | `src/app/core/services/ui/gsap-animations.service.ts` |
| `LayoutDrawerFacadeService` | `LayoutDrawerService` | `src/app/core/services/ui/layout-drawer.facade.service.ts` |
| `LayoutDrawerService` | — | `src/app/core/services/ui/layout-drawer.service.ts` |
| `LayoutService` | — | `src/app/core/services/ui/layout.service.ts` |
| `ModalOverlayService` | — | `src/app/core/services/ui/modal-overlay.service.ts` |
| `SearchPanelFacadeService` | `SearchPanelFacadeService` | `src/app/core/services/ui/search-panel.service.ts` |
| `TaskCreateContextService` | — | `src/app/core/services/ui/task-create-context.service.ts` |
| `ThemeService` | `ThemeService`, `PLATFORM_ID`, `GsapAnimationsService`, `MessageService` | `src/app/core/services/ui/theme.service.ts` |
| `ToastService` | `MessageService` | `src/app/core/services/ui/toast.service.ts` |

## Utils (funciones puras en `core/utils/`)

| Función / Export | Descripción | Path |
|---|---|---|
| `getSparklinePoints(data, w?, h?)` | Convierte array de valores 0-1 en string de puntos SVG para `<polyline>`. 6-8 puntos típicos, w=40 h=20 default. | `src/app/core/utils/sparkline.utils.ts` |
| `buildCarnetMenu(state)` | Construye items `SectionHeroMenuItem[]` para el menú Carnet Clase B (fix-019). Reglas: carnet-6 siempre, carnet-12 requiere 6 clases completadas. | `src/app/core/utils/carnet-menu.util.ts` |

<!-- AUTO-GENERATED:END -->
